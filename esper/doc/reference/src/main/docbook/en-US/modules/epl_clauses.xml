<chapter xml:id="epl_clauses" version="5.0"
         xsi:schemaLocation="http://docbook.org/ns/docbook http://www.docbook.org/xml/5.0/xsd/docbook.xsd http://www.w3.org/1999/xlink http://www.docbook.org/xml/5.0/xsd/xlink.xsd"
         xml:base="./" xmlns="http://docbook.org/ns/docbook" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xlink="http://www.w3.org/1999/xlink"
         xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:ns="http://docbook.org/ns/docbook">

    <title>EPL Reference: Clauses</title>
        
    <sect1 xml:id="epl-intro">
        <title>EPL Introduction</title>

        <para>
			The Event Processing Language (EPL) is a SQL-standard language with extensions, offering <literal>SELECT</literal>, <literal>FROM</literal>, <literal>WHERE</literal>, <literal>GROUP BY</literal>, <literal>HAVING</literal> and <literal>ORDER BY</literal> clauses. Streams replace tables as the source of data with events replacing rows as the basic unit of data. Since events are composed of data, the SQL concepts of correlation through joins, subqueries and aggregation through grouping can be effectively leveraged.
        </para>

        <para>
			The <literal>INSERT INTO</literal> clause is recast as a means of forwarding events to other streams for further processing. External data may be queried and joined with the stream data. Additional clauses such as the <literal>PATTERN</literal> and <literal>OUTPUT</literal> clauses are available to provide the missing SQL language constructs specific to event processing.
        </para>
        
        <para>
			EPL statements can specify data windows. Similar to tables in a SQL statement, data windows define the subset of events to be analyzed. 
            Data windows can be combined to an intersection or union of sets of events.
            Some of the often-used data windows are <literal>#length</literal>, <literal>#time</literal>, <literal>#unique</literal>, <literal>#lastevent</literal>, <literal>#firstevent</literal> and <literal>#keepall</literal>.
        </para>
        
        <para>
			EPL provides the concept of <emphasis>named window</emphasis>. Named windows are data windows that can be used by multiple statements. 
			The name of a named window can occur in a statement's <literal>FROM</literal> clause to query the named window or to include the named window in a join or subquery. 
        </para>

        <para>
			EPL provides the concept of <emphasis>table</emphasis>. Tables are globally-visible data structures that typically have primary key columns and that can hold aggregation state. 
			An overview of named windows and tables, and a comparison between them, can be found at <xref linkend="nwtable-overview"/>.
        </para>

        <para>
			EPL allows execution of on-demand (fire-and-forget, non-continuous, triggered by API) queries against named windows and tables through the runtime API. The query engine automatically indexes named window data for fast access by <literal>ON SELECT/MERGE/UPDATE/INSERT/DELETE</literal> without the need to create an index explicitly, or can access explicit (secondary) table indexes for operations on tables. For fast on-demand query execution via runtime API use the <literal>CREATE INDEX</literal> syntax to create an explicit index for the named window or table in question.
        </para>

        <para>
			Use <literal>CREATE SCHEMA</literal> to declare an event type.
        </para>

        <para>
			<emphasis>Variables</emphasis> can come in handy to parameterize statements and change parameters on-the-fly and in response to events. Variables can be used in an expression anywhere in a statement as well as in the output clause for dynamic control of output rates.
        </para>

        <para>
			Esper can be extended by plugging-in custom developed data windows, aggregation functions, and more.
        </para>

    </sect1>

    <sect1 xml:id="epl-select-syntax">
        <title>EPL Syntax</title>
        
		<para>
			EPL queries are created and stored in the engine, and publish results to listeners as events are received by the engine or timer events occur that match the criteria specified in the query. Events can also be obtained from running EPL queries via the <literal>safeIterator</literal> and <literal>iterator</literal> methods that provide a pull-data API.
		</para>

		<para>
			The <literal>select</literal> clause in an EPL query specifies the event properties or events to retrieve. The <literal>from</literal> clause in an EPL query specifies the event stream definitions and stream names to use. The <literal>where</literal> clause in an EPL query specifies search conditions that specify which event or event combination to search for. For example, the following statement returns the average price for IBM stock ticks in the last 30 seconds.
		</para>

			<programlisting><![CDATA[select avg(price) from StockTick#time(30 sec) where symbol='IBM']]></programlisting>

		<para>
			EPL queries follow the below syntax. EPL queries can be simple queries or more complex queries. A simple select contains only a <literal>select</literal> clause and a single stream definition. Complex EPL queries can be build that feature a more elaborate select list utilizing expressions, may join multiple streams, may contain a <literal>where</literal> clause with search conditions and so on.
		</para>

<synopsis>[<emphasis>annotations</emphasis>]
[<emphasis>expression_declarations</emphasis>]
[context <emphasis>context_name</emphasis>]
[into table <emphasis>table_name</emphasis>]
[insert into <emphasis>insert_into_def</emphasis>]
select <emphasis>select_list</emphasis>
from <emphasis>stream_def</emphasis> [as name] [, <emphasis>stream_def</emphasis> [as name]] [,...]
[where <emphasis>search_conditions</emphasis>]
[group by <emphasis>grouping_expression_list</emphasis>]
[having <emphasis>grouping_search_conditions</emphasis>]
[output <emphasis>output_specification</emphasis>]
[order by <emphasis>order_by_expression_list</emphasis>]
[limit <emphasis>num_rows</emphasis>]
</synopsis>

		<sect2 xml:id="epl-syntax-time-periods">
			<title>Specifying Time Periods</title>
			
			<para>
					Time-based windows as well as pattern observers and guards take a time period as a parameter. Time periods follow the syntax below.
			</para>
		
			<synopsis>time-period : [<emphasis>year-part</emphasis>] [<emphasis>month-part</emphasis>] [<emphasis>week-part</emphasis>] [<emphasis>day-part</emphasis>] [<emphasis>hour-part</emphasis>] 
      [<emphasis>minute-part</emphasis>] [<emphasis>seconds-part</emphasis>] [<emphasis>milliseconds-part</emphasis>] [<emphasis>microseconds-part</emphasis>]

year-part : <emphasis>(number|variable_name)</emphasis> ("years" | "year")
month-part : <emphasis>(number|variable_name)</emphasis> ("months" | "month")
week-part : <emphasis>(number|variable_name)</emphasis> ("weeks" | "week")
day-part : <emphasis>(number|variable_name)</emphasis> ("days" | "day")
hour-part : <emphasis>(number|variable_name)</emphasis> ("hours" | "hour")
minute-part : <emphasis>(number|variable_name)</emphasis> ("minutes" | "minute" | "min")
seconds-part : <emphasis>(number|variable_name)</emphasis> ("seconds" | "second" | "sec")
milliseconds-part : <emphasis>(number|variable_name)</emphasis> ("milliseconds" | "millisecond" | "msec")
microseconds-part : <emphasis>(number|variable_name)</emphasis> ("microseconds" | "microsecond" | "usec")</synopsis>
                
			<para>
					Some examples of time periods are:
			</para>
			<programlisting>10 seconds
10 minutes 30 seconds
20 sec 100 msec
1 day 2 hours 20 minutes 15 seconds 110 milliseconds 5 microseconds
0.5 minutes
1 year
1 year 1 month</programlisting>

			<para>
				Variable names and substitution parameters '<literal>?</literal>' for prepared statements are also allowed as part of a time period expression.
			</para>
						
			<note>
				<para>
					When the time period has a month or year part, all values must be integer-type values.
				</para>
			</note>
		</sect2>
		
		<sect2 xml:id="epl-syntax-comments">
			<title>Using Comments</title>
			<para>
				Comments can appear anywhere in the EPL or pattern statement text where whitespace is allowed. Comments can be written in two ways: slash-slash (<literal>// ...</literal>) comments and slash-star (<literal>/* ... */</literal>) comments.
			</para>
	
			<para>
				Slash-slash comments extend to the end of the line:
			</para>
			<programlisting><![CDATA[// This comment extends to the end of the line.
// Two forward slashes with no whitespace between them begin such comments.

select * from MyEvent  // this is a slash-slash comment

// All of this text together is a valid statement.]]></programlisting>
	
			<para>
				Slash-star comments can span multiple lines:
			</para>
			<programlisting><![CDATA[/* This comment is a "slash-star" comment that spans multiple lines.
 * It begins with the slash-star sequence with no space between the '/' and '*' characters.
 * By convention, subsequent lines can begin with a star and are aligned, but this is 
 * not required.
 */		
select * from MyEvent  /* this also works */]]></programlisting>

			<para>
				Comments styles can also be mixed:
			</para>
			<programlisting><![CDATA[select field1, // first comment
  /* second comment*/  field2
  from MyEvent]]></programlisting>
		</sect2>

		<sect2 xml:id="epl-syntax-keywords">
			<title>Reserved Keywords</title>
			<indexterm><primary>keywords</primary></indexterm>

			<para>
				Certain words such as <literal>select</literal>, <literal>delete</literal> or <literal>set</literal> are reserved and may not be used as identifiers. Please consult <xref linkend="appendix_keywords"/> for the list of reserved keywords and permitted keywords.
			</para>

			<para>
				Names of built-in functions and certain auxiliary keywords are permitted as event property names and in the rename syntax of the <literal>select</literal> clause. For example, <literal>count</literal> is acceptable. 
			</para>
			
			<para>
				Consider the example below, which assumes that <literal>'last'</literal> is an event property of MyEvent:
			</para>
			<programlisting><![CDATA[// valid
select last, count(*) as count from MyEvent]]></programlisting>
		
			<para>
				This example shows an incorrect use of a reserved keyword:
			</para>
			<programlisting><![CDATA[// invalid
select insert from MyEvent]]></programlisting>

			<para>
				EPL offers an escape syntax for reserved keywords: Event properties as well as event or stream names may be escaped via the backwards apostrophe <literal>`</literal> (ASCII 96) character.
			</para>

			<para>
				The next example queries an event type by name <literal>Order</literal> (a reserved keyword) that provides a property by name <literal>insert</literal> (a reserved keyword):
			</para>
			<programlisting><![CDATA[// valid
select `insert` from `Order`]]></programlisting>
		</sect2>

		<sect2 xml:id="epl-syntax-escapestring">
			<title>Escaping Strings</title>
			<indexterm><primary>escape</primary></indexterm>

			<para>
				You may surround string values by either double-quotes (<literal>"</literal>) or single-quotes (<literal>'</literal>). When your string constant in an EPL statement itself contains double quotes or single quotes,
				you must escape the quotes.
			</para>

			<para>
				Double and single quotes may be escaped by the backslash (<literal>\</literal>) character or by unicode notation. Unicode 0027 is a single quote (<literal>'</literal>) and 0022 is a double quote (<literal>"</literal>).
			</para>

			<para>
			  Escaping event property names is described in <xref linkend="eventrep-properties-escaping"/>.
			</para>

			<para>
				The sample EPL below escapes the single quote in the string constant <literal>John's</literal>, and filters out order events where the name value matches:
			</para>
			<programlisting><![CDATA[select * from OrderEvent(name='John\'s')
// ...equivalent to...
select * from OrderEvent(name='John\u0027s')]]></programlisting>

			<para>
				The next EPL escapes the string constant <literal>Quote "Hello"</literal>:
			</para>
			<programlisting><![CDATA[select * from OrderEvent(description like "Quote \"Hello\"")
// is equivalent to
select * from OrderEvent(description like "Quote \u0022Hello\u0022")]]></programlisting>

			<para>
				When building an escape string via the API, escape the backslash, as shown in below code snippet:
			</para>
			<programlisting><![CDATA[epService.getEPAdministrator().createEPL("select * from OrderEvent(name='John\\'s')");
// ... and for double quotes...
epService.getEPAdministrator().createEPL("select * from OrderEvent(
  description like \"Quote \\\"Hello\\\"\")");]]></programlisting>
		</sect2>

		<sect2 xml:id="epl-syntax-datatype">
			<title>Data Types</title>
			<indexterm><primary>data types</primary></indexterm>
			
			<para>
				For NEsper .NET also see <xref linkend="appendix_dotnet_epl_syntax_datatype"/>.
			</para>

			<para>
				EPL honors all Java built-in primitive and boxed types, including <literal>java.math.BigInteger</literal> and <literal>java.math.BigDecimal</literal>.
			</para>

			<para>
				EPL also follows Java standards in terms of widening, performing widening automatically in cases where widening type conversion is allowed without loss of precision, for both boxed and primitive types and including <literal>BigInteger</literal> and <literal>BigDecimal</literal>:
			</para>
			<orderedlist spacing="compact">
				<listitem>
					<para>
						byte to short, int, long, float, double, BigInteger or BigDecimal
					</para>
				</listitem>
				<listitem>
					<para>
						short to int, long, float, or double, BigInteger or BigDecimal
					</para>
				</listitem>
				<listitem>
					<para>
						char to int, long, float, or double, BigInteger or BigDecimal
					</para>
				</listitem>
				<listitem>
					<para>
						int to long, float, or double, BigInteger or BigDecimal
					</para>
				</listitem>
				<listitem>
					<para>
						long to float or double, BigInteger or BigDecimal
					</para>
				</listitem>
				<listitem>
					<para>
						float to double or BigDecimal
					</para>
				</listitem>
				<listitem>
					<para>
						double to BigDecimal
					</para>
				</listitem>
			</orderedlist>

			<para>
				In cases where loss of precision is possible because of narrowing requirements, EPL compilation outputs a compilation error.
			</para>

			<para>
				EPL supports casting via the <literal>cast</literal> function.
			</para>

			<para>
				EPL returns double-type values for division regardless of operand type. EPL can also be configured to follow Java rules for integer arithmetic instead as described in <xref linkend="config-engine-expression"/>.
			</para>

			<para>
				Division by zero returns positive or negative infinity. Division by zero can be configured to return null instead.
			</para>

			<sect3 xml:id="epl-syntax-datatype-constants">
				<title>Data Type of Constants</title>
				<indexterm><primary>constants</primary></indexterm>
				<indexterm><primary>literals</primary></indexterm>
	
				<para>
					An EPL constant is a number or a character string that indicates a fixed value. Constants can be used as expressions in many EPL statements, including variable assignment and case-when statements. They can also be used as parameter values for many built-in objects and clauses. Constants are also called literals.				
				</para>
				
				<para>
					EPL supports the standard SQL constant notation as well as Java data type literals.
				</para>
	
				<para>
					The following are types of EPL constants:
				</para>
	
				<table frame="topbot">
					<title>Types of EPL constants</title>
					<tgroup cols="3">
						<colspec colwidth="0.20*"/>
						<colspec colwidth="1*"/>
						<colspec colwidth="0.75*"/>
						<thead>
							<row>
								<entry>Type</entry>
								<entry>Description</entry>
								<entry>Examples</entry>
							</row>
						</thead>
						<tbody>
							<row>
								<entry>string</entry>
								<entry>A single character to an unlimited number of characters. Valid delimiters are the single quote (') or double quote (").</entry>
								<entry><programlisting><![CDATA[select 'volume' as field1,
   "sleep" as field2, 
  "\u0041" as unicodeA]]></programlisting></entry>
							</row>
							<row>
								<entry>boolean</entry>
								<entry>A boolean value.</entry>
								<entry><programlisting><![CDATA[select true as field1, 
  false as field2]]></programlisting></entry>
							</row>
							<row>
								<entry>integer</entry>
								<entry>An integer value (4 byte).</entry>
								<entry><programlisting><![CDATA[select 1 as field1, 
  -1 as field2, 
  1e2 as field3]]></programlisting></entry>
							</row>
							<row>
								<entry>long</entry>
								<entry>A long value (8 byte). Use the "L" or "l" (lowercase L) suffix.</entry>
								<entry><programlisting><![CDATA[select 1L as field1, 
  1l as field2]]></programlisting></entry>
							</row>
							<row>
								<entry>double</entry>
								<entry>A double-precision 64-bit IEEE 754 floating point.</entry>
								<entry><programlisting><![CDATA[select 1.67 as field1, 
  167e-2 as field2, 
  1.67d as field3]]></programlisting></entry>
							</row>
							<row>
								<entry>float</entry>
								<entry>A single-precision 32-bit IEEE 754 floating point. Use the "f" suffix.</entry>
								<entry><programlisting><![CDATA[select 1.2f as field1, 
  1.2F as field2]]></programlisting></entry>
							</row>
							<row>
								<entry>byte</entry>
								<entry>A 8-bit signed two's complement integer.</entry>
								<entry><programlisting><![CDATA[select 0x10 as field1]]></programlisting></entry>
							</row>
						</tbody>
					</tgroup>				
				</table>
				
				<para>
					EPL does not have a single-byte character data type for its literals. Single character literals are treated as string.
				</para>
				
				<para>
					Internal byte representation and boundary values of constants follow the Java standard.
				</para>

			</sect3>

			<sect3 xml:id="epl-syntax-datatype-bignumber">
				<title>BigInteger and BigDecimal</title>

				<para>
					EPL automatically performs widening of numbers to <literal>BigInteger</literal> and <literal>BigDecimal</literal> as required, and employs the respective <literal>equals</literal>, <literal>compareTo</literal> and arithmetic methods provided by <literal>BigInteger</literal> and <literal>BigDecimal</literal>.
				</para>
	
				<para>
					To explicitly create <literal>BigInteger</literal> and <literal>BigDecimal</literal> constants in EPL, please use the cast syntax : <literal>cast(</literal><emphasis>value</emphasis><literal>, BigInteger)</literal>.
				</para>
				
				<para>
					Note that since <literal>BigDecimal.valueOf(1.0)</literal> is not the same as <literal>BigDecimal.valueOf(1)</literal> (in terms of equality through <literal>equals</literal>), care should be taken towards the consistent use of scale.
				</para>
				
				<para>
					When using aggregation functions for <literal>BigInteger</literal> and <literal>BigDecimal</literal> values, please note these limitations:
				</para>
				<orderedlist spacing="compact">
					<listitem>
						<para>
							The <literal>median</literal>, <literal>stddev</literal> and <literal>avedev</literal> aggregation functions operate on the double value of the object and return a double value.
						</para>
					</listitem>
					<listitem>
						<para>
							All other aggregation functions return <literal>BigDecimal</literal> or <literal>BigInteger</literal> values (except <literal>count</literal>).
						</para>
					</listitem>
				</orderedlist>

				<para>
				  For <literal>BigDecimal</literal> precision and rounding, please see <xref linkend="config-engine-expression-mathcontext"/>. For division operations with BigDecimal number we recommend
				  configuring a math context.
				</para>
			</sect3>

		</sect2>

        <sect2 xml:id="epl-syntax-javaconst">
           <title>Using Constants and Enum Types</title>
			<indexterm><primary>constants</primary></indexterm>
			<indexterm><primary>enum types</primary></indexterm>
			<para>
				This chapter is about Java language constants and enum types and their use in EPL expressions. 
			</para>
			<para>
				Java language constants are public static final fields in Java that may participate in expressions of all kinds, as this example shows:
			</para>
			<programlisting><![CDATA[select * from MyEvent where property = MyConstantClass.FIELD_VALUE]]></programlisting>
	
			<para>
				Event properties that are enumeration values can be compared by their enum type value:
			</para>
			<programlisting><![CDATA[select * from MyEvent where enumProp = EnumClass.ENUM_VALUE_1]]></programlisting>
	
			<para>
				Event properties can also be passed to enum type functions or compared to an enum type method result:
			</para>
			<programlisting><![CDATA[select * from MyEvent where somevalue = EnumClass.ENUM_VALUE_1.getSomeValue()
  or EnumClass.ENUM_VALUE_2.analyze(someothervalue)]]></programlisting>

			<para>
				Enum types have a <literal>valueOf</literal> method that returns the enum type value:
			</para>
			<programlisting><![CDATA[select * from MyEvent where enumProp = EnumClass.valueOf('ENUM_VALUE_1')]]></programlisting>
	
			<para>
				If your application does not import, through configuration, the package that contains the enumeration class, then it must also specify the package name of the class. Enum types that are inner classes must be qualified with <literal>$</literal> following Java conventions.
			</para>
			<para>
				For example, the Color enum type as an inner class to <literal>MyEvent</literal> in package <literal>org.myorg</literal> can be referenced as shown:
			</para>
			<programlisting><![CDATA[select * from MyEvent(enumProp=org.myorg.MyEvent$Color.GREEN)#firstevent]]></programlisting>
	
			<para>
				Instance methods may also be invoked on event instances by specifying a stream name, as shown below:
			</para>
			<programlisting><![CDATA[select myevent.computeSomething() as result from MyEvent as myevent]]></programlisting>

			<para>
				Chaining instance methods is supported as this example shows:
			</para>
			<programlisting><![CDATA[select myevent.getComputerFor('books', 'movies').calculate() as result 
from MyEvent as myevent]]></programlisting>
		</sect2>

		<sect2 xml:id="epl-syntax-annotation">
			<title>Annotation</title>
			<indexterm><primary>annotation</primary></indexterm>

			<para>
				An annotation is an addition made to information in a statement. Esper provides certain built-in annotations for defining statement name, adding a statement description or for tagging statements such as for managing statements or directing statement output. 
				Other than the built-in annotations, applications can provide their own annotation classes that the EPL compiler can populate.
			</para>
			
			<para>
				An annotation is part of the statement text and precedes the EPL select or pattern statement. Annotations are therefore part of the EPL grammar. The syntax for annotations follows the host language (Java, .NET) annotation syntax:
			</para>
			<synopsis>@<emphasis>annotation_name</emphasis> [(<emphasis>annotation_parameters</emphasis>)]</synopsis>
			
			<para>
				An annotation consists of the annotation name and optional annotation parameters. The <emphasis>annotation_name</emphasis> is the simple class name or fully-qualified class name of the annotation class. The optional <emphasis>annotation_parameters</emphasis> 
				are a list of key-value pairs following the syntax:
			</para>
			<synopsis>@<emphasis>annotation_name</emphasis> (<emphasis>attribute_name</emphasis> = <emphasis>attribute_value</emphasis>, [name=value, ...])</synopsis>
			
			<para>
				The <emphasis>attribute_name</emphasis> is an identifier that must match the attributes defined by the annotation class. An <emphasis>attribute_value</emphasis> is a constant of any of the primitive types or string, an array, an enum type value or another (nested) annotation. Null values are not allowed as annotation attribute values. Enumeration values are supported in EPL statements and not support in statements created via the <literal>createPattern</literal> method.
			</para>
			
			<para>
				Use the <literal>getAnnotations</literal> method of <literal>EPStatement</literal> to obtain annotations provided via statement text.
			</para>

			<sect3 xml:id="epl-syntax-annotation-application">
				<title>Application-Provided Annotations</title>
				<indexterm><primary>annotation</primary><secondary>application-provided</secondary></indexterm>
				
				<para>
					Your application may provide its own annotation classes. The engine detects and populates annotation instances for application annotation classes.
				</para>

				<para>
					The name of application-provided annotations is case-sensitive.
				</para>

				<para>
					To enable the engine to recognize application annotation classes, your annotation name must include the package name (i.e. be fully-qualified) or your engine configuration must import the annotation class or package via the configuration API.  
				</para>

				<para>
					For example, assume that your application defines an annotation in its application code as follows:
				</para>
				<programlisting><![CDATA[public @interface ProcessMonitor {
  String processName();
  boolean isLongRunning default false;
  int[] subProcessIds;
}]]></programlisting>

				<para>
					Shown next is an EPL statement text that utilizes the annotation class defined earlier:
				</para>
				<programlisting><![CDATA[@ProcessMonitor(processName='CreditApproval',
  isLongRunning=true, subProcessIds = {1, 2, 3} )
  
select count(*) from ProcessEvent(processId in (1, 2, 3)#time(30)]]></programlisting>

				<para>
					Above example assumes the <literal>ProcessMonitor</literal> annotation class is imported via configuration XML or API. 					
				</para>
				
				<para>
					If <literal>ProcessMonitor</literal> should only be visible for use in annotations, use <literal>addAnnotationImport</literal> (or the <literal>auto-import-annotations</literal> XML tag).
					If <literal>ProcessMonitor</literal> should be visible in all of EPL including annotations, use <literal>addImport</literal> (or the <literal>auto-import</literal> XML tag).
				</para>

				<para>
					Here is an example API call to import for annotation-only all classes in package <literal>com.mycompany.app.myannotations</literal>:
				</para>
				<programlisting><![CDATA[epService.getEPAdministrator().getConfiguration().addAnnotationImport("com.mycompany.app.myannotations.*");]]></programlisting>

				<para>
					The next example imports the <literal>ProcessMonitor</literal> class only and only for annotation use:
				</para>
				<programlisting><![CDATA[epService.getEPAdministrator().getConfiguration().addAnnotationImport("com.mycompany.myannotations.ProcessMonitor");]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-withenum">
				<title>Annotations With Enumeration Values</title>
				<indexterm><primary>annotation</primary><secondary>enumeration value</secondary></indexterm>
				
				<para>
					For annotations that accept an enumeration value, the enumeration name does not need to be specified and matching is not case-sensitive.
				</para>
				
				<para>
					For example, assume the enum is:
				</para>
				<programlisting><![CDATA[public enum MyEnum {
    ENUM_VALUE_1,
    ENUM_VALUE_2;
}]]></programlisting>

				<para>
					Assume the annotation is:
				</para>
				<programlisting><![CDATA[public @interface MyAnnotationWithEnum {
    MyEnum myEnum();
}]]></programlisting>				

				<para>
					The EPL statement can specify:
				</para>
				<programlisting><![CDATA[@MyAnnotationWithEnum(myEnum = enum_value_1) select * from MyEvent]]></programlisting>				
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-builtin">
				<title>Built-In Statement Annotations</title>
				<indexterm><primary>annotation</primary><secondary>builtin</secondary></indexterm>
				
				<para>
					The name of built-in annotations is not case-sensitive, allowing both <literal>@NAME</literal> or <literal>@name</literal>, for example.
				</para>

				<para>
					The list of built-in EPL statement-level annotations is:
				</para>

				<table frame="topbot">
					<title>Built-In EPL Statement Annotations</title>
					<tgroup cols="3">
						<colspec colwidth="0.50*"/>
						<colspec colwidth="1*"/>
						<colspec colwidth="0.75*"/>
						<thead>
							<row>
								<entry>Name</entry>
								<entry>Purpose and Attributes</entry>
								<entry>Example</entry>
							</row>
						</thead>
						<tbody>
							<row>
								<entry>Name</entry>
								<entry>
									<para>Provides a statement name. Attributes are:</para>
									<para>value : Statement name.</para>
								</entry>
								<entry><programlisting><![CDATA[@Name("MyStatementName")]]></programlisting></entry>
							</row>
							<row>
								<entry>Description</entry>
								<entry>
									<para>Provides a statement textual description. Attributes are:</para>
									<para>value : Statement description.</para>
								</entry>
								<entry><programlisting><![CDATA[@Description("Place statement
description here.")]]></programlisting></entry>
							</row>
							<row>
								<entry>Tag</entry>
								<entry>
									<para>For tagging a statement with additional information. Attributes are:</para>
									<para>name : Tag name.</para>
									<para>value : Tag value.</para>
								</entry>
								<entry><programlisting><![CDATA[@Tag(name="MyTagName", 
 value="MyTagValue")]]></programlisting></entry>
							</row>
							<row>
								<entry>Priority</entry>
								<entry>
									<para>Applicable when an event (or schedule) matches filter criteria for multiple statements: Defines the order of statement processing (requires an engine-level setting).</para>
									<para>Attributes are:</para>
									<para>value : priority value.</para>
								</entry>
								<entry><programlisting><![CDATA[@Priority(10)]]></programlisting></entry>
							</row>
							<row>
								<entry>Drop</entry>
								<entry>
									<para>Applicable when an event (or schedule) matches filter criteria for multiple statements, drops the event after processing the statement (requires an engine-level setting).</para>
									<para>No attributes.</para>
								</entry>
								<entry><programlisting><![CDATA[@Drop]]></programlisting></entry>
							</row>
							<row>
								<entry>Hint</entry>
								<entry>
									<para>For providing one or more hints towards how the engine should execute a statement. Attributes are:</para>
									<para>value : A comma-separated list of one or more case-insensitive keywords.</para>
								</entry>
								<entry><programlisting><![CDATA[@Hint('iterate_only')]]></programlisting></entry>
							</row>
							<row>
								<entry>Hook</entry>
								<entry>
									<para>Use this annotation to register one or more statement-specific hooks providing a hook type for each individual hook, such as for SQL parameter, column or row conversion.</para>
									<para>Attributes are the hook <literal>type</literal> and the <literal>hook</literal> itself (usually a import or class name):</para>
								</entry>
								<entry><programlisting><![CDATA[@Hook(type=HookType.SQLCOL,
  hook='MyDBTypeConvertor')]]></programlisting></entry>
							</row>
							<row>
								<entry>Audit</entry>
								<entry>
									<para>Causes the engine to output detailed processing information for a statement.</para>
									<para>optional value : A comma-separated list of one or more case-insensitive keywords.</para>
								</entry>
								<entry><programlisting><![CDATA[@Audit]]></programlisting></entry>
							</row>
							<row>
								<entry>EventRepresentation</entry>
								<entry>
									<para>Causes the engine to use object-array or Avro event representation, if possible, for output and internal event types.</para>
								</entry>
								<entry>
									<para>
										For Object-Array:
									</para>
									<programlisting><![CDATA[@EventRepresentation(objectarray)]]></programlisting>
									<para>
										For Avro:
									</para>
									<programlisting><![CDATA[@EventRepresentation(avro)]]></programlisting></entry>
							</row>
							<row>
								<entry>IterableUnbound</entry>
								<entry>
									<para>For use when iterating statements with unbound streams, instructs the engine to retain the last event for iterating.</para>
								</entry>
								<entry><programlisting><![CDATA[@IterableUnbound]]></programlisting></entry>
							</row>
						</tbody>
					</tgroup>				
				</table>

				<para>
					The following example statement text specifies some of the built-in annotations in combination:
				</para>
				<programlisting><![CDATA[@Name("RevenuePerCustomer")
@Description("Outputs revenue per customer considering all events encountered so far.")
@Tag(name="grouping", value="customer")

select customerId, sum(revenue) from CustomerRevenueEvent]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-name">
				<title>@Name</title>
				<para>
					Use the @Name EPL annotation to specify a statement name within the EPL statement itself, as an alternative to specifying the statement name via API.
				</para>
				<para>
					If your application is also providing a statement name through the API, the statement name provided through the API overrides the annotation-provided statement name.
				</para>	
				<para>
					Example:
				</para>	
				<programlisting><![CDATA[@Name("SecurityFilter1") select * from SecurityFilter(ip="127.0.0.1")]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-description">
				<title>@Description</title>
				<para>
					Use the @Description EPL annotation to add a statement textual description.
				</para>
				<para>
					Example:
				</para>	
				<programlisting><![CDATA[@Description('This statement filters localhost.') select * from SecurityFilter(ip="127.0.0.1")]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-tag">
				<title>@Tag</title>
				<para>
					Use the @Tag EPL annotation to tag statements with name-value pairs, effectively adding a property to the statement. The attributes <literal>name</literal> and <literal>value</literal> are of type string.
				</para>
				<para>
					Example:
				</para>	
				<programlisting><![CDATA[@Tag(name='ip_sensitive', value='Y') 
@Tag(name='author', value='Jim')
select * from SecurityFilter(ip="127.0.0.1")]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-priority">
				<title>@Priority</title>
				<para>
					This annotation only takes effect if the engine-level setting for prioritized execution is set via configuration, as described in <xref linkend="config-engine-execution"/>.
				</para>

				<para>
					Use the @Priority EPL annotation to tag statements with a priority value. The default priority value is zero (0) for all statements. When an event (or single timer execution) requires processing the event for multiple statements, processing begins with the highest priority statement and ends with the lowest-priority statement.
				</para>
				<para>
					Example:
				</para>	
				<programlisting><![CDATA[@Priority(10) select * from SecurityFilter(ip="127.0.0.1")]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-drop">
				<title>@Drop</title>
				<para>
					This annotation only takes effect if the engine-level setting for prioritized execution is set via configuration, as described in <xref linkend="config-engine-execution"/>.
				</para>

				<para>
					Use the @Drop EPL annotation to tag statements that preempt all other same or lower-priority statements. When an event (or single timer execution) requires processing the event for multiple statements, processing begins with the highest priority statement and ends with the first statement marked with @Drop, which becomes the last statement to process that event.
				</para>

				<para>
					Unless a different priority is specified, the statement with the @Drop EPL annotation executes at priority 1. Thereby @Drop alone is an effective means to remove events from a stream.
				</para>
				
				<para>
					Example:
				</para>	
				<programlisting><![CDATA[@Drop select * from SecurityFilter(ip="127.0.0.1")]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-hint">
				<title>@Hint</title>
				<para>
					A hint can be used to provide tips for the engine to affect statement execution. Hints change performance or memory-use of a statement but generally do not change its output.
				</para>

				<para>
					The string value of a <literal>Hint</literal> annotation contains a keyword or a comma-separated list of multiple keywords. Hint keywords are case-insensitive. A list of hints is available in <xref linkend="perf-tips-15"/>.
				</para>

				<para>
					Example:
				</para>	
				<programlisting><![CDATA[@Hint('disable_reclaim_group') 
select ipaddress, count(*) from SecurityFilter#time(60 sec) group by ipaddress]]></programlisting>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-hook">
				<title>@Hook</title>
				<para>
					A hook is for attaching a callback to a statement.
				</para>

				<para>
					The type value of a <literal>@Hook</literal> annotation defines the type of hook and the <literal>hook</literal> value is an imported or fully-qualified class name providing the callback implementation.
				</para>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-audit">
				<title>@Audit</title>
				<para>
					Causes the engine to output detailed information about the statements processing. Described in more detail at <xref linkend="devlifecycle-debugging-audit"/>.
				</para>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-eventrepresentation">
				<title>@EventRepresentation</title>
				<para>
				  Use the <literal>@EventRepresentation</literal> annotation with <literal>create schema</literal> and <literal>create window</literal> statements to instruct the engine to use a specific event representation for the schema or named window.
				</para>

				<para>
				  Use the <literal>@EventRepresentation</literal> annotation with <literal>select</literal> statements to instruct the engine to use a specific event representation for output events.
				</para>

				<para>
				  When no <literal>@EventRepresentation</literal> annotation is specified, the engine uses the default event representation as configured, see <xref linkend="config-engine-eventmeta-representation"/>.
				</para>

				<para>
				  Use <literal>@EventRepresentation(objectarray)</literal> to instruct the engine to use object-array events.
				</para>

				<para>
				  Use <literal>@EventRepresentation(avro)</literal> to instruct the engine to use Avro events.
				</para>

				<para>
				  Use <literal>@EventRepresentation(map)</literal> to instruct the engine to use Map events.
				</para>
			</sect3>

			<sect3 xml:id="epl-syntax-annotation-iterableunbound">
				<title>@IterableUnbound</title>
				<para>
					Causes the engine, for statements with unbound streams, to retain the last event for the purpose of iterating using the iterator API. An engine-wide configuration is
					also available as described in <xref linkend="config-engine-viewresources-iterableunbound"/>.
				</para>
			</sect3>
		</sect2>

		<sect2 xml:id="epl-syntax-expression-alias">
			<title>Expression Alias</title>
			<indexterm><primary>expression alias</primary></indexterm>
			<indexterm><primary>lambda</primary><secondary>expression alias</secondary></indexterm>

			<para>
				An expression alias simply assigns a name to an expression. The alias name can be used in other expressions to refer to that expression, without the need to duplicate the expression.
			</para>

			<para>
				The expression alias obtains its scope from where it is used. Parameters cannot be provided. 
				A second means to sharing expressions is the expression declaration as described next, which allows passing parameters and is more tightly scoped.
			</para>

			<para>
				An EPL statement can contain and refer to any number of expression aliases. 
			    For expressions aliases that are visible across multiple EPL statements please consult <xref linkend="create-exprglobal-alias"/> 
			    that explains the <literal>create expression</literal> clause.
			</para>
						
			<para>
				The syntax for an expression alias is:
			</para>
			<synopsis>expression <emphasis>expression_name</emphasis> <literal>alias for { </literal><emphasis>expression</emphasis> <literal>}</literal></synopsis>
			
			<para>
				An expression alias consists of the expression name and an expression in curly braces.
				The return type of the expression is determined by the engine and need not be specified. 
				The scope is automatic and determined by where the alias name is used therefore parameters cannot be specified.
			</para>
			
			<para>
				This example declares an expression alias <literal>twoPI</literal> that substitutes <literal>Math.PI * 2</literal>:
			</para>
			<programlisting><![CDATA[expression twoPI alias for { Math.PI * 2 }
select twoPI from SampleEvent]]></programlisting>

			<para>
				The next example specifies an alias <literal>countPeople</literal> and uses the alias in the <literal>select</literal>-clause and the <literal>having</literal>-clause:
			</para>
			<programlisting><![CDATA[expression countPeople alias for { count(*) }
select countPeople from EnterRoomEvent#time(10 seconds) having countPeople > 10]]></programlisting>

			<para>
				When using the expression alias in an expression, empty parentheses can optionally be specified. In the above example, <literal>countPeople()</literal> can be used instead and equivalently.
			</para>			

			<para>
				The following scope rules apply for expression aliases:
			</para>			
			<orderedlist spacing="compact">
				<listitem>
					<para>
						Expression aliases do not remove implicit limitations: For example, aggregation functions cannot be used in a filter expression even if assigned an alias.
					</para>
				</listitem>
			</orderedlist>
		</sect2>

		<sect2 xml:id="epl-syntax-expression-decl">
			<title>Expression Declaration</title>
			<indexterm><primary>expression declaration</primary></indexterm>
			<indexterm><primary>lambda</primary><secondary>expression declaration</secondary></indexterm>

			<para>
				An EPL statement can contain expression declarations. Expressions that are common to multiple places in the same EPL statement can be moved to a named expression declaration and reused within the same statement without duplicating the expression itself.
			</para>
						
			<para>
			    For declaring expressions that are visible across multiple EPL statements i.e. globally visible expressions please consult <xref linkend="create-exprglobal-expr"/> 
			    that explains the <literal>create expression</literal> clause.
			</para>
			
			<para>
			    The engine may cache declared expression result values and reuse cache values, see <xref linkend="config-engine-execution-declaredexprvaluecachesize"/>.
			</para>

			<para>
				An expression declaration follows the lambda-style expression syntax. This syntax was chosen as it typically allows for a shorter and more concise expression body that can be easier to read then most procedural code. 
			</para>
			
			<para>
				The syntax for an expression declaration is:
			</para>
			<synopsis>expression <emphasis>expression_name</emphasis> { <emphasis>expression_body</emphasis> }</synopsis>
			
			<para>
				An expression declaration consists of the expression name and an expression body. The <emphasis>expression_name</emphasis> is any identifier. The <emphasis>expression_body</emphasis> contains optional parameters and the expression.
				The parameter types and the return type of the expression is determined by the engine and do not need to be specified.
			</para>
			
			<para>
				Parameters to a declared expression can be a stream name, pattern tag name or wildcard (<literal>*</literal>). Use wildcard to pass the event itself to the expression. In a join or subquery, or more generally in an expression where multiple streams or pattern tags are available, the EPL must specify the stream name or pattern tag name and cannot use wildcard.
			</para>
		
			<para>
				In the expression body the <literal>=&gt;</literal> lambda operator reads as "goes to" (<literal>-&gt;</literal> may be used and is equivalent). The left side of the lambda operator specifies the input parameters (if any) and the right side holds the expression. The lambda expression <literal>x => x * x</literal> is read "x goes to x times x".
			</para>
		
			<para>
				In the expression body, if your expression takes no parameters, you may simply specify the expression and do not need the <literal>=&gt;</literal> lambda operator.
			</para>
			
			<para>
				If your expression takes one parameters, specify the input parameter name followed by the <literal>=&gt;</literal> lambda operator and followed by the expression. The synopsis for use with a single input parameter is:
			</para>
			<synopsis>expression_body:   <emphasis>input_param_name</emphasis> => <emphasis>expression</emphasis> </synopsis>
			
			<para>
				If your expression takes two or more parameters, specify the input parameter names in parenthesis followed by the <literal>=&gt;</literal> lambda operator followed by the expression. The synopsis for use with a multiple input parameter is:
			</para>
			<synopsis>expression_body:   (<emphasis>input_param</emphasis> [,<emphasis>input_param</emphasis> [,...]]) => <emphasis>expression</emphasis> </synopsis>

			<para>
				The following example declares an expression that returns two times PI (ratio of the circumference of a circle to its diameter) and demonstrates its use in a select-clause:
			</para>
			<programlisting><![CDATA[expression twoPI { Math.PI * 2} select twoPI() from SampleEvent]]></programlisting>

			<para>
				The parentheses are optional when the expression accepts no parameters. The below is equivalent to the previous example:
			</para>
			<programlisting><![CDATA[expression twoPI { Math.PI * 2} select twoPI from SampleEvent]]></programlisting>

			<para>
				The next example declares an expression that accepts one parameter: a MarketData event. The expression computes a new "mid" price based on the buy and sell price:
			</para>
			<programlisting><![CDATA[expression midPrice { x => (x.buy + x.sell) / 2 } 
select midPrice(md) from MarketDataEvent as md]]></programlisting>

			<para>
				The variable name can be left off if event property names resolve without ambiguity.
			</para>
			
			<para>
				This example EPL removes the variable name <literal>x</literal>:
			</para>
			<programlisting><![CDATA[expression midPrice { x => (buy + sell) / 2 } 
select midPrice(md) from MarketDataEvent as md]]></programlisting>

			<para>
				The next example EPL specifies wildcard instead:
			</para>
			<programlisting><![CDATA[expression midPrice { x => (buy + sell) / 2 } 
select midPrice(*) from MarketDataEvent]]></programlisting>

			<para>
				A further example that demonstrates two parameters is listed next. The example joins two streams and uses the price value from MarketDataEvent and the sentiment value of NewsEvent to compute a weighted sentiment:
			</para>
			<programlisting><![CDATA[expression weightedSentiment { (x, y) => x.price * y.sentiment } 
select weightedSentiment(md, news) 
from MarketDataEvent#lastevent as md, NewsEvent#lastevent news]]></programlisting>

			<para>
				Any expression can be used in the expression body including aggregations, variables, subqueries or further declared or alias expressions. Sub-queries, when used without <literal>in</literal> or <literal>exists</literal>, must be placed within parenthesis.
			</para>
			
			<para>
			    An example subquery within an expression declaration is shown next:
			</para>
			<programlisting><![CDATA[expression newsSubq { md -> 
    (select sentiment from NewsEvent#unique(symbol) where symbol = md.symbol) 
} 
select newsSubq(mdstream)
from MarketDataEvent mdstream]]></programlisting>

			<para>
				When using expression declarations please note these limitations:
			</para>
			<orderedlist spacing="compact">
				<listitem>
					<para>
						Parameters to a declared expression can only be a stream name, pattern tag name or wildcard (<literal>*</literal>).
					</para>
				</listitem>
				<listitem>
					<para>
						Expression declarations do not remove implicit limitations: For example, aggregation functions cannot be used in a filter expression even if using an expression declaration.
					</para>
				</listitem>
			</orderedlist>
			
			<para>
				The following scope rules apply for declared expressions:
			</para>			
			<orderedlist spacing="compact">
				<listitem>
					<para>
						The scope of the expression body of a declared expression only includes the parameters explicitly listed. Consider using an expression alias instead.
					</para>
				</listitem>
			</orderedlist>
		</sect2>

		<sect2 xml:id="epl-syntax-script-decl">
			<title>Script Declaration</title>
			<indexterm><primary>script declaration</primary></indexterm>
			
			<para>
				Esper allows the use of scripting languages within EPL. Any scripting language that supports JSR 223 and also the MVEL scripting language can be specified in EPL.
			</para>
						
			<para>
				Please see <xref linkend="script"/> for more information.
			</para>
		</sect2>

		<sect2 xml:id="epl-syntax-context-decl">
			<title>Referring to a Context</title>
			
			<para>
			   You may refer to a context in the EPL text by specifying the <literal>context</literal> keyword followed by a context name. Context are described in more detail at <xref linkend="context"/>
			</para>
			
			<para>
				The effect of referring to a context is that your statement operates according to the context dimensional information as declared for the context.
			</para>

			<para>
			  The synopsis is:
			</para>
			<synopsis>... context <emphasis>context_name</emphasis> ...</synopsis>

			<para>
			  You may refer to a context in all statements except for the following types of statements:
			</para>
			
			<orderedlist>
				<listitem>
					<para>
						<literal>create schema</literal> for declaring event types.
					</para>
				</listitem>
				<listitem>
					<para>
						<literal>create variable</literal> for declaring a variable.
					</para>
				</listitem>
				<listitem>
					<para>
						<literal>create index</literal> for creating an index on a named window or table.
					</para>
				</listitem>
				<listitem>
					<para>
						<literal>update istream</literal> for updating insert stream events.
					</para>
				</listitem>
			</orderedlist>
		</sect2>
    </sect1>

    <sect1 xml:id="epl-select-list">
        <title>Choosing Event Properties and Events: The <emphasis>Select</emphasis> Clause</title>
        
		<indexterm><primary>select clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>select clause</secondary></indexterm>
		<para>
			The <literal>select</literal> clause is required in all EPL statements. The <literal>select</literal> clause can be used to select all properties via the wildcard <literal>*</literal>, or to specify a list of event properties and expressions. The <literal>select</literal> clause defines the event type (event property names and types) of the resulting events published by the statement, or pulled from the statement via the iterator methods.
		</para>

		<para>
			The <literal>select</literal> clause also offers optional <literal>istream</literal>, <literal>irstream</literal> and <literal>rstream</literal> keywords to control whether input stream, remove stream or input and remove stream events are posted to <literal>UpdateListener</literal> instances and observers to a statement. By default, the engine provides only the insert stream to listener and observers. See <xref linkend="config-engine-stream-selection"/> on how to change the default.
		</para>

		<para>
			The syntax for the <literal>select</literal> clause is summarized below.
		</para>

		<synopsis>select [istream | irstream | rstream] [distinct] * | <emphasis>expression_list</emphasis> ... </synopsis>

		<para>
			The <literal>istream</literal> keyword is the default, and indicates that the engine only delivers insert stream events to listeners and observers. The <literal>irstream</literal> keyword indicates that the engine delivers both insert and remove stream. Finally, the <literal>rstream</literal> keyword tells the engine to deliver only the remove stream.
		</para>

		<para>
			The <literal>distinct</literal> keyword outputs only unique rows depending on the column list you have specified after it. It must occur after the <literal>select</literal> and after the optional stream keywords, as described in more detail below.
		</para>

        <sect2 xml:id="epl-select-all-props" >
           <title>Choosing the Event Itself: Select *</title>

			<para>
				The syntax for selecting the event itself is:
			</para>
			<synopsis>select * from <emphasis>stream_def</emphasis></synopsis>

			<para>
				The following statement selects StockTick events for the last 30 seconds of IBM stock ticks.
			</para>
			<programlisting><![CDATA[select * from StockTick(symbol='IBM')#time(30 sec)]]></programlisting>

			<para>
			  You may well be asking: Why does the statement specify a time window here? First, the statement is meant to demonstrate the use of <literal>*</literal> wildcard. 
			  When the engine pushes statement results to your listener and as the statement does not select remove stream events via <literal>rstream</literal> keyword, the listener receives only new events and the time window could be left off.
			  By adding the time window the pull API (iterator API or JDBC driver) returns the last 30 seconds of events.
			</para>

			<para>
				The <literal>*</literal> wildcard and expressions can also be combined in a <literal>select</literal> clause. The combination selects all event properties and in addition the computed values
				as specified by any additional expressions that are part of the <literal>select</literal> clause. Here is an example that selects all properties of stock tick events plus a computed product of price and volume that the 
				statement names 'pricevolume':
			</para>
			<programlisting><![CDATA[select *, price * volume as pricevolume from StockTick]]></programlisting>

			<para>
				When using wildcard (*), Esper does not actually read or copy your event properties out of your event or events, neither does it copy the event object. 
				It simply wraps your native type in an <literal>EventBean</literal> interface. Your application has access to
				the underlying event object through the <literal>getUnderlying</literal> method and has access to the property values through the <literal>get</literal> method.
			</para>

			<para>
				In a join statement, using the <literal>select *</literal> syntax selects one event property per stream to hold the event for that stream. The property name is the stream name in the <literal>from</literal> clause. 
			</para>
		</sect2>

        <sect2 xml:id="epl-select-event-props" >
           <title>Choosing Specific Event Properties</title>
			<para>
				To choose the particular event properties to return:
			</para>
			<synopsis>select <emphasis>event_property</emphasis> [, <emphasis>event_property</emphasis>] [, ...] from <emphasis>stream_def</emphasis></synopsis>

			<para>
				The following statement simply selects the symbol and price properties of stock ticks, and the total volume for stock tick events in a 60-second time window. 
			</para>
			<programlisting><![CDATA[select symbol, price, sum(volume) from StockTick(symbol='IBM')#time(60 sec)
]]></programlisting>
		</sect2>

        <sect2 xml:id="epl-select-expressions" >
           <title>Expressions</title>
			<para>
				The <literal>select</literal> clause can contain one or more expressions.
			</para>
			<synopsis>select <emphasis>expression</emphasis> [, <emphasis>expression</emphasis>] [, ...] from <emphasis>stream_def</emphasis></synopsis>

			<para>
				The following statement selects the volume multiplied by price for a time batch of the last 30 seconds of stock tick events. 
			</para>
			<programlisting><![CDATA[select volume * price from StockTick#time_batch(30 sec)]]></programlisting>
		</sect2>

        <sect2 xml:id="epl-select-renaming" >
            <title>Renaming Event Properties</title>
			<para>
				Event properties and expressions can be renamed using below syntax.
			</para>
			<synopsis>select [<emphasis>event_property</emphasis> | <emphasis>expression</emphasis>] [as] <emphasis>identifier</emphasis> [, ...]</synopsis>

			<para>
				The following statement selects volume multiplied by price and specifies the name <emphasis>volPrice</emphasis> for the resulting column.
			</para>
			<programlisting><![CDATA[select volume * price as volPrice from StockTick]]></programlisting>

			<para>
				Identifiers cannot contain the "." (dot) character, i.e. "vol.price" is not a valid identifier for the rename syntax.
			</para>
			
			<para>
				The <literal>as</literal> keyword is optional. The following EPL is therefore equivalent to above:
			</para>
			<programlisting><![CDATA[select volume * price volPrice from StockTick]]></programlisting>
		</sect2>
		
        <sect2 xml:id="epl-select-all-join">
           <title>Choosing Event Properties and Events in a Join</title>
           
			<para>
				If your statement is joining multiple streams, your may specify property names that are unique among the joined streams, or use wildcard (*) as explained earlier.
			</para>

			<para>
				In case the property name in your <literal>select</literal> or other clauses is not unique considering all joined streams, you will need to use the name of the stream as a prefix to the property.
			</para>

			<para>
				This example is a join between the two streams StockTick and News, respectively named as 'tick' and 'news'. The example selects from the StockTick event the symbol value using the 'tick' stream name as a prefix:
			</para>
			<programlisting><![CDATA[select tick.symbol from StockTick#time(10) as tick, News#time(10) as news
where news.symbol = tick.symbol]]></programlisting>

			<para>
				Use the wildcard (*) selector in a join to generate a property for each stream, with the property value being the event itself. The output events of the statement below have two properties: the 'tick' property holds the StockTick event and the 'news' property holds the News event:
			</para>
			<programlisting><![CDATA[select * from StockTick#time(10) as tick, News#time(10) as news]]></programlisting>

			<para>
				The following syntax can also be used to specify what stream's properties to select:
			</para>
			<synopsis>select <emphasis>stream_name</emphasis>.* [as <emphasis>name</emphasis>] from ...</synopsis>

			<para>
				The selection of <literal>tick.*</literal> selects the StockTick stream events only:
			</para>
			<programlisting><![CDATA[select tick.* from StockTick#time(10) as tick, News#time(10) as news
where tick.symbol = news.symbol]]></programlisting>

			<para>
				The next example uses the <literal>as</literal> keyword to name each stream's joined events. This instructs the engine to create a property for each named event:
			</para>
			<programlisting><![CDATA[select tick.* as stocktick, news.* as news 
from StockTick#time(10) as tick, News#time(10) as news
where stock.symbol = news.symbol]]></programlisting>

			<para>
				The output events of the above example have two properties 'stocktick' and 'news' that are the StockTick and News events.
			</para>

			<para>
				The stream name itself, as further described in <xref linkend="epl-from-clause-stream-name"/>, may be used within expressions or alone. 
			</para>
			
			<para>
				This example passes events to a user-defined function named <literal>compute</literal> and also shows <literal>insert-into</literal> to populate an event stream of combined events:
			</para>
			<programlisting><![CDATA[insert into TickNewStream select tick, news, MyLib.compute(news, tick) as result
from StockTick#time(10) as tick, News#time(10) as news
where tick.symbol = news.symbol]]></programlisting>
			<programlisting><![CDATA[// second statement that uses the TickNewStream stream
select tick.price, news.text, result from TickNewStream]]></programlisting>

			<para>
				In summary, the <emphasis>stream_name.*</emphasis> streamname wildcard syntax can be used to select a stream as the underlying event or as a property, but cannot appear within an expression. While the <emphasis>stream_name</emphasis> syntax (without wildcard) always selects a property (and not as an underlying event), and can occur anywhere within an expression.
			</para>
		</sect2>

        <sect2 xml:id="epl-select-pattern" >
           <title>Choosing Event Properties and Events From a Pattern</title>
           
			<para>
				If your statement employs pattern expressions, then your pattern expression tags events with a tag name. Each tag name becomes available for use as a property in the <literal>select</literal> clause and all other clauses.
			</para>
			
			<para>
				For example, here is a very simple pattern that matches on every StockTick event received within 30 seconds after start of the statement. The sample selects the symbol and price properties of the matching events:
			</para>
			<programlisting><![CDATA[select tick.symbol as symbol, tick.price as price
from pattern[every tick=StockTick where timer:within(10 sec)]]]></programlisting>

			<para>
				The use of the wildcard selector, as shown in the next statement, creates a property for each tagged event in the output. The next statement outputs events that hold a single 'tick' property whose value is the event itself:
			</para>
			<programlisting><![CDATA[select * from pattern[every tick=StockTick where timer:within(10 sec)]]]></programlisting>

			<para>
				You may also select the matching event itself using the <literal>tick.*</literal> syntax. The engine outputs the StockTick event itself to listeners:
			</para>
			<programlisting><![CDATA[select tick.* from pattern[every tick=StockTick where timer:within(10 sec)]]]></programlisting>

			<para>
				A tag name as specified in a pattern is a valid expression itself. This example uses the <literal>insert into</literal> clause to make available the events matched by a pattern to further statements:
			</para>
			<programlisting><![CDATA[// make a new stream of ticks and news available
insert into StockTickAndNews 
select tick, news from pattern [every tick=StockTick -> news=News(symbol=tick.symbol)]]]></programlisting>
			<programlisting><![CDATA[// second statement to select from the stream of ticks and news
select tick.symbol, tick.price, news.text from StockTickAndNews]]></programlisting>
		</sect2>

        <sect2 xml:id="epl-select-using-istream-rstream" >
            <title>Selecting <literal>Insert</literal> and <literal>Remove</literal> Stream Events</title>

			<para>
				The optional <literal>istream</literal>, <literal>irstream</literal> and <literal>rstream</literal> keywords in the <literal>select</literal> clause control the event streams posted to listeners and observers to a statement. 
			</para>

			<para>
				If neither keyword is specified, and in the default engine configuration, the engine posts only 
				insert stream events via the <literal>newEvents</literal> parameter to the <literal>update</literal> method of <literal>UpdateListener</literal> instances listening to 
				the statement. The engine does not post remove stream events, by default.
				</para>
			
			<para>
				The insert stream consists of the events entering the respective window(s) or stream(s) or aggregations, while the remove stream consists of the events leaving the respective window(s) or the changed aggregation result.
				See <xref linkend="processingmodel"/> for more information on insert and remove streams.
				</para>

			<para>
				The engine posts remove stream events to the <literal>oldEvents</literal> parameter of the <literal>update</literal> method only if the <literal>irstream</literal> keyword
				occurs in the <literal>select</literal> clause. This behavior can be changed via engine-wide configuration as described in <xref linkend="config-engine-stream-selection"/>.
			</para>

			<para>
				By specifying the <literal>istream</literal> keyword you can instruct the engine to only post insert stream events via the <literal>newEvents</literal> parameter to the <literal>update</literal> method on listeners. The engine will then not post any remove stream events, and the <literal>oldEvents</literal> parameter is always a null value.
			</para>
				
			<para>
				By specifying the <literal>irstream</literal> keyword you can instruct the engine to post both insert stream and remove stream events.
			</para>

			<para>
				By specifying the <literal>rstream</literal> keyword you can instruct the engine to only post remove stream events via the <literal>newEvents</literal> parameter to the <literal>update</literal> method on listeners. The engine will then not post any insert stream events, and the <literal>oldEvents</literal> parameter is also always a null value.
			</para>

			<para>
				The following statement selects only the events that are leaving the 30 second time window.
			</para>
			<programlisting><![CDATA[select rstream * from StockTick#time(30 sec)]]></programlisting>

			<para>
				The <literal>istream</literal> and <literal>rstream</literal> keywords in the <literal>select</literal> clause are matched by same-name keywords available in the <literal>insert into</literal> clause. While the keywords in the <literal>select</literal> clause control the event stream posted to listeners to the statement, the same keywords in the <literal>insert into</literal> clause specify the event stream that the engine makes available to other statements.
			</para>
		</sect2>

		<sect2 xml:id="epl-syntax-prefix-propsandstream">
			<title>Qualifying Property Names and Stream Names</title>
			
			<para>
				Property or column names can optionally be qualified by a stream name and the provider URI. The syntax is:
			</para>
			<synopsis>[[<emphasis>provider_URI</emphasis>.]<emphasis>stream_name</emphasis>.]<emphasis>property_name</emphasis></synopsis>

			<para>
				The <emphasis>provider_URI</emphasis> is the URI supplied to the <literal>EPServiceProviderManager</literal> class, or the string <literal>default</literal> for the default provider.
			</para>
		
			<para>
				This example assumes the provider is the default provider:
			</para>
			<programlisting>select MyEvent.myProperty from MyEvent
// ... equivalent to ...
select default.MyEvent.myProperty from MyEvent</programlisting>

			<para>
				Stream names can also be qualified by the provider URI. The syntax is:
			</para>
			<synopsis>[<emphasis>provider_URI</emphasis>.]<emphasis>stream_name</emphasis></synopsis>

			<para>
				The next example assumes a provider URI by name of <literal>Processor</literal>:
			</para>
			<programlisting>select Processor.MyEvent.myProperty from Processor.MyEvent</programlisting>

		</sect2>

		<sect2 xml:id="epl-syntax-distinct">
			<title>Select <literal>Distinct</literal></title>
			
			<para>
				The optional <literal>distinct</literal> keyword removes duplicate output events from output. The keyword must occur after the <literal>select</literal> keyword and after the optional <literal>irstream</literal> keyword.
			</para>

			<para>
				The <literal>distinct</literal> keyword in your <literal>select</literal> instructs the engine to consolidate, at time of output, the output event(s) and remove output events with identical property values.
				Duplicate removal only takes place when two or more events are output together at any one time, therefore <literal>distinct</literal> is typically used with a batch data window, output rate limiting, on-demand queries, on-select or iterator pull API.
			</para>

			<para>
				If two or more output event objects have same property values for all properties of the event, the <literal>distinct</literal> removes all but one duplicated event before outputting events to listeners. Indexed, nested and mapped properties
				are considered in the comparison, if present in the output event.				
			</para>

			<para>
				The next example outputs sensor ids of temperature sensor events, but only every 10 seconds and only unique sensor id values during the 10 seconds:
			</para>
			<programlisting>select distinct sensorId from TemperatureSensorEvent output every 10 seconds</programlisting>

			<para>
				Use <literal>distinct</literal> with wildcard (<literal>*</literal>) to remove duplicate output events considering all properties of an event.
			</para>

			<para>
				This example statement outputs all distinct events either when 100 events arrive or when 10 seconds passed, whichever occurs first:
			</para>
			<programlisting>select distinct * from TemperatureSensorEvent#time_length_batch(10, 100)</programlisting>
			
			<para>
				When selecting nested, indexed, mapped or dynamic properties in a <literal>select</literal> clause with <literal>distinct</literal>, it is relevant to know that the comparison uses hash code and the Java <literal>equals</literal> semantics.
			</para>
		</sect2>

		<sect2 xml:id="epl-transposefunc">
			<title>Transposing an Expression Result to a Stream</title>
			
			<para>
			   For transposing an instance of a Java object returned by an expression to a stream use the transpose function as described in <xref linkend="functionreference-transpose"/>.
			</para>
		</sect2>

		<sect2 xml:id="epl-selecteventbean">
			<title>Selecting EventBean Instead of Underlying Event</title>
			
			<para>
				By default, for certain select-clause expressions that output events or a collection of events,
				the engine outputs the underlying event objects. The term <emphasis>outputs</emphasis> means the data passed to listeners, 
				subscribers and inserted-into into another stream via insert-into.			   
			</para>

			<para>
				The select-clause expressions for which underlying event objects are output by default are:
			</para>
			
			<itemizedlist>
				<listitem>
					Event Aggregation Functions (including extension API)
				</listitem>
				<listitem>
					The <literal>previous</literal> family of single-row functions
				</listitem>
				<listitem>
					Subselects that select events
				</listitem>
				<listitem>
					Declared expressions and enumeration methods that operate on any of the above
				</listitem>
			</itemizedlist>

			<para>
				To have the engine output <literal>EventBean</literal> instance(s) instead, add <literal>@eventbean</literal>
				to the relevant expressions of the <literal>select</literal>-clause.
			</para>

			<para>
				The sample EPL shown below outputs current data window contents as <literal>EventBean</literal> instances
				into the stream <literal>OutStream</literal>, thereby statements consuming the stream may operate on such instances:
			</para>
			<programlisting>insert into OutStream 
select prevwindow(s0) @eventbean as win 
from MyEvent#length(2) as s0</programlisting>
			
			<para>
				The next EPL consumes the stream and selects the last event:
			</para>
			<programlisting>select win.lastOf() from OutStream</programlisting>
			
			<para>
				It is not necessary to use <literal>@eventbean</literal> if an event type by the same name (<literal>OutStream</literal> in the example) is already declared 
				and a property exist on the type by the same name (<literal>win</literal> in this example) and the type of the property is the event type (<literal>MyEvent</literal> in the example) 
				returned by the expression. This is further described in <xref linkend="insert-into-reconcile"/>.
			</para>
		</sect2>

    </sect1>

    <sect1 xml:id="epl-from-clause">
        <title>Specifying Event Streams: The <emphasis>From</emphasis> Clause</title>

		<indexterm><primary>join</primary><secondary>from clause</secondary></indexterm>
		<indexterm><primary>from clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>from clause</secondary></indexterm>
		<para>
			The <literal>from</literal> clause is required in all EPL statements. It specifies one or more event streams, named windows or tables. Each event stream, named window or table can optionally be given a name by means of the <literal>as</literal> keyword.
		</para>
		<synopsis>from <emphasis>stream_def</emphasis> [as <emphasis>name</emphasis>] [unidirectional] [retain-union | retain-intersection] 
    [, <emphasis>stream_def</emphasis> [as <emphasis>stream_name</emphasis>]] [, ...]</synopsis>

		<para>
			The event stream definition <emphasis>stream_def</emphasis> as shown in the syntax above can consists of either a filter-based event stream definition or a pattern-based event stream definition. 
		</para>

		<para>
			For joins and outer joins, specify two or more event streams. Joins and the <literal>unidirectional</literal> keyword are described in more detail in <xref linkend="epl-join"/>.
			Joins are handy when multiple streams or patterns can trigger output and outer joins can be used to union and connect streams via <literal>or</literal>.
		</para>

		<para>
			Esper supports joins against relational databases for access to historical or reference data as explained in <xref linkend="histdata_overview"/>. Esper can also join results returned by an arbitrary invocation, as discussed in <xref linkend="joining_method"/>.
		</para>

		<para>
			The <emphasis>stream_name</emphasis> is an optional identifier assigned to the stream. The stream name can itself occur in any expression and provides access to the event itself from the named stream. Also, a stream name may be combined with a method name to invoke instance methods on events of that stream.
		</para>

		<para>
			For all streams with the exception of historical sources your query may employ data windows as outlined below. The <literal>retain-intersection</literal> (the default) and <literal>retain-union</literal> keywords build a union or intersection of two or more data windows as described in <xref linkend="epl-from-clause-view-multidatawindow"/>.
		</para>

        <sect2 xml:id="epl-from-clause-filter" >
           <title>Filter-Based Event Streams</title>

			<para>
				The <emphasis>stream_def</emphasis> syntax for a filter-based event stream is as below:
			</para>
	
			<synopsis><emphasis>event_stream_name</emphasis> [(<emphasis>filter_criteria</emphasis>)] [<emphasis>contained_selection</emphasis>] [#<emphasis>window_spec</emphasis>] [#<emphasis>window_spec</emphasis>] [...]</synopsis>
	
			<para>
			  The <emphasis>event_stream_name</emphasis> is either the name of an event type or name of an event stream populated by an <literal>insert into</literal> statement 
			  or the name of a named window or table.
			</para>
			
			<para>
			  The <emphasis>filter_criteria</emphasis> is optional and consists of a list of expressions filtering the events of the event stream, within parenthesis after the event stream name.
			  Filter criteria cannot be specified for tables.
			</para>

			<para>
			  The <emphasis>contained_selection</emphasis> is optional and is for use with coarse-grained events that have properties that are themselves one or more events, see <xref linkend="epl-containedeventselect"/> for the synopsis and examples.
			  Contained-event cannot be specified for tables.
			</para>
			
			<para>
			  The <emphasis>window_spec</emphasis> specify one or more data windows.
			  Data windows cannot be specified for named windows and tables. Instead of the <literal>#</literal> hash character the <literal>.</literal> dot character can also be used, however the dot character requires the data window namespace.
			</para>

			<para>
				The following EPL statement shows event type, filter criteria and data windows combined in one statement. It selects all event properties for the last 100 events of IBM stock ticks for volume. In the example, the event type is the fully qualified Java class name <literal>org.esper.example.StockTick</literal>. The expression filters for events where the property <literal>symbol</literal> has a value of "IBM". This query specifies a length window and thus computes the total volume of the last 100 events.
			</para>
			<programlisting><![CDATA[select sum(volume) from org.esper.example.StockTick(symbol='IBM')#length(100)]]></programlisting>
	
			<para>
				Esper filters out events in an event stream as defined by filter criteria that are placed in parenthesis, before it sends events to the data window(s) (if any). Thus, compared to search conditions in a <literal>where</literal> clause, filter criteria remove unneeded events early. In the above example, events with a symbol other than IBM do not enter the length window.
			</para>

			<sect3 xml:id="epl-filter-event-type" >
			   <title>Specifying an Event Type</title>

				<para>
					The simplest form of filter is a filter for events of a given type without any conditions on the event property values. This filter matches any event of that type regardless of the event's properties. The example below is such a filter. 
				 </para>
				 <programlisting><![CDATA[select * from com.mypackage.myevents.RfidEvent]]></programlisting>

				<para>
					Instead of the fully-qualified Java class name any other event name can be mapped via Configuration to a Java class, making the resulting statement more readable:
				</para>
				<programlisting><![CDATA[select * from RfidEvent]]></programlisting>

				 <para>
					 Interfaces and superclasses are also supported as event types. In the below example <literal>IRfidReadable</literal> is an interface class.
				 </para>
				 <programlisting><![CDATA[select * from org.myorg.rfid.IRfidReadable]]></programlisting>
			</sect3>
		
			<sect3 xml:id="epl-filter-criteria" >
			   <title>Specifying Filter Criteria</title>
				<para>
					The filtering criteria to filter for events with certain event property values are placed within parenthesis after the event type name:
				 </para>
				 <programlisting><![CDATA[select * from RfidEvent(category="Perishable")]]></programlisting>
				 
				<para>
					All expressions can be used in filters, including static methods that return a boolean value:
				 </para>
				 <programlisting><![CDATA[select * from com.mycompany.RfidEvent(MyRFIDLib.isInRange(x, y) or (x < 0 and y < 0))]]></programlisting>
					
				<para>
					Filter expressions can be separated via a single comma '<literal>,</literal>'. The comma represents a logical AND between filter expressions:
				 </para>
				 <programlisting><![CDATA[select * from RfidEvent(zone=1, category=10)
...is equivalent to...
select * from RfidEvent(zone=1 and category=10)]]></programlisting>
	
				<para>
					The engine analyzes the filter expressions and determines the filter indexes to use or to create. The following operators are the preferred means of filtering event streams, especially in the presence of a larger number of filters or statements. Please read <xref linkend="processingmodel_indexes_filterindexes"/> for more information. The engine can translate the following operators, including combinations of these operators connected via <literal>and</literal> and <literal>or</literal>, into filter indexes:
				</para>
		
				<itemizedlist spacing="compact">
					<listitem>
						<para>
							equals <literal>=</literal>
						</para>
					</listitem>
					<listitem>
						<para>
							not equals <literal>!=</literal>
						</para>
					</listitem>
					<listitem>
						<para>
							comparison operators <literal>&lt; , &gt; , &gt;=, &lt;=</literal>
						</para>
					</listitem>
					<listitem>
						<para>
							ranges 
						</para>
						<itemizedlist spacing="compact">
							<listitem>
								<para>
									use the <literal>between</literal> keyword for a closed range where both endpoints are included
								</para>
							</listitem>
							<listitem>
								<para>
									use the <literal>in </literal> keyword and round <literal>()</literal> or square brackets <literal>[]</literal> to control how endpoints are included
								</para>
							</listitem>
							<listitem>
								<para>
									for inverted ranges use the <literal>not</literal> keyword and the <literal>between</literal> or <literal>in</literal> keywords
								</para>
							</listitem>
						</itemizedlist>
					</listitem>
					<listitem>
						<para>
							list-of-values checks using the <literal>in</literal> keyword or the <literal>not in </literal> keywords followed by a comma-separated list of values
						</para>
					</listitem>
					<listitem>
						<para>
							single-row functions that have been registered and are invoked via function name (see user-defined functions) and that either return a boolean value or that have their return value compared to a constant
						</para>
					</listitem>
					<listitem>
						<para>
							the <literal>and</literal> and <literal>or</literal> logical operators
						</para>
					</listitem>
				</itemizedlist>
	
				<para>
					At compile time the engine scans new filter expressions for sub-expressions that can be placed into filter indexes. Indexing filter values to match event properties of incoming events enables the engine to match incoming events faster, especially if your application creates a large number of statements or context partitions or requires many similar filters. The above list of operators represents the set of operators that the engine can best convert into filter index entries. The use of comma or logical <literal>and</literal> in filter expressions is fully equivalent. 
				</para>
			</sect3>
			
			<sect3 xml:id="epl-filter-ranges" >
				<title>Filtering Ranges</title>
				<para>
					Ranges come in the following 4 varieties. The use of round <literal>()</literal> 
					or square <literal>[]</literal> bracket dictates whether an endpoint is included or excluded. The low point and the high-point of the range are separated by the colon <literal>:</literal> character.
				 </para>
	
				<itemizedlist spacing="compact">
					<listitem>
						<para>
							Open ranges that contain neither endpoint <literal>(low:high)</literal>
						</para>
					</listitem>
					<listitem>
						<para>
							Closed ranges that contain both endpoints <literal>[low:high]</literal>. The equivalent 'between' keyword also defines a closed range.
						</para>
					</listitem>
					<listitem>
						<para>
							Half-open ranges that contain the low endpoint but not the high endpoint <literal>[low:high)</literal>
						</para>
					</listitem>
					<listitem>
						<para>
							Half-closed ranges that contain the high endpoint but not the low endpoint <literal>(low:high]</literal>
						</para>
					</listitem>
				</itemizedlist>
				
				<para>
					The next statement shows a filter specifying a range for <literal>x</literal> and <literal>y</literal> values of RFID events. The range includes both endpoints therefore uses <literal>[]</literal> hard brackets.
				</para>
				<programlisting><![CDATA[mypackage.RfidEvent(x in [100:200], y in [0:100])]]></programlisting>
	
				<para>
					The <literal>between</literal> keyword is equivalent for closed ranges. The same filter using the <literal>between</literal> keyword is:
				</para>
				<programlisting><![CDATA[mypackage.RfidEvent(x between 100 and 200, y between 0 and 50)]]></programlisting>
	
				<para>
					The <literal>not</literal> keyword can be used to determine if a value falls outside a given range:
				</para>
				<programlisting><![CDATA[mypackage.RfidEvent(x not in [0:100])]]></programlisting>
	
				<para>
					The equivalent statement using the <literal>between</literal> keyword is:
				</para>
				<programlisting><![CDATA[mypackage.RfidEvent(x not between 0 and 100)]]></programlisting>
			</sect3>
			
			<sect3 xml:id="epl-filter-in" >
				<title>Filtering Sets of Values</title>
				<para>
					The <literal>in</literal> keyword for filter criteria determines if a given value matches any value in a list of values.
				 </para>
	
				<para>
					In this example you are interested in RFID events where the category matches any of the given values:
				</para>
				<programlisting><![CDATA[mypackage.RfidEvent(category in ('Perishable', 'Container'))]]></programlisting>
	
				<para>
					By using the <literal>not in </literal>keywords you can filter events with a property value that does not match any of the values in a list of values:
				 </para>
				<programlisting><![CDATA[mypackage.RfidEvent(category not in ('Household', 'Electrical'))]]></programlisting>
			</sect3>
			
			<sect3 xml:id="epl-filter-limitations" >
				<title>Filter Limitations</title>
				<para>
					The following restrictions apply to filter criteria:
				</para>        
				<itemizedlist spacing="compact">
					<listitem>
						<para>
							Range and comparison operators require the event property to be of a numeric or string type. 
						</para>
					</listitem>
					<listitem>
						<para>
							Aggregation functions are not allowed within filter expressions.
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>prev</literal> previous event function and the <literal>prior</literal> prior event function cannot be used in filter expressions.
						</para>
					</listitem>
				</itemizedlist>
			</sect3>
		</sect2>
		
        <sect2 xml:id="epl-from-clause-patterns" >
           <title>Pattern-Based Event Streams</title>
			<para>
				Event pattern expressions can also be used to specify one or more event streams in an EPL statement.
				For pattern-based event streams, the event stream definition <emphasis>stream_def</emphasis> consists of the keyword <literal>pattern</literal> and a pattern expression in brackets <literal>[]</literal>. The syntax for an event stream definition using a pattern expression is below. As in filter-based event streams you can specify data windows.
			</para>
			
			<synopsis>pattern [<emphasis>pattern_expression</emphasis>] [#<emphasis>window_spec</emphasis>] [#<emphasis>window_spec</emphasis>] [...]</synopsis>

			<para>
				The next statement specifies an event stream that consists of both stock tick events and trade events. The example tags stock tick events with the name "tick" and trade events with the name "trade".
			</para>			
            <programlisting><![CDATA[select * from pattern [every tick=StockTickEvent or every trade=TradeEvent]]]></programlisting>
			
			<para>
				This statement generates an event every time the engine receives either one of the event types. The generated events resemble a map with "tick" and "trade" keys. For stock tick events, the "tick" key value is the underlying stock tick event, and the "trade" key value is a null value. For trade events, the "trade" key value is the underlying trade event, and the "tick" key value is a null value.
			</para>			

			<para>
				Lets further refine this statement adding a data window the gives us the last 30 seconds of either stock tick or trade events. Lets also select prices and a price total.
			</para>			
            <programlisting><![CDATA[select tick.price as tickPrice, trade.price as tradePrice, 
       sum(tick.price) + sum(trade.price) as total
  from pattern [every tick=StockTickEvent or every trade=TradeEvent]#time(30 sec)]]></programlisting>

			<para>
				Note that in the statement above <literal>tickPrice</literal> and <literal>tradePrice</literal> can each be null values depending on the event processed. Therefore, an aggregation function such as <literal>sum(tick.price + trade.price))</literal> would always return null values as either of the two price properties are always a null value for any event matching the pattern. Use the <literal>coalesce</literal> function to handle null values, for example: <literal>sum(coalesce(tick.price, 0) + coalesce(trade.price, 0))</literal>.
			</para>			
			
			<note>
				<para>
					When used with patterns, specifying a data window defines what pattern matches to retain in memory for the purpose of joins or for using the iterator API.
				</para>
				<para>
					When used with patterns, specifying a data window does not limit pattern matches to events in the window. The <literal>match_recognize</literal> pattern matching however does limit matches to events retained by a data window.
				</para>
			</note>
		</sect2>

        <sect2 xml:id="epl-from-clause-window-spec" >
           <title>Specifying Data Windows</title>
			<para>
				Data windows retain a subset of events. They provide an retain/expiry policy for events and the engine automatically removes events according to the retain/expiry policy. Data windows can be grouped and data windows can be intersected or unioned. See the section <xref linkend="epl-views"/> on the data windows available. Data windows can take parameters. Any expressions can be a parameter, with limitations.
			</para>

			<para>
				The example statement below outputs a count per expressway for car location events (contains information about the location of a car on a highway) of the last 60 seconds:
			</para>
				
         <programlisting><![CDATA[select expressway, count(*) from CarLocEvent#time(60) 
group by expressway]]></programlisting>

			<para>
				The next example declares <literal>#groupwin</literal> and a <literal>#length</literal> window to indicate that there is a separate length window per car id:
			</para>
				
         <programlisting><![CDATA[select cardId, expressway, direction, segment, count(*) 
from CarLocEvent#groupwin(carId)#length(4) 
group by carId, expressway, direction, segment]]></programlisting>

			<para>
				The <literal>#groupwin(carId)</literal> groups car location events by car id. The <literal>#length(4)</literal> keeps a length window of the 4 last events, with one separate length window for each car id. 
				The example reports the number of events per car id and per expressway, direction and segment considering the last 4 events for each car id only. 
			</para>
			
			<para>
				The special keep-all window keeps all events: It does not expire events and does not provide a remove stream, i.e. events are not removed from the keep-all window unless by means of <literal>on-delete</literal> or <literal>on-merge</literal> or fire-and-forget <literal>delete</literal>.
			</para>
		</sect2>

		<sect2 xml:id="epl-from-clause-view-multidatawindow" >
		   <title>Multiple Data Windows</title>
		   <para>
			   Data windows provide an expiry policy that indicates when to remove events from the data window, with the exception of the keep-all data window which has no expiry policy and the <literal>#groupwin</literal> grouped-window for allocating a new data window per group.
		   </para>
		   
		   <para>
			   EPL allows the freedom to use multiple data windows onto a stream and thus combine expiry policies. Combining data windows into an intersection (the default) or a union can achieve a useful strategy for retaining events and expiring events that are no longer of interest. Named windows, tables and <literal>on-merge</literal> and <literal>on-delete</literal> provide an additional degree of freedom.
		   </para>

		   <para>
			   In order to combine two or more data windows there is no keyword required. The <emphasis>retain-intersection</emphasis> keyword is the default and the <emphasis>retain-union</emphasis> keyword may instead be provided for a stream.
		   </para>

			<para>
				The concept of union and intersection come from Set mathematics. In the language of Set mathematics, two sets A and B can be "added" together: The intersection of A and B is the set of all things which are members of both A and B, i.e. the members two sets have "in common". The union of A and B is the set of all things which are members of either A or B. 
			</para>

		   <para>
			   Use the <emphasis>retain-intersection</emphasis> (the default) keyword to retain an intersection of all events as defined by two or more data windows. All events removed from any of the intersected data windows are entered into the remove stream. This is the default behavior if neither retain keyword is specified.
		   </para>

		   <para>
			   Use the <emphasis>retain-union</emphasis> keyword to retain a union of all events as defined by two or more data windows. Only events removed from all data windows are entered into the remove stream.
		   </para>

		   <para>
			   The next example statement totals the price of OrderEvent events in a union of the last 30 seconds and unique by product name:
		   </para>
            <programlisting><![CDATA[select sum(price) from OrderEvent#time(30 sec)#unique(productName) retain-union]]></programlisting>		   
		   <para>
			   In the above statement, all OrderEvent events that are either less then 30 seconds old or that are the last event for the product name are considered.
		   </para>		   

		   <para>
			   Here is an example statement totals the price of OrderEvent events in an intersection of the last 30 seconds and unique by product name:
		   </para>
            <programlisting><![CDATA[select sum(price) from OrderEvent#time(30 sec)#unique(productName) retain-intersection]]></programlisting>		   
		   <para>
			   In the above statement, only those OrderEvent events that are both less then 30 seconds old and are the last event for the product name are considered. The number of events that the engine retains is the number of unique events per product name in the last 30 seconds (and not the number of events in the last 30 seconds).
		   </para>		   
		   
		   <para>
		     For an intersection the engine retains the minimal number of events representing that intersection. Thus when combining a time window of 30 seconds and a last-event window, for example, the number of events retained at any time is zero or one event (and not 30 seconds of events).
		   </para>

		   <para>
		     When combining a batch window into an intersection with another data window the combined data window gains batching semantics: Only when the batch criteria is fulfilled does the engine provide the batch of intersecting insert stream events. Multiple batch data windows may not be combined into an intersection.
		   </para>
		   
		   <para>
		     The table below provides additional examples for data window intersections:
		   </para>
		   
			<table frame="topbot">
				<title>Intersection Data Window Examples</title>
				<tgroup cols="2">
					<colspec colwidth="1*"/>
					<colspec colwidth="1*"/>
					<thead>
						<row>
							<entry>Example</entry>
							<entry>Description</entry>
						</row>
					</thead>
					<tbody>
						<row>
							<entry><literal>#time(30)#firstunique(keys)</literal></entry>
							<entry>Retains 30 seconds of events unique per <literal>keys</literal> value (first event per value).</entry>
						</row>
						<row>
							<entry><literal>#firstlength(3)#firstunique(keys)</literal></entry>
							<entry>Retains the first 3 events that are also unique per <literal>keys</literal> value.</entry>
						</row>
						<row>
							<entry><literal>#time_batch(N seconds)#unique(keys)</literal></entry>
							<entry>Posts a batch every N seconds that contains the last of each unique event per <literal>keys</literal> value.</entry>
						</row>
						<row>
							<entry><literal>#time_batch(N seconds)#firstunique(keys)</literal></entry>
							<entry>Posts a batch every N seconds that contains the first of each unique event per <literal>keys</literal> value.</entry>
						</row>
						<row>
							<entry><literal>#length_batch(N)#unique(keys)</literal></entry>
							<entry>Posts a batch of unique events (last event per value) when N unique events per <literal>keys</literal> value are encountered.</entry>
						</row>
						<row>
							<entry><literal>#length_batch(N)#firstunique(keys)</literal></entry>
							<entry>Posts a batch of unique events (first event per value) when N unique events per <literal>keys</literal> value are encountered.</entry>
						</row>
					</tbody>
				</tgroup>				
			</table>

		   <para>
			   For advanced users and for backward compatibility, it is possible to configure Esper to allow multiple data windows without either of the <literal>retain</literal> keywords, as described in <xref linkend="config-engine-viewresources-multipolicy"/>.
		   </para>
		</sect2>
		
        <sect2 xml:id="epl-from-clause-stream-name" >
           <title>Using the Stream Name</title>
			<para>
				Your <literal>from</literal> clause may assign a name to each stream. This assigned stream name can serve any of the following purposes.
			</para>
						
			<para>
				First, the stream name can be used to disambiguate property names. The <literal>stream_name.property_name</literal> syntax uniquely identifies which property to select if property names overlap between streams. Here is an example:
			</para>
            <programlisting><![CDATA[select prod.productId, ord.productId from ProductEvent as prod, OrderEvent as ord]]></programlisting>

			<para>
				Second, the stream name can be used with a wildcard (*) character to select events in a join, or assign new names to the streams in a join:
			</para>
            <programlisting><![CDATA[// Select ProductEvent only
select prod.* from ProductEvent as prod, OrderEvent]]></programlisting>
            <programlisting><![CDATA[// Assign column names 'product' and 'order' to each event
select prod.* as product, ord.* as order from ProductEvent as prod, OrderEvent as ord]]></programlisting>

			<para>
				Further, the stream name by itself can occur in any expression: The engine passes the event itself to that expression. For example, the engine passes the ProductEvent and the OrderEvent to the user-defined function 'checkOrder':
			</para>
            <programlisting><![CDATA[select prod.productId, MyFunc.checkOrder(prod, ord) 
from ProductEvent as prod, OrderEvent as ord]]></programlisting>

			<para>
				Last, you may invoke an instance method on each event of a stream, and pass parameters to the instance method as well. Instance method calls are allowed anywhere in an expression.
			</para>
				
			<para>
				The next statement demonstrates this capability by invoking a method 'computeTotal' on OrderEvent events and a method 'getMultiplier' on ProductEvent events:
			</para>
            <programlisting><![CDATA[select ord.computeTotal(prod.getMultiplier()) from ProductEvent as prod, OrderEvent as ord]]></programlisting>

			<para>
				Instance methods may also be chained: Your EPL may invoke a method on the result returned by a method invocation.
			</para>

			<para>
				Assume that your product event exposes a method <literal>getZone</literal> which returns a zone object. Assume that the Zone class declares a method <literal>checkZone</literal>.
				This example statement invokes a method chain:
			</para>
            <programlisting><![CDATA[select prod.getZone().checkZone("zone 1") from ProductEvent as prod]]></programlisting>

			<para>
				Use the backwards apostrophe ` (aka. back tick) character to escape stream names in the <literal>from</literal>-clause and in <literal>on-</literal>trigger statements (e.g. <literal>from MyEvent as `order`...</literal>).
			</para>
		</sect2>

    </sect1>
    
    <sect1 xml:id="epl-where-clause">
        <title>Specifying Search Conditions: The <emphasis>Where</emphasis> Clause</title>

		<indexterm><primary>where clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>where clause</secondary></indexterm>
		<para>
			The <literal>where</literal> clause is an optional clause in EPL statements. Via the <literal>where</literal> clause event streams can be joined and correlated. 
		</para>
		
		<tip>
			<para>
				For filtering events in order to remove unwanted events, use filters as part of the <literal>from</literal> clause as described in <xref linkend="epl-from-clause-filter"/> 
				or for patterns in <xref linkend="pattern-filter"/>.
			</para>
			<para>
				Place expressions that remove unwanted events into parenthesis right after the event type, like <literal>... from OrderEvent(fraud.severity = 5 and amount > 500) ...</literal>. 
				There is related information at <xref linkend="processingmodel_indexes_filterindexes"/> and <xref linkend="perf-tips-5"/>.
			</para>
		</tip>

		<para>
			Any expression can be placed in the <literal>where</literal> clause. Typically you would use comparison operators <literal>=, &lt; , &gt; , &gt;=, &lt;=, !=, &lt;&gt;, is null, is not null</literal> and logical combinations via <literal>and</literal> and <literal>or</literal> for joining, correlating or comparing events. 
			The <literal>where</literal> clause introduces join conditions as outlined in <xref linkend="epl-join"/>.
		</para>
		
		<para>
			Some examples are listed below.
		</para>		
		<programlisting>...where settlement.orderId = order.orderId</programlisting>
		<programlisting>...where exists (select orderId from Settlement#time(1 min) where settlement.orderId = order.orderId)</programlisting>
		
		<para>
			The following two EPL statements are equivalent since both queries filter events by the <literal>amount</literal> property value and both queries do not specify a data window. 
        </para>
        <programlisting><![CDATA[// preferable: specify filter criteria with the "eventtype(...filters...)" notation
@name('first') select * from Withdrawal(amount > 200)]]></programlisting>
        <programlisting><![CDATA[// equivalent only when there is no data window
@name('second') select * from Withdrawal where amount > 200]]></programlisting>
        
        <para>
			You can control whether the engine rewrites the second query to the form of the first query. If you specify @Hint('disable_whereexpr_moveto_filter') you can instruct the engine to not move the <literal>where</literal>-clause expression into the filter.
        </para>

    </sect1>

    <sect1 xml:id="epl-grouping-aggregating">
        <title>Aggregates and Grouping: The <emphasis>Group-By</emphasis> Clause and the <emphasis>Having</emphasis> Clause</title>

        <sect2 xml:id="epl-grouping-aggregate-functions" >
				<title>Using Aggregate Functions</title>
			
				<para>
					The aggregate functions are further documented in <xref linkend="epl-function-aggregation"/>. 
					You can use aggregate functions to calculate and summarize data from event properties.
				</para>

				<para>
					For example, to find out the total price for all stock tick events in the last 30 seconds, type:
				</para>
			    <programlisting>select sum(price) from StockTickEvent#time(30 sec)</programlisting>

				<para>
					Aggregation functions do not require the use of data windows. The examples herein specify data windows for the purpose of example.
					An alternative means to instruct the engine when to start and stop aggregating and on what level to aggregate is via context declarations.
				</para>

				<para>
					For example, to find out the total price for all stock tick events since statement start, type:
				</para>
			    <programlisting>select sum(price) from StockTickEvent</programlisting>

				<para>
					Here is the syntax for aggregate functions: 
				</para>
				<synopsis><emphasis>aggregate_function</emphasis>( [all | distinct] <emphasis>expression</emphasis> [,<emphasis>expression</emphasis> [,...]] 
    [, group_by:<emphasis>local_group_by</emphasis>] [, filter:<emphasis>filter_expression</emphasis>] )</synopsis>

				<para>
					You can apply aggregate functions to all events in an event stream window or to one or more groups of events (i.e. <literal>group by</literal>). From each set of events to which an aggregate function is applied, Esper generates a single value. 
				</para>

				<para>
					<literal>Expression</literal> is usually an event property name. However it can also be a constant, function, or any combination of event property names, constants, 
					and functions connected by arithmetic operators.
				</para>
				
				<para>
					You can provide a grouping dimension for each aggregation function by providing the optional <literal>group_by</literal> parameter as part of aggregation function parameters. 
					Please refer to <xref linkend="epl-grouping-agglocal"/>.
				</para>			

				<para>
					You can provide a filter expression for each aggregation function by providing the optional <literal>filter</literal> parameter as part of aggregation function parameters. 
					Please refer to <xref linkend="epl-grouping-aggfilter"/>.
				</para>			

				<para>
					For example, to find out the average price for all stock tick events in the last 30 seconds if the price was doubled:
				</para>
			    <programlisting>select avg(price * 2) from StockTickEvent#time(30 seconds)</programlisting>

				<para>
					You can use the optional keyword <literal>distinct</literal> with all aggregate functions to eliminate duplicate values before the aggregate function is applied. The optional 
					keyword <literal>all</literal> which performs the operation on all events is the default. 
				</para>
			
				<para>
					You can use aggregation functions in a <literal>select</literal> clause and in a <literal>having</literal> clause. You cannot use aggregate functions in a <literal>where</literal> clause, but you can use the <literal>where</literal> clause to restrict the events to which the aggregate is applied. The next query computes the average and sum of the price of stock tick events for the symbol IBM only, for the last 10 stock tick events regardless of their symbol.
				</para>
			    <programlisting>select 'IBM stats' as title, avg(price) as avgPrice, sum(price) as sumPrice
from StockTickEvent#length(10)
where symbol='IBM'</programlisting>				
								
				<para>
					In the above example the length window of 10 elements is not affected by the <literal>where</literal> clause, i.e. all events enter and leave the length window regardless of their symbol. If you only care about the last 10 IBM events, you need to add filter criteria as below.
				</para>
			    <programlisting>select 'IBM stats' as title, avg(price) as avgPrice, sum(price) as sumPrice
from StockTickEvent(symbol='IBM')#length(10)
where symbol='IBM'</programlisting>				

				<para>
					You can use aggregate functions with any type of event property or expression, with the following exceptions:
				</para>

				<orderedlist spacing="compact">
					<listitem>
						<para>
							You can use <literal>sum, avg, median, stddev, avedev</literal> with numeric event properties only	
						</para>
					</listitem>
				</orderedlist>

				<para>
					Esper ignores any null values returned by the event property or expression on which the aggregate function is operating, except for the <literal>count(*)</literal> function, which counts null values as well. All aggregate functions return null if the data set contains no events, or if all events in the data set contain only null values for the aggregated expression.
				</para>
		</sect2>

        <sect2 xml:id="epl-grouping-group-by" >
				 <title>Organizing Statement Results into Groups: The <emphasis>Group-by</emphasis> Clause</title>

				<indexterm><primary>group by clause</primary></indexterm>
				<indexterm><primary>EPL</primary><secondary>group by clause</secondary></indexterm>

				<para>
					The <literal>group by</literal> clause is optional in all EPL statements. The <literal>group by</literal> clause divides the output of an EPL statement into groups. You can group by one or more event property names, or by the result of computed expressions. When used with aggregate functions, <literal>group by</literal> retrieves the calculations in each subgroup. You can use <literal>group by</literal> without aggregate functions, but generally that can produce confusing results.
				</para>
	
				<para>
					For example, the below statement returns the total price per symbol for all stock tick events in the last 30 seconds:
				</para>
				<programlisting>select symbol, sum(price) from StockTickEvent#time(30 sec) group by symbol</programlisting>
	
				<para>
					The syntax of the <literal>group by</literal> clause is:
				</para>
				<synopsis>group by <emphasis>aggregate_free_expression</emphasis> [, <emphasis>aggregate_free_expression</emphasis>] [, ...]</synopsis>
			
				<para>
					Esper places the following restrictions on expressions in the <literal>group by</literal> clause:
				</para>
	
				<orderedlist spacing="compact">
					<listitem>
						<para>
							Expressions in the <literal>group by</literal> cannot contain aggregate functions.
						</para>
					</listitem>
					<listitem>
						<para>
							When grouping an unbound stream, i.e. no data window is specified onto the stream providing groups, or when using output rate limiting with the ALL keyword, you should ensure your group-by expression does not return an unlimited number of values. If, for example, your group-by expression is a fine-grained timestamp, group state that accumulates for an unlimited number of groups potentially reduces available memory significantly.
							Use a @Hint as described below to instruct the engine when to discard group state.
						</para>
					</listitem>
				</orderedlist>
				
				<para>
					You can list more then one expression in the <literal>group by</literal> clause to nest groups. Once the sets are established with <literal>group by</literal> the aggregation
					functions are applied. This statement posts the median volume for all stock tick events in the last 30 seconds per symbol and tick data feed. Esper posts one event for each group to statement listeners:
				</para>
				<programlisting>select symbol, tickDataFeed, median(volume) 
from StockTickEvent#time(30 sec) 
group by symbol, tickDataFeed</programlisting>
				
				<para>
					In the statement above the event properties in the <literal>select</literal> list (symbol, tickDataFeed) are also listed in the <literal>group by</literal> clause.
					The statement thus follows the SQL standard which prescribes that non-aggregated event properties in the <literal>select</literal> list must match the 
					<literal>group by</literal> columns.
				</para>

				<para>
					Esper also supports statements in which one or more event properties in the <literal>select</literal> list are not listed in the <literal>group by</literal> clause.
					The statement below demonstrates this case. It calculates the standard deviation since statement start over stock ticks aggregating by symbol and posting for 
					each event the symbol, tickDataFeed and the standard deviation on price.
				</para>
				<programlisting>select symbol, tickDataFeed, stddev(price) from StockTickEvent group by symbol</programlisting>
				
				<para>
					The above example still aggregates the <literal>price</literal> event property based on the <literal>symbol</literal>, but produces one event per incoming event, not one 
					event per group.
				</para>

				<para>
					Additionally, Esper supports statements in which one or more event properties in the <literal>group by</literal> clause are not listed in the <literal>select</literal> list.
					This is an example that calculates the mean deviation per <literal>symbol</literal> and <literal>tickDataFeed</literal> and posts one event per group with <literal>symbol</literal> and mean deviation of price in the generated events. Since tickDataFeed is not in the posted results, this can potentially be confusing.
				</para>
				<programlisting>select symbol, avedev(price) 
from StockTickEvent#time(30 sec) 
group by symbol, tickDataFeed</programlisting>

				<para>
					Expressions are also allowed in the <literal>group by</literal> list:
				</para>
				<programlisting>select symbol * price, count(*) from StockTickEvent#time(30 sec) group by symbol * price</programlisting>
				
				<para>
					If the <literal>group by</literal> expression resulted in a null value, the null value becomes its own group. All null values are aggregated into the same group. If you are using the <literal>count(expression)</literal> aggregate function which does not count null values, the count returns zero if only null values are encountered.								
				</para>

				<para>
					You can use a <literal>where</literal> clause in a statement with <literal>group by</literal>. Events that do not satisfy the conditions in the <literal>where</literal> clause are eliminated before any grouping is done. For example, the statement below posts the number of stock ticks in the last 30 seconds with a volume larger then 100, posting one event per group (symbol).
				</para>
				<programlisting>select symbol, count(*) from StockTickEvent#time(30 sec) where volume &gt; 100 group by symbol</programlisting>

				<sect3 xml:id="epl-groupby-hints" >
					 <title>Hints Pertaining to Group-By</title>
					<para>
						The Esper engine reclaims aggregation state agressively when it determines that a group has no data points, based on the data in the data windows. When your application data creates a large number of groups with a small or zero number of data points then performance may suffer as state is reclaimed and created anew. Esper provides the <literal>@Hint('disable_reclaim_group')</literal> hint that you can specify as part of an EPL statement text to avoid group reclaim.
					</para>

					<para>
						When aggregating values over an unbound stream (i.e. no data window is specified onto the stream) and when your group-by expression returns an unlimited number of values, for example when a timestamp expression is used, 
						then please note the next hint.  
					</para>
					
					<para>
						A sample statement that aggregates stock tick events by timestamp, assuming the event type offers a property by name <literal>timestamp</literal> that, reflects time in high resolution, for example arrival or system time:
					</para>
					<programlisting>// Note the below statement could lead to an out-of-memory problem:
select symbol, sum(price) from StockTickEvent group by timestamp</programlisting>
					
					<para>
						As the engine has no means of detecting when aggregation state (sums per symbol) can be discarded, you may use the following hints to control aggregation state lifetime.
					</para>
					
					<para>
						The @Hint("<literal>reclaim_group_aged=</literal><emphasis>age_in_seconds</emphasis>") hint instructs the engine to discard aggregation state that has not been 
						updated for <emphasis>age_in_seconds</emphasis> seconds. 
					</para>
					
					<para>
						The optional @Hint("<literal>reclaim_group_freq=</literal><emphasis>sweep_frequency_in_seconds</emphasis>") can be used in addition to control the frequency at which the engine sweeps aggregation 
						state to determine aggregation state age and remove state that is older then <emphasis>age_in_seconds</emphasis> seconds.
						If the hint is not specified, the frequency defaults to the same value as <emphasis>age_in_seconds</emphasis>.
					</para>

					<para>
						The updated sample statement with both hints:
					</para>
					<programlisting>// Instruct engine to remove state older then 10 seconds and sweep every 5 seconds
@Hint('reclaim_group_aged=10,reclaim_group_freq=5')
select symbol, sum(price) from StockTickEvent group by timestamp</programlisting>

					<para>
						Variables may also be used to provide values for <emphasis>age_in_seconds</emphasis> and <emphasis>sweep_frequency_in_seconds</emphasis>. 
					</para>

					<para>
						This example statement uses a variable named <literal>varAge</literal> to control how long aggregation state remains in memory, and the engine defaults the sweep frequency to the same value as the variable provides:
					</para>
					<programlisting>@Hint('reclaim_group_aged=varAge')
select symbol, sum(price) from StockTickEvent group by timestamp</programlisting>
				</sect3>
		</sect2>

        <sect2 xml:id="epl-grouping-rollup">
			 <title>Using Group-By with Rollup, Cube and Grouping Sets</title>
			 
			 <para>
				 EPL supports the SQL-standard <literal>rollup</literal>, <literal>cube</literal> and <literal>grouping sets</literal> keywords. These keywords are available only in the <literal>group-by</literal> clause and instruct the engine to compute higher-level (or super-aggregate) aggregation values, i.e. to perform multiple levels of analysis (groupings) at the same time.
			 </para>
			 
			 <para>
				 EPL also supports the SQL-standard <literal>grouping</literal> and <literal>grouping_id</literal> functions. These functions can be used in the <literal>select</literal>-clause, <literal>having</literal>-clause or <literal>order by</literal>-clause to obtain information about the current row's grouping level in expressions. Please see <xref linkend="epl-single-row-function-grouping"/>.
			 </para>

			<para>
				Detailed examples and information in respect to output rate limiting can be found in <xref linkend="outputspec-groupedrowpergrouprollup"/>.
			</para>
 
			 <para>
			     Use the <literal>rollup</literal> keyword in the <literal>group-by</literal> lists of expressions to compute the equivalent of an OLAP dimension or hierarchy. 
			 </para>
			 
			 <para>
				 For example, the following statement outputs for each incoming event three rows. The first row contains the total volume per symbol and feed, the second row contains the total volume per symbol and the third row contains the total volume overall. This example aggregates across all events for each aggregation level (3 groupings) since it declares no data window:
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by rollup(symbol, tickDataFeed)</programlisting>

			<para>
				The value of <literal>tickDataFeed</literal> is <literal>null</literal> for the output row that contains the total per symbol and the output row that contains the total volume overall. 
				The value of both <literal>symbol</literal> and <literal>tickDataFeed</literal> is <literal>null</literal> for the output row that contains the overall total.
			</para>

			 <para>
			     Use the <literal>cube</literal> keyword in the <literal>group-by</literal> lists of expressions to compute a cross-tabulation. 
			 </para>
			 
			 <para>
				 The following statement outputs for each incoming event four rows. The first row contains the total volume per symbol and feed, the second row contains the total volume per symbol, the third row contains the total volume per feed and the forth row contains the total volume overall (4 groupings):
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by cube(symbol, tickDataFeed)</programlisting>
			
			<para>
				The <literal>grouping sets</literal> keywords allows you to specify only the groupings you want. It can thus be used to generate the same groupings that simple <literal>group-by</literal> expressions, <literal>rollup</literal> or <literal>cube</literal> would produce.
			</para>

			 <para>
				 In this example each incoming event causes the engine to compute two output rows: The first row contains the total volume per symbol and the second row contains the total volume per feed (2 groupings):
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by grouping sets(symbol, feed)</programlisting>

			<para>
				Your <literal>group-by</literal> expression can list grouping expressions and use <literal>rollup</literal>, <literal>cube</literal> and <literal>grouping sets</literal> keywords in addition or in combination.
			</para>

			 <para>
				 This statement outputs the total per combination of symbol and feed and the total per symbol (2 groupings):
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by symbol, rollup(tickDataFeed)</programlisting>

			<para>
				You can specify combinations of expressions by using parenthesis.
			</para>

			 <para>
				 The next statement is equivalent and also outputs the total per symbol and feed and the total per symbol (2 groupings, note the parenthesis):
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by grouping sets ((symbol, tickDataFeed), symbol)</programlisting>

			 <para>
				 Use empty parenthesis to aggregate across all dimensions.
			 </para>

			 <para>
				 This statement outputs the total per symbol, the total per feed and the total overall (3 groupings):
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by grouping sets (symbol, tickDataFeed, ())</programlisting>

			<para>
				The order of any output events for both insert and remove stream data is well-defined and exactly as indicated before. For example, specifying <literal>grouping sets ((), symbol, tickDataFeed)</literal> outputs a total overall, a total by symbol and a total by feed in that order. If the statement has an <literal>order-by</literal>-clause then the ordering criteria of the <literal>order-by</literal>-clause take precedence. 
			</para>

			<para>
				You can use <literal>rollup</literal> and <literal>cube</literal> within <literal>grouping sets</literal>.
			</para>
			
			 <para>
				 This statement outputs the total per symbol and feed, the total per symbol, the total overall and the total by feed (4 groupings):
			 </para>
			<programlisting>select symbol, tickDataFeed, sum(volume) from StockTickEvent
group by grouping sets (rollup(symbol, tickDataFeed), tickDataFeed)</programlisting>

			<note>
				 <para>
					 In order to use any of the  <literal>rollup</literal>, <literal>cube</literal> and <literal>grouping sets</literal> keywords the statement must be fully-aggregated.
					 All non-aggregated properties in the <literal>select</literal>-clause, <literal>having</literal>-clause or <literal>order-by</literal>-clause must also be listed in the <literal>group by</literal> clause.
				</para>
			</note>

			<sect3 xml:id="epl-grouping-rollup-examples" >
				 <title>Grouping Dimension Examples</title>
				 
				 <para>
					 This section provides additional examples of <literal>group-by</literal>-clauses and groupings or dimensions. 
					 The examples use event properties <literal>a, b, c, d, e</literal> to keep the examples easy to read.
					 Empty parenthesis <literal>()</literal> stand for aggregation overall (across all dimensions).
				 </para>
				 
				 <para>
					 If a statement provides no <literal>order-by</literal> clause, its order of output events is exactly as indicated below. Otherwise <literal>order-by</literal> takes precedence
					 and within the same ordering criteria the order of output events is as indicated below.
				 </para>

				 <table frame="topbot">
					<title></title>
					<tgroup cols="2">
						<colspec colwidth="1.1*"/>
						<colspec colwidth="1.4*"/>
						<thead>
							<row>
								<entry><literal>Group-By</literal> Clause</entry>
								<entry>Grouping</entry>
							</row>
						</thead>
						<tbody>
							<row>
								<entry><programlisting><![CDATA[group by a, b, c]]></programlisting></entry>
								<entry><programlisting><![CDATA[a, b, c]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by rollup(a, b, c)]]></programlisting></entry>
								<entry><programlisting><![CDATA[a, b, c
a, b
a
()]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by a, rollup(b, c)]]></programlisting></entry>
								<entry><programlisting><![CDATA[a, b, c
a, b
a]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by rollup(a, b), rollup(c, d)]]></programlisting></entry>
								<entry><programlisting><![CDATA[a,b,c,d
a,b,c
a,b
a,c,d
a,c
a
c,d
c
()]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by cube(a, b, c)]]></programlisting></entry>
								<entry><programlisting><![CDATA[a, b, c
a, b
a, c
a
b, c
b
c
()]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by cube(a, b, c, d)]]></programlisting></entry>
								<entry><programlisting><![CDATA[a,b,c,d
a,b,c
a,b,d
a,b
a,c,d
a,c
a,d
a
b,c,d
b,c
b,d
b
c,d
c
d
()]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by grouping sets(a, b, c)]]></programlisting></entry>
								<entry><programlisting><![CDATA[a
b
c]]></programlisting></entry>
							</row>
							<row>
								<entry><programlisting><![CDATA[group by grouping sets((a, b), rollup(c, d))]]></programlisting></entry>
								<entry><programlisting><![CDATA[a,b
c,d
c
()]]></programlisting></entry>
							</row>
						</tbody>
					</tgroup>
				</table> 
				
				<para>
					The following table outlines sample equivalent <literal>group-by</literal>-clauses.
				</para>

			   <table frame="topbot">
					<title>Equivalent <literal>Group-By</literal>-Clause Expressions</title>
					<tgroup cols="2">
						<colspec colwidth="0.8*"/>
						<colspec colwidth="0.8*"/>
						<thead>
							<row>
								<entry>Expression</entry>
								<entry>Equivalent</entry>
							</row>
						</thead>
						<tbody>
							<row>
								<entry>group by a, b</entry>
								<entry>group by grouping sets((a, b))</entry>
							</row>
							<row>
								<entry>group by rollup(a, b)</entry>
								<entry>group by grouping sets((a, b), a, ())</entry>
							</row>
							<row>
								<entry>group by cube(a, b)</entry>
								<entry>group by grouping sets((a, b), a, b, ())</entry>
							</row>
							<row>
								<entry>group by a, b, rollup(c, d)</entry>
								<entry>group by grouping sets((a, b, c, d), (a, b, c), (a, b))</entry>
							</row>
							<row>
								<entry>group by rollup((a, b), c)</entry>
								<entry>group by grouping sets((a, b, c), (a, b), ())</entry>
							</row>
							<row>
								<entry>group by grouping sets((a))</entry>
								<entry>group by grouping sets(a)</entry>
							</row>
						</tbody>
					</tgroup>
				</table> 
			</sect3>
			
			<sect3 xml:id="epl-grouping-rollup-other" >
				 <title>Rollup Usage Notes</title>
				 
				 <para>
					 The <literal>prev</literal> and <literal>prior</literal> functions returns the previous event's property values and since they are not aggregation functions
					 return the same value for each grouping. Declared or alias expressions and correlated subqueries also receive the same value for each grouping.
				 </para>

				 <para>
					 Context partitions operate on a higher level then rollups, i.e. rollups are never across context partitions.
				 </para>
			</sect3>
		</sect2>

        <sect2 xml:id="epl-grouping-agglocal" >
			 <title>Specifying Grouping for Each Aggregation Function</title>
			 <para>
				EPL allows each aggregation function to specify its own grouping criteria. This is useful for aggregating across multiple dimensions.
			 </para>

			<para>
				The syntax for the <literal>group_by</literal> parameter for use with aggregation functions is: 
			</para>
			<synopsis>group_by: ( [<emphasis>expression</emphasis> [,<emphasis>expression</emphasis> [,...]]] )</synopsis>
			
			<para>
				 The <literal>group_by</literal> identifier can occur at any place within the aggregation function parameters. It follows a colon and within parenthesis an 
				 optional list of grouping expressions. The parenthesis are not required when providing a single expression. 
				 For grouping on the top level (overall aggregation) please use <literal>()</literal> empty parenthesis.
			</para>		
  
			<para>
				The presence of <literal>group_by</literal> aggregation function parameters, the grouping expressions as well as the <literal>group-by</literal> clause 
				determine the number of output rows for queries as further described in <xref linkend="processingmodel_aggregation"/>.
			</para>
			 
			<para>
				For un-grouped queries (without a <literal>group by </literal> clause), if any aggregation function specifies a <literal>group_by</literal> other than the <literal>()</literal> overall group, the query executes
				as aggregated and un-grouped.
			</para>

			<para>
			  For example, the next statement is an aggregated (but not fully aggregated) and ungrouped query and outputs various totals for each arriving event:
			</para>
			<programlisting>select sum(price, group_by:()) as totalPriceOverall,
  sum(price, group_by:account) as totalPricePerAccount,
  sum(price, group_by:(account, feed)) as totalPricePerAccountAndFeed
from Orders</programlisting>

			<para>
				For grouped queries (with a <literal>group by </literal> clause), if all aggregation functions specifiy either no <literal>group_by</literal> or <literal>group_by</literal> criteria that subsume the criteria in the <literal>group by</literal> clause, the query executes as a fully-aggregated and grouped query. Otherwise the query executes as an aggregated and grouped query.
			</para>

			<para>
			  The next example is fully-aggregated and grouped and it computes, for the last one minute of orders, the ratio of orders per account compared to all orders:
			</para>
			<programlisting>select count(*)/count(*, group_by:()) as ratio
from Orders#time(1 min) group by account</programlisting>

			<para>
			  The next example is an aggregated (and not fully-aggregated) and grouped query that in addition outputs a count per order category:
			</para>
			<programlisting>select count(*) as cnt, count(*, group_by:()) as cntOverall,  count(*, group_by:(category))  as cntPerCategory
from Orders#time(1 min) group by account</programlisting>

			<para>
				Please note the following restrictions:
			</para>
	
			<orderedlist spacing="compact">
				<listitem>
					<para>
						Expressions in the <literal>group_by</literal> cannot contain aggregate functions.
					</para>
				</listitem>
				<listitem>
					<para>
						Hints pertaining to group-by are not available when a statement specifies aggregation functions with <literal>group_by</literal>.
					</para>
				</listitem>
				<listitem>
					<para>
						The <literal>group_by</literal> aggregation function parameters are not available in subqueries, match-recognize, statements that aggregate into tables using <literal>into table</literal> or in combination with <literal>rollup</literal> and <literal>grouping sets</literal>.
					</para>
				</listitem>
			</orderedlist>
		</sect2>

        <sect2 xml:id="epl-grouping-aggfilter" >
			 <title>Specifying a Filter Expression for Each Aggregation Function</title>
			 <para>
				EPL allows each aggregation function to specify its own filter expression. This is useful for conditionally aggregating.
			 </para>

			<para>
				The syntax for the <literal>filter</literal> parameter for use with aggregation functions is: 
			</para>
			<synopsis>filter:<emphasis>expression</emphasis></synopsis>
			
			<para>
				 The <literal>filter</literal> identifier can occur at any place within the aggregation function parameters. It follows a colon and the filter expression.
				 The filter expression must return a <literal>boolean</literal>-type value.
			</para>		
  			 
			<para>
				If a filter expression is present, the engine evaluates the filter expression to determine whether to update the aggregation.
			</para>		

			<para>
			  For example, the next statement returns the total price of small orders (price less 100), the total price of large orders (price >= 100), as well as the events themselves of each category, considering the last 10 seconds of orders:
			</para>
			<programlisting>select 
  sum(price, filter: price &lt; 100) as smallOrderTotal, 
  sum(price, filter: price &gt;= 100) as largeOrderTotal, 
  window(*, filter: price &lt; 100) as smallOrderEvents, 
  window(*, filter: price &gt;= 100) as largeOrderEvents
    from Orders#time(10)</programlisting>
    
			<note>
				<para>
					Filter expression that are parameters to aggregation functions must return reproducible results:  
					When the expression is evaluated against the same input values it should return the same result.
					Aggregation functions and subqueries are not allowed therein.
				</para>
			</note>
		</sect2>

        <sect2 xml:id="epl-grouping-having" >
			 <title>Selecting Groups of Events: The <emphasis>Having</emphasis> Clause</title>

				<indexterm><primary>having clause</primary></indexterm>
				<indexterm><primary>EPL</primary><secondary>having clause</secondary></indexterm>
				<para>
					Use the <literal>having</literal> clause to pass or reject events defined by the <literal>group-by</literal> clause. The <literal>having</literal> clause sets conditions for the <literal>group by</literal> clause in the same way <literal>where</literal> sets conditions for the <literal>select</literal> clause, except <literal>where</literal> cannot include aggregate functions, while <literal>having</literal> often does.
				</para>
			
				<para>
					This statement is an example of a <literal>having</literal> clause with an aggregate function. It posts the total price per symbol for the last 30 seconds of stock tick events for only those symbols in which the total price exceeds 1000. The <literal>having</literal> clause eliminates all symbols where the total price is equal or less then 1000.
				</para>
				<programlisting>select symbol, sum(price) 
from StockTickEvent#time(30 sec) 
group by symbol 
having sum(price) &gt; 1000</programlisting>

				<para>
					To include more then one condition in the <literal>having</literal> clause combine the conditions with <literal>and</literal>, <literal>or</literal> or <literal>not</literal>.
					This is shown in the statement below which selects only groups with a total price greater then 1000 and an average volume less then 500.
				</para>
				<programlisting>select symbol, sum(price), avg(volume)
from StockTickEvent#time(30 sec) 
group by symbol 
having sum(price) &gt; 1000 and avg(volume) &lt; 500</programlisting>			

				<para>
					A statement with the <literal>having</literal> clause should also have a <literal>group by</literal> clause. If you omit <literal>group-by</literal>, all the events not excluded
					by the <literal>where</literal> clause return as a single group. In that case <literal>having</literal> acts like a <literal>where</literal> except that <literal>having</literal> can have aggregate functions. 
				</para>
				
				<para>
					The <literal>having</literal> clause can also be used without <literal>group by</literal> clause as the below example shows. The example below posts events where the price is less then the current running average price of all stock tick events in the last 30 seconds.
				</para>
				<programlisting>select symbol, price, avg(price) 
from StockTickEvent#time(30 sec) 
having price &lt; avg(price)</programlisting>

		</sect2>

        <sect2 xml:id="epl-filter-where-grouping-having-interactions" >
			 <title>How the Stream Filter, <emphasis>Where</emphasis>, <emphasis>Group By</emphasis> and <emphasis>Having</emphasis>-Clauses Interact</title>
				<para>
					When you include filters, the <literal>where</literal> condition, the <literal>group by</literal> clause and the <literal>having</literal> condition in an EPL statement
					the sequence in which each clause affects events determines the final result:
				</para>

				<orderedlist spacing="compact">
					<listitem>
						<para>
							The event stream's filter condition, if present, dictates which events enter a window (if one is used). The filter discards any events not meeting filter criteria.
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>where</literal> clause excludes events that do not meet its search condition.
						</para>
					</listitem>
					<listitem>
						<para>
							Aggregate functions in the select list calculate summary values for each group.
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>having</literal> clause excludes events from the final results that do not meet its search condition.
						</para>
					</listitem>
				</orderedlist>

				<para>
					The following query illustrates the use of filter, <literal>where</literal>, <literal>group by</literal> and <literal>having</literal> clauses in one statement with 
					a <literal>select</literal> clause containing an aggregate function.
				</para>
				
				<programlisting>select tickDataFeed, stddev(price)
from StockTickEvent(symbol='IBM')#length(10) 
where volume > 1000
group by tickDataFeed 
having stddev(price) &gt; 0.8</programlisting>

				<para>
					Esper filters events using the filter criteria for the event stream <literal>StockTickEvent</literal>. In the example above only events with symbol IBM enter the length window over the last 10 events, all other events are simply discarded. The <literal>where</literal> clause removes any events posted by the length window (events entering the window and event leaving the window) that do not match the condition of volume greater then 1000. Remaining events are applied to the <literal>stddev</literal> standard deviation aggregate function for each tick data feed as specified in the <literal>group by</literal> clause. Each <literal>tickDataFeed</literal> value generates one event. Esper applies the <literal>having</literal> clause and only lets events pass for <literal>tickDataFeed</literal> groups with a standard deviation of price greater then 0.8.
				</para>

		</sect2>

        <sect2 xml:id="epl-group-by-versus-view">
			 <title>Comparing Keyed Segmented Context, the <emphasis>Group By</emphasis> Clause and <emphasis>#groupwin</emphasis> for Data Windows</title>

			<para>
				The keyed segmented context <emphasis>create context ... partition by</emphasis> and the <emphasis>group by</emphasis> clause as well as the built-in <emphasis>#groupwin</emphasis> are similar in their ability to group events but very different in their semantics. This section explains the key differences in their behavior and use.
			</para>
				
			<para>
				The keyed segmented context as declared with <emphasis>create context ... partition by</emphasis> and <emphasis>context .... select ...</emphasis> creates a new context partition per key value(s). The engine maintains separate data windows as well as separate aggregations per context partition; thereby the keyed segmented context applies to both. See <xref linkend="context_def_keyed"/> for additional examples.
			</para>

			<para>
				The <emphasis>group by</emphasis> clause works together with aggregation functions in your statement to produce an aggregation result per group. In greater detail, this means that when a new event arrives, the engine applies the expressions in the <emphasis>group by</emphasis> clause to determine a grouping key. If the engine has not encountered that grouping key before (a new group), the engine creates a set of new aggregation results for that grouping key and performs the aggregation changing that new set of aggregation results. If the grouping key points to an existing set of prior aggregation results (an existing group), the engine performs the aggregation changing the prior set of aggregation results for that group.
			</para>

			<para>
				The <emphasis>#groupwin</emphasis> instructs the system to have a separate data window per group, see <xref linkend="view-std-groupwin"/>. It causes allocation of separate data window(s) for each grouping key encountered.
			</para>
			
			<para>
				The table below summarizes the point:
			</para>
			
			<table frame="topbot">
				<title>Grouping Options</title>
				<tgroup cols="2">
					<colspec colwidth="0.50*"/>
					<colspec colwidth="1*"/>
					<thead>
						<row>
							<entry>Option</entry>
							<entry>Description</entry>
						</row>
					</thead>
					<tbody>
						<row>
							<entry>Keyed Segmented Context</entry>
							<entry>
							  <para>
							    Separate context partition per key value.
							  </para>
							  <para>
							    Affects all of data windows, aggregations, patterns, etc. (except variables which are global).
							  </para>
							</entry>
						</row>
						<row>
							<entry>Grouped Data Window (<emphasis>#groupwin</emphasis>)</entry>
							<entry>
							  <para>
							    Separate data window per key value.
							  </para>
							  <para>
							    Affects only the data window that is declared next to it.
							  </para>
							</entry>
						</row>
						<row>
							<entry>Group By Clause (<emphasis>group by</emphasis>)</entry>
							<entry>
							  <para>
							    Separate aggregation values per key value.
							  </para>
							  <para>
							    Affects only aggregation values.
							  </para>
							</entry>
						</row>
					</tbody>
				</tgroup>				
			</table>
			
			<para>
			  Please review the performance section for advice related to performance or memory-use.
			</para>

			<para>
				The next example shows queries that produce equivalent results. The query using the <emphasis>group by</emphasis> clause is generally preferable as is easier to read. The second form introduces the <literal>#uni</literal> special data window which computes univariate statistics for a given property:
			</para>

			<programlisting>select symbol, avg(price) from StockTickEvent group by symbol
// ... is equivalent to ...
select symbol, average from StockTickEvent#groupwin(symbol)#uni(price)</programlisting>

			<para>
				The next example shows two queries that are NOT equivalent as the length window is ungrouped in the first query, and grouped in the second query:
			</para>

			<programlisting>select symbol, sum(price) from StockTickEvent#length(10) group by symbol
// ... NOT equivalent to ...
select symbol, sum(price) from StockTickEvent#groupwin(symbol)#length(10)</programlisting>

			<para>
				The key difference between the two statements is that in the first statement the length window is ungrouped and applies to all events regardless of group. While in the second query each group gets its own length window. For example, in the second query events arriving for symbol "ABC" get a length window of 10 events, and events arriving for symbol "DEF" get their own length window of 10 events. 
			</para>

		</sect2>

    </sect1>

    <sect1 xml:id="epl-output-rate">
        <title>Stabilizing and Controlling Output: The <emphasis>Output</emphasis> Clause</title>

		<indexterm><primary>output control and stabilizing clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>output control and stabilizing</secondary></indexterm>

        <sect2 xml:id="epl-output-options" >
				<title>Output Clause Options</title>
			
				<para>
					The <literal>output</literal> clause is optional in Esper and is used to control or stabilize the rate at which events are output and to suppress output events. The EPL language provides for several different ways to control output rate.
				</para>

				<para>
					Here is the syntax for the <literal>output</literal> clause that specifies a rate in time interval or number of events:
				</para>
				<synopsis>output [after <emphasis>suppression_def</emphasis>] 
  [[all | first | last | snapshot] every <emphasis>output_rate</emphasis> [seconds | events]]
[and when terminated]</synopsis>

				<para>
					An alternate syntax specifies the time period between output as outlined in <xref linkend="epl-syntax-time-periods"/> :
				</para>
				<synopsis>output [after <emphasis>suppression_def</emphasis>] 
  [[all | first | last | snapshot] every <emphasis>time_period</emphasis>]
[and when terminated]</synopsis>

				<para>
					A crontab-like schedule can also be specified. The schedule parameters follow the pattern observer parameters and are further described in <xref linkend="pattern-timer-at"/> :
				</para>
				<synopsis>output [after <emphasis>suppression_def</emphasis>] 
  [[all | first | last | snapshot] at 
   (<emphasis>minutes, hours, days of month, months, days of week [, seconds]</emphasis>)]
[and when terminated]</synopsis>

				<para>
					For use with contexts, in order to trigger output only when a context partition terminates, specify <literal>when terminated</literal> as further described in <xref linkend="context_outputterminated"/>:
				</para>
				<synopsis>output [after <emphasis>suppression_def</emphasis>] 
  [[all | first | last | snapshot] when terminated 
  [and <emphasis>termination_expression</emphasis>]
  [then set variable_name = assign_expression [, variable_name = assign_expression [,...]]]
  ]</synopsis>

				<para>
					Last, output can be controlled by an expression that may contain variables, user-defined functions and information about the number of collected events. Output that is controlled by an expression is discussed in detail below.
				</para>

				<para>
					The <literal>after</literal> keyword and <emphasis>suppression_def</emphasis> can appear alone or together with further output conditions and suppresses output events.
				</para>

				<para>
					For example, the following statement outputs, every 60 seconds, the total price for all orders in the 30-minute time window:		
				</para>
			    <programlisting>select sum(price) from OrderEvent#time(30 min) output snapshot every 60 seconds</programlisting>

				<para>
					The <literal>all</literal> keyword is the default and specifies that all events in a batch should be output, each incoming row in the batch producing an output row.
					Note that for statements that group via the <literal>group by</literal> clause, the <literal>all</literal> keyword provides special behavior as below.
				</para>

				<para>
					The <literal>first</literal> keyword specifies that only the first event in an output batch is to be output.
					Using the <literal>first</literal> keyword instructs the engine to output the first matching event as soon as it arrives, and then ignores matching events for the time interval or number of events specified. 
					After the time interval elapsed, or the number of matching events has been reached, the next first matching event is output again and the following interval the engine again ignores matching events.
					For statements that group via the <literal>group by</literal> clause, the <literal>first</literal> keywords provides special behavior as below.
				</para>

				<para>
					The <literal>last</literal> keyword specifies to only output the last event at the end of the given time interval or after the given number of matching events
					have been accumulated. Again, for statements that group via the <literal>group by</literal> clause the <literal>last</literal> keyword provides special behavior as below.
				</para>

				<para>
					The <literal>snapshot</literal> keyword is often used with unbound streams and/or aggregation to output current aggregation results. While the other keywords control how a batch of events between output intervals is being considered, the <literal>snapshot</literal> keyword outputs current state of a statement independent of the last batch. Its output is comparable to the <literal>iterator</literal> method provided by a statement. More information on <literal>output snapshot</literal> can be found in <xref linkend="epl-output-snapshot"/>.
				</para>

				<para>
					The <emphasis>output_rate</emphasis> is the frequency at which the engine outputs events. It can be specified in terms of time or number of events. The value can be a number to denote a fixed output rate, or the name of a variable whose value is the output rate. By means of a variable the output rate can be controlled externally and changed dynamically at runtime. 
				</para>

				<para>
					Please consult the <xref linkend="appendix_outputspec"/> for detailed information on insert and remove stream output for the various <literal>output</literal> clause keywords.
				</para>
				
				<para>
				  For use with contexts you may append the keywords <literal>and when terminated</literal> to trigger output at the rate defined and in addition trigger output when the context partition terminates. Please see <xref linkend="context_outputterminated"/> for details.
				</para>
				
				<note>
					<para>
						Please see <xref linkend="appendix_outputlimit"/> for information on how the system retains input events and computes output events according to the specified output rate.
					</para>
				</note>
				
				<para>
					The time interval can also be specified in terms of minutes; the following statement is identical to the first one.
				</para>
			    <programlisting>select * from StockTickEvent output every 1.5 minutes</programlisting>

				<para>
					A second way that output can be stabilized is by batching events until a certain number of events have been collected:
				</para>				
			    <programlisting>select * from StockTickEvent output every 5 events</programlisting>
			    
				<para>
					Additionally, event output can be further modified by the optional <literal>last</literal> keyword, which causes output of only the last event to arrive into an output batch.
				</para>
			    <programlisting>select * from StockTickEvent output last every 5 events</programlisting>

				<para>
					Using the <literal>first</literal> keyword you can be notified at the start of the interval. The allows to watch for situations such as a rate falling below a threshold
					and only be informed every now and again after the specified output interval, but be informed the moment it first happens. 
				</para>
			    <programlisting>select * from TickRate where rate&lt;100 output first every 60 seconds</programlisting>

				<para>
					A sample statement using the Unix "crontab"-command schedule is shown next. See <xref linkend="pattern-timer-at"/> for details on schedule syntax. Here, output occurs every 15 minutes from 8am to 5:45pm (hours 8 to 17 at 0, 15, 30 and 45 minutes past the hour):
				</para>
				<synopsis>select symbol, sum(price) from StockTickEvent group by symbol output at (*/15, 8:17, *, *, *)</synopsis>

				<sect3 xml:id="epl-output-expression" >
					<title>Controlling Output Using an Expression</title>

					<indexterm><primary>output when</primary></indexterm>
					<para>
						Output can also be controlled by an expression that may check variable values, use user-defined functions and query built-in properties that provide additional information. The synopsis is as follows:
					</para>
					<synopsis>output [after <emphasis>suppression_def</emphasis>] 
  [[all | first | last | snapshot] when <emphasis>trigger_expression</emphasis> 
    [then set <emphasis>variable_name</emphasis> = <emphasis>assign_expression</emphasis> [, <emphasis>variable_name</emphasis> = <emphasis>assign_expression</emphasis> [,...]]]
  [and when terminated 
    [and <emphasis>termination_expression</emphasis>]
    [then set variable_name = assign_expression [, variable_name = assign_expression [,...]]]
  ]</synopsis>

					<para>
						The <literal>when</literal> keyword must be followed by a trigger expression returning a boolean value of true or false, indicating whether to output.
						Use the optional <literal>then</literal> keyword to change variable values after the trigger expression evaluates to true. An assignment expression assigns a new value to variable(s).
					</para>
					
					<para>
					  For use with contexts you may append the keywords <literal>and when terminated</literal> to also trigger output when the context partition terminates. Please see <xref linkend="context_outputterminated"/> for details. You may optionally specify a termination expression. If that expression is provided the 
					  engine evaluates the expression when the context partition terminates: The evaluation result of <literal>true</literal> means output occurs when the context partition terminates, <literal>false</literal> means no output occurs when the context partition terminates.
					  You may specify <literal>then set</literal> followed by a list of assignments to assign variables. Assignments are executed on context partition termination regardless of the termination expression, if present.
					</para>

					<para>
						Lets consider an example. The next statement assumes that your application has defined a variable by name OutputTriggerVar of boolean type. The statement outputs rows only when the OutputTriggerVar variable has a boolean value of true:
					</para>
					<synopsis>select sum(price) from StockTickEvent output when OutputTriggerVar = true</synopsis>

					<para>
						The engine evaluates the trigger expression when streams and data windows (if any) post one or more insert or remove stream events after considering the <literal>where</literal> clause, if present. It also evaluates the trigger expression when any of the variables used in the trigger expression, if any, changes value. Thus output occurs as follows:
					</para>

					<orderedlist spacing="compact">
						<listitem>
							<para>
								When there are insert or remove stream events and the <literal>when</literal> trigger expression evaluates to true, the engine outputs the resulting rows.
							</para>
						</listitem>
						<listitem>
							<para>
								When any of the variables in the <literal>when</literal> trigger expression changes value, the engine evaluates the expression and outputs results. Result output occurs within the minimum time interval of timer resolution.
							</para>
						</listitem>
					</orderedlist>

					<para>
						By adding a <literal>then</literal> part to the EPL, you can reset any variables after the trigger expression evaluated to true:
					</para>
			    <programlisting>select sum(price) from StockTickEvent 
  output when OutputTriggerVar = true  
  then set OutputTriggerVar = false</programlisting>

					<para>
						Expressions in the <literal>when</literal> and <literal>then</literal> may, for example, use variables, user defined functions or any of the built-in named properties that are described in the below list.
					</para>
					
					<para>
						The following built-in properties are available for use:
					</para>
					<table frame="topbot" xml:id="epl-output-expression-builtin" revision="2">
						<title>Built-In Properties for Use With Output When</title>
						<tgroup cols="2">
							<colspec colwidth="1*"/>
							<colspec colwidth="2.5*"/>
							<thead>
								<row>
									<entry>Built-In Property Name</entry>
									<entry>Description</entry>
								</row>
							</thead>
							<tbody>
								<row>
									<entry><literal>last_output_timestamp</literal></entry>
									<entry>Timestamp when the last output occurred for the statement; Initially set to time of statement creation</entry>
								</row>
								<row>
									<entry><literal>count_insert</literal></entry>
									<entry>Number of insert stream events</entry>
								</row>
								<row>
									<entry><literal>count_insert_total</literal></entry>
									<entry>Number of insert stream events in total (not reset when output occurs).</entry>
								</row>
								<row>
									<entry><literal>count_remove</literal></entry>
									<entry>Number of remove stream events</entry>
								</row>
								<row>
									<entry><literal>count_remove_total</literal></entry>
									<entry>Number of remove stream events in total (not reset when output occurs).</entry>
								</row>
							</tbody>
						</tgroup>
					</table>

					<para>
						The values provided by <literal>count_insert</literal> and <literal>count_remove</literal> are non-continues: The number returned for these properties may 'jump' up rather then count up by 1.
						The counts reset to zero upon output.
					</para>        
					
					<para>
						The following restrictions apply to expressions used in the output rate clause:
					</para>        
					<itemizedlist spacing="compact">
						<listitem>
							<para>
								Event property names cannot be used in the output clause.
							</para>
						</listitem>
						<listitem>
							<para>
								Aggregation functions cannot be used in the output clause.
							</para>
						</listitem>
						<listitem>
							<para>
								The <literal>prev</literal> previous event function and the <literal>prior</literal> prior event function cannot be used in the output clause.
							</para>
						</listitem>
					</itemizedlist>
				</sect3>

				<sect3 xml:id="epl-output-suppress" >
					<title>Suppressing Output With <literal>After</literal></title>

					<indexterm><primary>output</primary><secondary>suppressing output</secondary></indexterm>
					<indexterm><primary>after</primary></indexterm>
					<para>
						The <literal>after</literal> keyword and its time period or number of events parameters is optional and can occur after the <literal>output</literal> keyword, either alone or with output conditions as listed above.
					</para>

					<para>
						The synopsis of <literal>after</literal> is as follows:
					</para>
					<synopsis>output after <emphasis>time_period</emphasis> | <emphasis>number</emphasis> events [...]</synopsis>

					<para>
						When using <literal>after</literal> either alone or together with further output conditions, the engine discards all output events until the time period passed as measured from the start of the statement, or until the
						number of output events are reached. The discarded events are not output and do not count towards any further output conditions if any are specified.
					</para>

					<para>
						For example, the following statement outputs every minute the total price for all orders in the 30-minute time window but only after 30 minutes have passed:		
					</para>
					<programlisting>select sum(price) from OrderEvent#time(30 min) output after 30 min snapshot every 1 min</programlisting>

					<para>
						An example in which <literal>after</literal> occur alone is below, in a statement that outputs total price for all orders in the last minute but only after 1 minute passed, each time an event arrives or leaves the data window:
					</para>
					<programlisting>select sum(price) from OrderEvent#time(1 min) output after 1 min</programlisting>

					<para>
						To demonstrate <literal>after</literal> when used with an event count, this statement find pairs of orders with the same id but suppresses output for the first 5 pairs:
					</para>
					<programlisting>select * from pattern[every o=OrderEvent->p=OrderEvent(id=o.id)] output after 5 events</programlisting>
				</sect3>

				<sect3 xml:id="epl-output-snapshot" >
					<title>Output Snapshot</title>
					<indexterm><primary>output</primary><secondary>snapshot</secondary></indexterm>
					
					<para>
						For fully aggregated and un-grouped statements, <literal>output snapshot</literal> outputs a single row with current aggregation value(s).
					</para>

					<para>
						For aggregated ungrouped and grouped statements, as well as for unaggregated statements, 
						<literal>output snapshot</literal> considers events held by the data window and outputs a row for each event.
						If the statement specifies no data window or a join results in no rows, the output is no rows.
					</para>

					<para>
						For fully aggregated and grouped statements that select from a single stream (or pattern, non-joining) and that do not specify a data window, 
						the engine outputs current aggregation results for all groups. For fully aggregated and grouped statements with a join and/or data windows the output consists of aggregation values 
						according to events held in the data window (single stream) or that are join results (join).
					</para>

					<para>
						When the <literal>from</literal>-clause lists only tables, use <literal>output snapshot</literal> to output table contents. 
					</para>
				</sect3>
		</sect2>

        <sect2 xml:id="epl-output-groupby" >
				<title>Aggregation, Group By, Having and Output Clause Interaction</title>
			
				<para>
					Remove stream events can also be useful in conjunction with aggregation and the <literal>output</literal> clause: When the engine posts remove stream events for fully-aggregated queries, it presents the aggregation state before the expiring event leaves the data window. Your application can thus easily obtain a delta between the new aggregation value and the prior aggregation value.
				</para>
				
				<para>
					The engine evaluates the having-clause at the granularity of the data posted by data windows (if any) or when an event arrives (without a data windows). That is, if you utilize a time window and output every 10 events, the <literal>having</literal> clause applies to each individual event or events entering and leaving the time window (and not once per batch of 10 events).
				</para>

				<para>
					The <literal>output</literal> clause interacts in two ways with the <literal>group by</literal> and <literal>having</literal> clauses. First, in the <literal>output every n events </literal>case, the number <literal>n</literal> refers to the number of events arriving into the <literal>group by clause</literal>. That is, if the <literal>group by</literal> clause outputs only 1 event per group, or if the arriving events don't satisfy the <literal>having</literal> clause, then the actual number of events output by the statement could be fewer than <literal>n</literal>.
				</para>
				
				<para>
					Second, the <literal>last</literal>, <literal>all</literal> and <literal>first</literal> keywords have special meanings when used in a statement with aggregate functions and the <literal>group by</literal> clause:
				</para>
				<itemizedlist spacing="compact">
					<listitem>
						<para>
							When no keyword is specified, the engine produces an output row for each row in the batch or when using group-by then an output per group only for those groups present in the batch, following <xref linkend="processingmodel_aggregation"/>.
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>all</literal> keyword (the default) specifies that the most recent data for <emphasis>all</emphasis> groups seen so far should be output, whether or not these groups' aggregate values have just been updated
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>last</literal> keyword specifies that only groups whose aggregate values have been updated with the most recent batch of events should be output.
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>first</literal> keyword specifies that only groups whose aggregate values have been updated with the most recent batch of events should be output following the defined frequency, keeping frequency state for each group. 
						</para>
					</listitem>
					<listitem>
						<para>
							The <literal>snapshot</literal> keyword does not consider the recent batch and has special behavior as discussed in <xref linkend="epl-output-snapshot"/>.
						</para>
					</listitem>
				</itemizedlist>

				<para>
					Please consult the <xref linkend="appendix_outputspec"/> for detailed information on insert and remove stream output for aggregation and group-by.
				</para>

				<para>
					By adding an output rate limiting clause to a statement that contains a <emphasis>group by</emphasis> clause you can control output of groups to obtain one row for each group, generating an event per group at the given output frequency.
				</para>
				
				<para>
				  The next statement outputs total price per symbol cumulatively (no data window was used here). As it specifies the <literal>all</literal> keyword, the statement outputs the current value for all groups seen so far, regardless of whether the group was updated in the last interval. Output occurs after an interval of 5 seconds passed and at the end of each subsequent interval:
				</para>
				<programlisting>select symbol, sum(price) from StockTickEvent group by symbol output all every 5 seconds</programlisting>

				<para>
				  The below statement outputs total price per symbol considering events in the last 3 minutes. When events leave the 3-minute data window output also occurs as new aggregation values are computed. The <literal>last</literal> keyword instructs the engine to output only those groups that had changes. Output occurs after an interval of 10 seconds passed and at the end of each subsequent interval:
				</para>
				<programlisting>select symbol, sum(price) from StockTickEvent#time(3 min)
group by symbol output last every 10 seconds</programlisting>

				<para>
				  This statement also outputs total price per symbol considering events in the last 3 minutes. The <literal>first</literal> keyword instructs the engine to output as soon as there is a new value for a group. After output for a given group the engine suppresses output for the same group for 10 seconds and does not suppress output for other groups. Output occurs again for that group after the interval when the group has new value(s):
				</para>
				<programlisting>select symbol, sum(price) from StockTickEvent#time(3 min)
group by symbol output first every 10 seconds</programlisting>

		</sect2>
    </sect1>

    <sect1 xml:id="epl-order-by">
        <title>Sorting Output: the <emphasis>Order By</emphasis> Clause</title>

		<indexterm><primary>ordering output</primary></indexterm>
		<indexterm><primary>output ordering</primary></indexterm>
		<indexterm><primary>order by clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>order by clause</secondary></indexterm>

		<para>
			The <literal>order by</literal> clause is optional. It is used for ordering output events by their properties, or by expressions involving those properties. .				
		</para>

		<para>
			For example, the following statement outputs batches of 5 or more stock tick events that are sorted first by price ascending and then by volume ascending:
		</para>
		<programlisting>select symbol from StockTickEvent#time(60 sec) 
output every 5 events 
order by price, volume</programlisting>

		<para>
			Here is the syntax for the <literal>order by</literal> clause:				
		</para>
		<synopsis>order by <emphasis>expression</emphasis> [asc | desc] [, <emphasis>expression</emphasis> [asc | desc]] [, ...]</synopsis>

		<para>
			If the <literal>order by</literal> clause is absent then the engine still makes certain guarantees about the ordering of output: 
		</para>
		<itemizedlist spacing="compact">
			<listitem>
				<para>
					If the statement is not a join, does not group via <literal>group by</literal> clause and does not declare grouped data windows via <literal>#groupwin</literal>, the order in which events are delivered to listeners and through the <literal>iterator</literal> pull API is the order of event arrival.
				</para>
			</listitem>
			<listitem>
				<para>
					If the statement is a join or outer join, or groups, then the order in which events are delivered to listeners and through the <literal>iterator</literal> pull API is not well-defined. Use the <literal>order by</literal> clause if your application requires events to be delivered in a well-defined order.
				</para>
			</listitem>
		</itemizedlist>

		<para>
			Esper places the following restrictions on the expressions in the <literal>order by</literal> clause:
		</para>
		
		<orderedlist spacing="compact">
			<listitem>
				<para>
					All aggregate functions that appear in the <literal>order by</literal> clause must also appear in the <literal>select</literal> expression.
				</para>
			</listitem>
		</orderedlist>
		
		<para>
			Otherwise, any kind of expression that can appear in the <literal>select</literal> clause, 
			as well as any name defined in the <literal>select</literal> clause, is also valid in the order by clause.
		</para>		
		
		<para>
			By default all sort operations on string values are performed via the <literal>compare</literal> method and are thus not locale dependent. To account for differences in language or locale, see <xref linkend="config-engine-language"/> to change this setting.
		</para>
	</sect1>

    <sect1 xml:id="epl-limit">
        <title>Limiting Row Count: the <emphasis>Limit</emphasis> Clause</title>

		<indexterm><primary>limiting output row count</primary></indexterm>
		<indexterm><primary>output row count</primary></indexterm>
		<indexterm><primary>limit clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>limit clause</secondary></indexterm>

		<para>
			The <literal>limit</literal> clause is typically used together with the <literal>order by</literal> and <literal>output</literal> clause to limit your query results to those that fall within a specified range. You can use it to receive the first given number of result rows, or to receive a range of result rows. 
		</para>

		<para>
			There are two syntaxes for the <literal>limit</literal> clause, each can be parameterized by integer constants or by variable names. The first syntax is shown below:				
		</para>
		<synopsis>limit <emphasis>row_count</emphasis> [offset <emphasis>offset_count</emphasis>]</synopsis>

		<para>
			The required <emphasis>row_count</emphasis> parameter specifies the number of rows to output. The <emphasis>row_count</emphasis> can be an integer constant 
			and can also be the name of the integer-type variable to evaluate at runtime.
		</para>

		<para>
			The optional <emphasis>offset_count</emphasis> parameter specifies the number of rows that should be skipped (offset) at the beginning of the result set. A variable can also be used for this parameter.
		</para>

		<para>
			The next sample EPL query outputs the top 10 counts per property 'uri' every 1 minute.
		</para>
		<programlisting>select uri, count(*) from WebEvent 
group by uri 
output snapshot every 1 minute
order by count(*) desc 
limit 10</programlisting>

		<para>
			The next statement demonstrates the use of the <literal>offset</literal> keyword. It outputs ranks 3 to 10 per property 'uri' every 1 minute:
		</para>
		<programlisting>select uri, count(*) from WebEvent 
group by uri 
output snapshot every 1 minute
order by count(*) desc 
limit 8 offset 2</programlisting>

		<para>
			The second syntax for the <literal>limit</literal> clause is for SQL standard compatibility and specifies the offset first, followed by the row count:
		</para>
		<synopsis>limit <emphasis>offset_count</emphasis>[, <emphasis>row_count</emphasis>]</synopsis>

		<para>
			The following are equivalent:
		</para>
		<programlisting>limit 8 offset 2
// ...equivalent to
limit 2, 8</programlisting>

		<para>
			A negative value for <emphasis>row_count</emphasis> returns an unlimited number or rows, and a zero value returns no rows. If variables are used, then the current variable value at the time of output dictates the row count and offset. A variable returning a null value for <emphasis>row_count</emphasis> also returns an unlimited number or rows. 
		</para>
		
		<para>
			A negative value for offset is not allowed. If your variable returns a negative or null value for offset then the value is assumed to be zero (i.e. no offset).
		</para>

		<para>
			The <literal>iterator</literal> pull API also honors the <literal>limit</literal> clause, if present.
		</para>
		
	</sect1>

    <sect1 xml:id="epl-insert-into">
        <title>Merging Streams and Continuous Insertion: The <emphasis>Insert Into</emphasis> Clause</title>

		<indexterm><primary>insert into clause</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>insert into clause</secondary></indexterm>

		<para>
			The <literal>insert into</literal> clause is optional in Esper. The clause can be specified to make the results of a statement available as an event stream for use
			in further statements, or to insert events into a named window or table. The clause can also be used to merge multiple event streams to form a single stream of events.
		</para>

		<para>
			The syntax for the <literal>insert into</literal> clause is as follows:
		</para>

		<synopsis>insert [istream | irstream | rstream] into <emphasis>event_stream_name</emphasis>  [ ( [<emphasis>property_name</emphasis> [, property_name]] ) ]</synopsis>

		<para>
			The <literal>istream</literal> (default) and <literal>rstream</literal> keywords are optional. If no keyword or the <literal>istream</literal> keyword is specified, the engine supplies the insert stream events generated by the statement. The insert stream consists of the events entering the respective window(s) or stream(s). If the <literal>rstream</literal> keyword is specified, the engine supplies the remove stream events generated by the statement. The remove stream consists of the events leaving the respective window(s). 
		</para>

		<para>
			If your application specifies <literal>irstream</literal>, the engine inserts into the new stream both the insert and remove stream. This is often useful in connection with the <literal>istream</literal> built-in function that returns an inserted/removed boolean indicator for each event, see <xref linkend="epl-single-row-function-istream"/>.			
		</para>

		<para>
			The <literal>event_stream_name</literal> is an identifier that names the event stream (and also implicitly names the types of events in the stream) generated by the engine. 
			It may also specify a named window name or a table name.
			The identifier can be used in further statements to filter and process events of that event stream, unless inserting into a table. 
			The <literal>insert into</literal> clause can consist of just an event stream name, or an event stream name and one or more property names.
		</para>

		<para>
			The engine also allows listeners to be attached to a statement that contain an <literal>insert into</literal> clause. Listeners receive all events posted to the event stream.
		</para>

		<para>
			To merge event streams, simply use the same <literal>event_stream_name</literal> identifier in all EPL statements that merge their result event streams. Make sure to use the
			same number and names of event properties and event property types match up.
		</para>

		<para>
			Esper places the following restrictions on the <literal>insert into</literal> clause:
		</para>
		
		<orderedlist spacing="compact">
			<listitem>
				<para>
					The number of elements in the <literal>select</literal> clause must match the number of elements in the <literal>insert into</literal> clause if the clause specifies a list of event property names
				</para>
			</listitem>
			<listitem>
				<para>
					If the event stream name has already been defined by a prior statement or configuration, and the event property names and/or event types do not match, an exception is thrown at statement creation time.
				</para>
			</listitem>
		</orderedlist>

		<para>
			The following sample inserts into an event stream by name CombinedEvent:
		</para>
		<programlisting>insert into CombinedEvent
select A.customerId as custId, A.timestamp - B.timestamp as latency
  from EventA#time(30 min) A, EventB#time(30 min) B
 where A.txnId = B.txnId</programlisting>

		<para>
			Each event in the <literal>CombinedEvent</literal> event stream has two event properties named "custId" and "latency". The events generated by the above statement can be used in further statements, such as shown in the next statement:
		</para>

		<programlisting>select custId, sum(latency)
  from CombinedEvent#time(30 min)
 group by custId</programlisting>

		<para>
			The example statement below shows the alternative form of the <literal>insert into</literal> clause that explicitly defines the property names to use.
		</para>

		<programlisting>insert into CombinedEvent (custId, latency)
select A.customerId, A.timestamp - B.timestamp 
...</programlisting>

		<para>
			The <literal>rstream</literal> keyword can be useful to indicate to the engine to generate only remove stream events. This can be useful if you want to trigger 
			actions when events leave a window rather then when events enter a window. The statement below generates <literal>CombinedEvent</literal> events when
			EventA and EventB leave the window after 30 minutes.
		</para>

		<programlisting>insert rstream into CombinedEvent
select A.customerId as custId, A.timestamp - B.timestamp as latency
  from EventA#time(30 min) A, EventB#time(30 min) B
 where A.txnId = B.txnId</programlisting>

		<para>
			The <literal>insert into</literal> clause can be used in connection with patterns to provide pattern results to further statements for analysis:
		</para>

		<programlisting>insert into ReUpEvent
select linkUp.ip as ip 
from pattern [every linkDown=LinkDownEvent -> linkUp=LinkUpEvent(ip=linkDown.ip)]</programlisting>

		<sect2 xml:id="insert-into-property-transpose">
			<title>Transposing a Property to a Stream</title>			
			<para>
				Sometimes your events may carry properties that are themselves event objects. Therefore EPL offers a special syntax to insert the value of a property itself as an event into a stream:
			</para>
			<synopsis>insert into <emphasis>stream_name</emphasis> select <emphasis>property_name</emphasis>.* from ...</synopsis>

			<para>
				This feature is only supported for JavaBean events and for <literal>Map</literal> and Object-array (<literal>Object[]</literal>) event types that associate an event type name with the property type. It is not supported for <literal>XML</literal> events. Nested property names are also not supported.
			</para>

			<para>
				In this example, the class <literal>Summary</literal> with properties <literal>bid</literal> and <literal>ask</literal> that are of type <literal>Quote</literal> is:
			</para>
			<programlisting><![CDATA[public class Summary {
  private Quote bid;
  private Quote ask;
  ...]]></programlisting>			
  
			<para>
				The statement to populate a stream of <literal>Quote</literal> events is thus:
			</para>
			<programlisting><![CDATA[insert into MyBidStream select bid.* from Summary]]></programlisting>			
		</sect2>

		<sect2 xml:id="insert-into-merging-columns">
			<title>Merging Streams by Event Type</title>			
			<para>
				The <literal>insert into</literal> clause allows to merge multiple event streams into a event single stream. 
				The clause names an event stream to insert into by specifing an <emphasis>event_stream_name</emphasis>. The first statement that inserts into the named stream defines the stream's event types. Further statements that
				insert into the same event stream must match the type of events inserted into the stream as declared by the first statement.
			</para>

			<para>
			    One approach to merging event streams specifies individual colum names either in the <literal>select</literal> clause or in the <literal>insert into</literal> clause of the statement. This approach has been shown in earlier examples.
			</para>
			
			<para>
			    Another approach to merging event streams specifies the wildcard (*) in the <literal>select</literal> clause (or the stream wildcard) to select the underlying event. The events in the event stream must then 
			    have the same event type as generated by the <literal>from</literal> clause.
			</para>
			
			<para>
			    Assume a statement creates an event stream named MergedStream by selecting OrderEvent events:
			</para>
			<programlisting>insert into MergedStream select * from OrderEvent</programlisting>

			<para>
			    A statement can use the stream wildcard selector to select only OrderEvent events in a join:
			</para>
			<programlisting>insert into MergedStream select ord.* from ItemScanEvent, OrderEvent as ord</programlisting>

			<para>
			    And a statement may also use an application-supplied user-defined function to convert events to OrderEvent instances:
			</para>
			<programlisting>insert into MergedStream select MyLib.convert(item) from ItemScanEvent as item</programlisting>			

			<para>
			    Esper specifically recognizes a conversion function as follows: A conversion function must be the only selected column, and it must return either a Java object or <literal>java.util.Map</literal> or <literal>Object[]</literal> (object array).
			    Your EPL should not use the <literal>as</literal> keyword to assign a column name.
			</para>
		</sect2>

		<sect2 xml:id="insert-into-merging-types">
			<title>Merging Disparate Types of Events: Variant Streams</title>			
			<indexterm><primary>variant stream</primary></indexterm>
			<para>
				A <emphasis>variant stream</emphasis> is a predefined stream into which events of multiple disparate event types can be inserted. 
			</para>

			<para>
				A variant stream name may appear anywhere in a pattern or <literal>from</literal> clause. In a pattern, a filter against a variant stream matches any events of any of the event types inserted into the variant stream. 
				In a <literal>from</literal> clause including for named windows, data windows may hold events of any of the event types inserted into the variant stream.
			</para>

			<para>
				A variant stream is thus useful in problems that require different types of event to be treated the same.
			</para>

			<para>
				Variant streams can be declared by means of <literal>create variant schema</literal> or can be predefined via runtime or initialization-time configuration as described in <xref linkend="config-variantstream"/>. Your application may declare or predefine variant streams to carry events of a limited set of event types, or you may choose the variant stream to carry any and all types of events. This choice affects what event properties are available for consuming statements or patterns of the variant stream.
			</para>
				
			<para>
				Assume that an application predefined a variant stream named <literal>OrderStream</literal> to carry only <literal>ServiceOrder</literal> and <literal>ProductOrder</literal> events. An <literal>insert into</literal> clause inserts events into
				the variant stream:
			</para>
			<programlisting>insert into OrderStream select * from ServiceOrder</programlisting>			
			<programlisting>insert into OrderStream select * from ProductOrder</programlisting>			

			<para>
				Here is a sample statement that consumes the variant stream and outputs a total price per customer id for the last 30 seconds of <literal>ServiceOrder</literal> and <literal>ProductOrder</literal> events:
			</para>
			<programlisting>select customerId, sum(price) from OrderStream#time(30 sec) group by customerId</programlisting>			

			<para>
				If your application predefines the variant stream to hold specific type of events, as the sample above did, then all event properties that are common to all specified types are visible on the variant stream, including nested, indexed and mapped properties. For access to properties that are only available on one of the types, the dynamic property syntax must be used. In the example above, the <literal>customerId</literal> and <literal>price</literal> were properties common to both <literal>ServiceOrder</literal> and <literal>ProductOrder</literal> events.
			</para>		

			<para>
				For example, here is a consuming statement that selects a <literal>service duraction</literal> property that only <literal>ServiceOrder</literal> events have, and that must therefore be casted to double and null values removed in order to aggregate:
			</para>
			<programlisting>select customerId, sum(coalesce(cast(serviceDuraction?, double), 0)) 
from OrderStream#time(30 sec) group by customerId</programlisting>			

			<para>
				If your application predefines a variant stream to hold any type of events (the <literal>any</literal> type variance), then all event properties of the variant stream are effectively dynamic properties. 
			</para>		

			<para>
				For example, an application may define an <literal>OutgoingEvents</literal> variant stream to hold any type of event. The next statement is a sample consumer of the <literal>OutgoingEvents</literal> variant stream that looks for the <literal>destination</literal> property and fires for each event in which the property exists with a value of <literal>'email'</literal>:
			</para>		
			<programlisting>select * from OutgoingEvents(destination = 'email')</programlisting>			
		</sect2>

		<sect2 xml:id="insert-into-decorated">
			<title>Decorated Events</title>
			<indexterm><primary>decorated event</primary></indexterm>

			<para>
				Your <literal>select</literal> clause may use the '*' wildcard together with further expressions to populate a stream of events. A sample statement is:				
			</para>
			<programlisting>insert into OrderStream select *, price*units as linePrice from PurchaseOrder</programlisting>			

			<para>
				When using wildcard and selecting additional expression results, the engine produces what is called <emphasis>decorating</emphasis> events for the resulting stream. Decorating events add additional property values to an underlying event.
			</para>

			<para>
				In the above example the resulting OrderStream consists of underlying PurchaseOrder events <emphasis>decorated</emphasis> by a <literal>linePrice</literal> property that is a result of the <literal>price*units</literal> expression.
			</para>

			<para>
				In order to use <literal>insert into </literal> to insert into an existing stream of decorated events, your underlying event type must match, and all additional decorating property names and types of the <literal>select</literal> clause must also match.
			</para>
		</sect2>
		
		<sect2 xml:id="insert-into-eventasproperty">
			<title>Event as a Property</title>
			<indexterm><primary>event as a property</primary></indexterm>

			<para>
				Your <literal>select</literal> clause may use the stream name to populate a stream of events in which each event has properties that are itself an event. A sample statement is:				
			</para>
			<programlisting>insert into CompositeStream select order, service, order.price+service.price as totalPrice 
from PurchaseOrder#lastevent as order, ServiceEvent#lastevent as service</programlisting>			

			<para>
				When using the stream name (or tag in patterns) in the select-clause, the engine produces composite events: One or more of the properties of the composite event are events themselves.
			</para>

			<para>
				In the above example the resulting CompositeStream consists of 3 columns: the PurchaseOrder event, the ServiceEvent event and the <literal>totalPrice</literal> property that is a result of the <literal>order.price+service.price</literal> expression.
			</para>

			<para>
				In order to use <literal>insert into </literal> to insert into an existing stream of events in which properties are themselves events, each event column's event type must match, and all additional property names and types of the <literal>select</literal> clause must also match.
			</para>
		</sect2>
		
		<sect2 xml:id="insert-into-populate">
			<title>Instantiating and Populating an Underlying Event Object</title>

			<para>
				Your <literal>insert into</literal> clause may also directly instantiate and populate application underlying event objects or <literal>Map</literal> or <literal>Object[]</literal> event objects. This is described in greater detail in <xref linkend="eventrep-insertinto"/>.
			</para>
			
			<para>
			    If instead you have an expression that returns an event object, please read on to the next section.
			</para>
		</sect2>

		<sect2 xml:id="insert-into-expression">
			<title>Transposing an Expression Result</title>

			<para>
				You can transpose an object returned as an expression result into a stream using the <literal>transpose</literal> function as described further in <xref linkend="functionreference-transpose"/>.
			</para>			
		</sect2>

		<sect2 xml:id="insert-into-reconcile">
			<title>Select-Clause Expression and Inserted-Into Column Event Type</title>

			<para>
				When you declare the inserted-into event type in advance to the statement that inserts, the engine compares the inserted-into event type information to the return type of expressions in the select-clause.
				The comparison uses the column alias assigned to each select-clause expression using the <literal>as</literal> keyword.
			</para>			
			
			<para>
				When the inserted-into column type is an event type and when using a subquery or the <literal>new</literal> operator, 
				the engine compares column names assigned to subquery columns or <literal>new</literal> operator columns.
			</para>			

			<para>
			    For example, assume a <literal>PurchaseOrder</literal> event type that has a property called <literal>items</literal> that consists of <literal>Item</literal> rows:
			</para>
			<programlisting>create schema Item(name string, price double)</programlisting>			
			<programlisting>create schema PurchaseOrder(orderId string, items Item[])</programlisting>			
			
			<para>
				Declare a statement that inserts into the <literal>PurchaseOrder</literal> stream:
			</para>
			<programlisting>insert into PurchaseOrder 
select '001' as orderId, new {name='i1', price=10} as items
from TriggerEvent</programlisting>		
			
			<para>
				The alias assigned to the first and second expression in the select-clause, namely <literal>orderId</literal> and <literal>items</literal>,
				both match the event property names of the <literal>Purchase Order</literal> event type. The column names provided to the <literal>new</literal> operator 
				also both match the event property names of the <literal>Item</literal> event type.
			</para>
			
			<para>
				When the event type declares the column as a single value (and not an array) and when the select-clause expression produces a multiple rows, 
				the engine only populate the first row.
			</para>

			<para>
			    Consider a <literal>PurchaseOrder</literal> event type that has a property called <literal>item</literal> that consists of a single <literal>Item</literal> event:
			</para>
			<programlisting>create schema PurchaseOrder(orderId string, items Item)</programlisting>			
			
			<para>
				The sample subquery below populates only the very first event, discarding remaining subquery result events, since the <literal>items</literal> property above is declared as holding a single <literal>Item</literal>-typed event only (versus <literal>Item[]</literal> to hold multiple <literal>Item</literal>-typed events).
			</para>
			<programlisting>insert into PurchaseOrder select 
(select 'i1' as name, 10 as price from HistoryEvent#length(2)) as items 
from TriggerEvent</programlisting>		
			
			<para>
				Consider using a subquery with filter, or one of the enumeration methods to select a specific subquery result row.
			</para>
		</sect2>

		<sect2 xml:id="insert-into-typewithoutprops">
			<title>Insert Into for Event Types Without Properties</title>

			<para>
				When using insert-into and the type information for the inserted-into stream exists and the type has no properties, specify a <literal>select</literal>-clause that selects a single column of value <literal>null</literal> and that provides no column name.
			</para>			
			
			<para>
				For example, the next EPL declares a <literal>TriggerStream</literal> type that has no event properties:
			</para>
			<programlisting>create schema TriggerStream ()</programlisting>		
			
			<para>
				To populate events of type <literal>TriggerStream</literal>, let the <literal>select</literal>-clause simply select <literal>null</literal>, like this:
			</para>
			<programlisting>insert into TriggerStream select null from ... </programlisting>					

			<para>
				This example uses a pattern to populate a <literal>TrggerStream</literal> event every 10 seconds:
			</para>
			<programlisting>insert into TriggerStream select null from pattern[every timer:interval(10 sec)]</programlisting>					
		</sect2>
	</sect1>

    <sect1 xml:id="epl-subqueries">
        <title>Subqueries</title>

		<indexterm><primary>subqueries</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>subqueries</secondary></indexterm>

		<para>
			A subquery is a <literal>select</literal> within another statement. Esper supports subqueries in the <literal>select</literal> clause, <literal>where</literal> clause, <literal>having</literal> clause and in stream and pattern filter expressions. Subqueries provide an alternative way to perform operations that would otherwise require complex joins. Subqueries can also make statements more readable then complex joins.
	    </para>

		<para>
			Esper supports both simple subqueries as well as correlated subqueries. In a simple subquery, the inner query is not correlated to the outer query. Here is an example simple subquery within a <literal>select</literal> clause:
	    </para>
			
		<programlisting><![CDATA[select assetId, (select zone from ZoneClosed#lastevent) as lastClosed from RFIDEvent]]></programlisting>

		<para>
			If the inner query is dependent on the outer query, you will have a correlated subquery. An example of a correlated subquery is shown below. Notice the <literal>where</literal> clause in the inner query, where the condition involves a stream from the outer query:
	    </para>

		<programlisting><![CDATA[select * from RfidEvent as RFID where 'Dock 1' = 
  (select name from Zones#unique(zoneId) where zoneId = RFID.zoneId)]]></programlisting>
		
		<para>
			The example above shows a subquery in the <literal>where</literal> clause. The statement selects RFID events in which the zone name matches a string constant based on zone id. The statement sets <literal>#unique</literal> to guarantee that only the last event per zone id is retained for processing by the subquery.
		</para>

		<para>
			The next example is a correlated subquery within a <literal>select</literal> clause. In this statement the <literal>select</literal> clause retrieves the zone name by means of a subquery against the Zones set of events correlated by zone id:
		</para>

		<programlisting><![CDATA[select zoneId, (select name from Zones#unique(zoneId) 
  where zoneId = RFID.zoneId) as name from RFIDEvent]]></programlisting>

		<para>
			Note that when a simple or correlated subquery returns multiple rows, the engine returns a <literal>null</literal> value as the subquery result. To limit the number of events returned by a subquery consider using one of the <literal>#lastevent</literal>, <literal>#unique</literal> data windows or aggregation functions or the multi-row and multi-column-select as described below.
		</para>

		<para>
			The <literal>select</literal> clause of a subquery also allows wildcard selects, which return as an event property the underlying event object of the event type as defined in the <literal>from</literal> clause. An example:
		</para>

		<programlisting><![CDATA[select (select * from MarketData#lastevent) as md 
  from pattern [every timer:interval(10 sec)]]]></programlisting>

		<para>
			The output events to the statement above contain the underlying MarketData event in a property named "md". The statement populates the last MarketData event into a property named "md" every 10 seconds following the pattern definition, or populates a <literal>null</literal> value if no MarketData event has been encountered so far.
		</para>

		<para>
			Aggregation functions may be used in the <literal>select</literal> clause of the subselect as this example outlines:
		</para>
		<programlisting><![CDATA[select * from MarketData
where price > (select max(price) from MarketData(symbol='GOOG')#lastevent)]]></programlisting>

		<para>
			As the sub-select expression is evaluated first (by default), the query above actually never fires for the GOOG symbol, only for other symbols that have a price higher then the current maximum for GOOG. As a sidenote, the <literal>insert into</literal> clause can also be handy to compute aggregation results for use in multiple subqueries.
		</para>
		
		<para>
			When using aggregation functions in a correlated subselect the engine computes the aggregation based on data window (if provided), named window or table contents matching the where-clause.
		</para>
		
		<para>
			The following example compares the quantity value provided by the current order event against the total quantity of all order events in the last 1 hour for the same client.
		</para>
		<programlisting><![CDATA[select * from OrderEvent oe
where qty > 
  (select sum(qty) from OrderEvent#time(1 hour) pd 
  where pd.client = oe.client)]]></programlisting>

		<para>
			Filter expressions in a pattern or stream may also employ subqueries. Subqueries can be uncorrelated or can be correlated to properties of the stream or to properties of tagged events in a pattern. Subqueries may reference named windows and tables as well.
		</para>

		<para>
			The following example filters <literal>BarData</literal> events that have a close price less then the last moving average (field <literal>movAgv</literal>) as provided by stream <literal>SMA20Stream</literal> (an uncorrelated subquery):
		</para>
		<programlisting><![CDATA[select * from BarData(ticker='MSFT', closePrice < 
    (select movAgv from SMA20Stream(ticker='MSFT')#lastevent))]]></programlisting>

		<para>
			A few generic examples follow to demonstrate the point. The examples use short event and property names so they are easy to read. Assume <literal>A</literal> and <literal>B</literal> are streams and <literal>DNamedWindow</literal> is a named window, and <literal>ETable</literal> is a table and properties <literal>a_id, b_id, d_id, e_id, a_val, b_val, d_val, e_val</literal> respectively:
		</para>
		<programlisting><![CDATA[// Sample correlated subquery as part of stream filter criteria
select * from A(a_val in 
  (select b_val from B#unique(b_val) as b where a.a_id = b.b_id)) as a]]></programlisting>
		<programlisting><![CDATA[// Sample correlated subquery against a named window
select * from A(a_val in 
  (select d_val from DNamedWindow as d where a.a_id = d.d_id)) as a]]></programlisting>
		<programlisting><![CDATA[// Sample correlated subquery in the filter criteria as part of a pattern, querying a named window
select * from pattern [
  a=A -> b=B(bvalue = 
    (select d_val from DNamedWindow as d where d.d_id = b.b_id and d.d_id = a.a_id))
]]]></programlisting>
		<programlisting><![CDATA[// Sample correlated subquery against a table
select * from A(a_val in 
  (select e_val from ETable as e where a.a_id = e.e_id)) as a]]></programlisting>

		<para>
			Subquery state starts to accumulate as soon as a statement starts (and not only when a pattern-subexpression activates).
		</para>

		<para>
			The following restrictions apply to subqueries:
		</para>

		<orderedlist spacing="compact">
			<listitem>
				<para>
					Subqueries can only consist of a <literal>select</literal> clause, a <literal>from</literal> clause, a <literal>where</literal> clause, a <literal>group by</literal> clause and a <literal>having</literal> clause. Joins, outer-joins and output rate limiting are not permitted within subqueries.
				</para>
			</listitem>
			<listitem>
				<para>
					If using aggregation functions in a subquery, note these limitations:
				</para>
				<orderedlist spacing="compact">
					<listitem>
						<para>
							None of the properties of the correlated stream(s) can be used within aggregation functions.
						</para>
					</listitem>
					<listitem>
						<para>
							The properties of the subselect stream must all be within aggregation functions.
						</para>
					</listitem>
				</orderedlist>
			</listitem>
			<listitem>
				<para>
					With the exception of subqueries against named windows and tables and subqueries that are both uncorrelated and fully-aggregated, 
					the subquery stream definition must define a data window to limit subquery results, for the purpose of identifying the events held for subquery execution.
				</para>
			</listitem>
			<listitem>
				<para>
					The <literal>having</literal>-clause, if present, requires that properties of the selected stream are aggregated and does not allow un-aggregated properties of the selected stream.
					You may use the <literal>first</literal> aggregation function to obtain properties of the selected stream instead.
				</para>
			</listitem>
		</orderedlist>

		<para>
			The order of evaluation of subqueries relative to the containing statement is guaranteed: If the containing statement and its subqueries are reacting to the same type of event, the subquery will receive the
			event first before the containing statement's clauses are evaluated. This behavior can be changed via configuration. The order of evaluation of subqueries is not guaranteed between subqueries.
		</para>

		<para>
			Performance of your statement containing one or more subqueries principally depends on two parameters. First, if your subquery correlates one or more columns in the subquery stream with the enclosing statement's streams, the engine automatically builds the appropriate indexes for fast row retrieval based on the key values correlated (joined). The second parameter is the number of rows found in the subquery stream and the complexity of the filter criteria (<literal>where</literal> clause), as each row in the subquery stream must evaluate against the <literal>where</literal> clause filter.
		</para>

		<sect2 xml:id="epl-subqueries-exists">
			<title>The '<literal>Exists</literal>' Keyword</title>

			<para>
				The <literal>exists</literal> condition is considered "to be met" if the subquery returns at least one row. The <literal>not exists</literal> condition is considered true if the subquery returns no rows.
			</para>
			
			<para>
				The synopsis for the <literal>exists</literal> keyword is as follows:
			</para>
			<synopsis>exists (<emphasis>subquery</emphasis>)</synopsis>

			<para>
				Let's take a look at a simple example. The following is an EPL statement that uses the <literal>exists</literal> condition:
			</para>

			<programlisting><![CDATA[select assetId from RFIDEvent as RFID 
  where exists (select * from Asset#unique(assetId) where assetId = RFID.assetId)]]></programlisting>

			<para>
				This select statement will return all RFID events where there is at least one event in Assets unique by asset id with the same asset id.
			</para>
		</sect2>

		<sect2 xml:id="epl-subqueries-in">
			<title>The '<literal>In</literal>' and '<literal>Not In</literal>' Keywords</title>
			
			<para>
				The <literal>in</literal> subquery condition is true if the value of an expression matches one or more of the values returned by the subquery. Consequently, the <literal>not in</literal> condition is true if the value of an expression matches none of the values returned by the subquery.
			</para>
			
			<para>
				The synopsis for the <literal>in</literal> keyword is as follows:
			</para>
			<synopsis><emphasis>expression</emphasis> in (<emphasis>subquery</emphasis>)</synopsis>

			<para>
				The right-hand side subquery must return exactly one column.
			</para>

			<para>
				The next statement demonstrates the use of the <literal>in</literal> subquery condition: 
			</para>

			<programlisting><![CDATA[select assetId from RFIDEvent
  where zone in (select zone from ZoneUpdate(status = 'closed')#time(10 min))]]></programlisting>

			<para>
				The above statement demonstrated the <literal>in</literal> subquery to select RFID events for which the zone status is in a closed state. 
			</para>

			<para>
				Note that if the left-hand expression yields null, or if there are no equal right-hand values and at least one right-hand row yields null, the result of the <literal>in</literal> construct will be null, not false (or true for <literal>not-in</literal>). This is in accordance with SQL's normal rules for Boolean combinations of null values.
			</para>
		</sect2>

		<sect2 xml:id="epl-subqueries-anysome">
			<title>The '<literal>Any</literal>' and '<literal>Some</literal>' Keywords</title>
			
			<para>
				The <literal>any</literal> subquery condition is true if the expression returns true for one or more of the values returned by the subquery.
			</para>

			<para>
				The synopsis for the <literal>any</literal> keyword is as follows:
			</para>
			<synopsis><emphasis>expression</emphasis> <emphasis>operator</emphasis> any (<emphasis>subquery</emphasis>)
<emphasis>expression</emphasis> <emphasis>operator</emphasis> some (<emphasis>subquery</emphasis>)</synopsis>

			<para>
				The left-hand expression is evaluated and compared to each row of the subquery result using the given operator, which must yield a Boolean result. The result of <literal>any</literal> is "true" if any true result is obtained. The result is "false" if no true result is found (including the special case where the subquery returns no rows). 
			</para>

			<para>
				The <emphasis>operator</emphasis> can be any of the following values: <literal>=, !=, &lt;&gt;, &lt;, &lt;=, &gt;, &gt;=</literal>.
			</para>

			<para>
				The <literal>some</literal> keyword is a synonym for <literal>any</literal>. The <literal>in</literal> construct is equivalent to <literal>= any</literal>.
			</para>
		
			<para>
				The right-hand side subquery must return exactly one column.
			</para>

			<para>
				The next statement demonstrates the use of the <literal>any</literal> subquery condition: 
			</para>

			<programlisting><![CDATA[select * from ProductOrder as ord
  where quantity < any
    (select minimumQuantity from MinimumQuantity#keepall)]]></programlisting>

			<para>
				The above query compares ProductOrder event's quantity value with all rows from the MinimumQuantity stream of events and returns only those ProductOrder events that have a quantity that is less then any of the minimum quantity values of the MinimumQuantity events.
			</para>

			<para>
				Note that if there are no successes and at least one right-hand row yields null for the operator's result, the result of the <literal>any</literal> construct will be null, not false. This is in accordance with SQL's normal rules for Boolean combinations of null values. 
			</para>
		</sect2>

		<sect2 xml:id="epl-subqueries-all">
			<title>The '<literal>All</literal>' Keyword</title>
			
			<para>
				The <literal>all</literal> subquery condition is true if the expression returns true for all of the values returned by the subquery.
			</para>

			<para>
				The synopsis for the <literal>all</literal> keyword is as follows:
			</para>
			<synopsis><emphasis>expression</emphasis> <emphasis>operator</emphasis> all (<emphasis>subquery</emphasis>)</synopsis>

			<para>
				The left-hand expression is evaluated and compared to each row of the subquery result using the given operator, which must yield a Boolean result. The result of <literal>all</literal> is "true" if all rows yield true (including the special case where the subquery returns no rows). The result is "false" if any false result is found. The result is <literal>null</literal> if the comparison does not return false for any row, and it returns <literal>null</literal> for at least one row.
			</para>

			<para>
				The <emphasis>operator</emphasis> can be any of the following values: <literal>=, !=, &lt;&gt;, &lt;, &lt;=, &gt;, &gt;=</literal>.
			</para>

			<para>
				The <literal>not in</literal> construct is equivalent to <literal>!= all</literal>.
			</para>
		
			<para>
				The right-hand side subquery must return exactly one column.
			</para>

			<para>
				The next statement demonstrates the use of the <literal>all</literal> subquery condition: 
			</para>

			<programlisting><![CDATA[select * from ProductOrder as ord
  where quantity < all
    (select minimumQuantity from MinimumQuantity#keepall)]]></programlisting>

			<para>
				The above query compares ProductOrder event's quantity value with all rows from the MinimumQuantity stream of events and returns only those ProductOrder events that have a quantity that is less then all of the minimum quantity values of the MinimumQuantity events.
			</para>
		</sect2>

		<sect2 xml:id="epl-subqueries-groupby">
			<title>Subquery With <literal>Group By</literal> Clause</title>
			
			<para>
				The optional <literal>group by</literal> clause in subqueries works the same way as the group-by clause outside of subqueries,
				except that it impacts only those aggregations within the subquery.
			</para>
			  
			<para>
				The following restrictions apply:
			</para>

			<orderedlist spacing="compact">
				<listitem>
					<para>
						Expressions in the group-by clause cannot contain aggregate functions, subqueries or the <literal>prev</literal> and <literal>prior</literal> functions.
					</para>
				</listitem>
				<listitem>
					<para>
					    Subqueries only support the fully-aggregated case when using group-by: All non-aggregated properties in the select clause must be listed in the group by clause.
					</para>
				</listitem>
				<listitem>
					<para>
					    The group-by expressions cannot be correlated. All properties in the <literal>group by</literal> must be provided by the subselect stream.
					</para>
				</listitem>
			</orderedlist>
		</sect2>

		<sect2 xml:id="epl-subqueries-multicolumn">
			<title>Multi-Column Selection</title>

			<para>
				Your subquery may select multiple columns in the <literal>select</literal> clause including multiple aggregated values from a data window or named window or table.
			</para>
			
			<para>
				The following example is a correlated subquery that selects wildcard and in addition selects the <literal>bid</literal> and <literal>offer</literal> properties of the last <literal>MarketData</literal> event for the same symbol as the arriving <literal>OrderEvent</literal>:
			</para>

			<programlisting><![CDATA[select *,
  (select bid, offer from MarketData#unique(symbol) as md 
   where md.symbol = oe.symbol) as bidoffer
from OrderEvent oe]]></programlisting>

			<para>
				Output events for the above query contain all properties of the original <literal>OrderEvent</literal> event. In addition each output event contains a <literal>bidoffer</literal> nested property that itself contains the <literal>bid</literal> and <literal>offer</literal> properties. You may retrieve the bid and offer from output events directly via the <literal>bidoffer.bid</literal> property name syntax for nested properties.  
			</para>

			<para>
				The next example is similar to the above query but instead selects aggregations and selects from a named window by name <literal>OrderNamedWindow</literal> (creation not shown here). For each arriving <literal>OrderEvent</literal> it selects the total quantity and count of all order events for the same client, as currently held by the named window:
			</para>
			<programlisting><![CDATA[select *,
  (select sum(qty) as sumPrice, count(*) as countRows 
   from OrderNamedWindow as onw
   where onw.client = oe.client) as pastOrderTotals
from OrderEvent as oe]]></programlisting>

			<para>
				The next EPL statement computes a prorated quantity considering the maximum and minimum quantity for the last 1 minute of order events:
			</para>
			<programlisting><![CDATA[expression subq {
  (select max(quantity) as maxq, min(quantity) as minq from OrderEvent#time(1 min))
}
select (quantity - minq) / (subq().maxq  - subq().minq) as prorated
from OrderEvent]]></programlisting>

			<para>
				Output events for the above query contain all properties of the original <literal>OrderEvent</literal> event. In addition each output event contains a <literal>pastOrderTotals</literal> nested property that itself contains the <literal>sumPrice</literal> and <literal>countRows</literal> properties. 
			</para>
		</sect2>

		<sect2 xml:id="epl-subqueries-multirow">
			<title>Multi-Row Selection</title>

			<para>
				While a subquery cannot change the cardinality of the selected stream, a subquery can return multiple values from the selected data window or named window or table. This section shows examples of the <literal>window</literal> aggregation function as well as the use of enumeration methods with subselects. 
			</para>
			
			<para>
				Consider using an inner join, outer join or unidirectional join instead to achieve a 1-to-many cardinality in the number of output events.
			</para>

			<para>
				The next example is an uncorrelated subquery that selects all current <literal>ZoneEvent</literal> events considering the last <literal>ZoneEvent</literal> per zone for each arriving <literal>RFIDEvent</literal>.
			</para>
			<programlisting><![CDATA[select assetId,
 (select window(z.*) as winzones from ZoneEvent#unique(zone) as z) as zones
 from RFIDEvent]]></programlisting>

			<para>
				Output events for the above query contain two properties: the <literal>assetId</literal> property and the <literal>zones</literal> property. The latter property is a nested property that contains the <literal>winzones</literal> property. You may retrieve the zones from output events directly via the <literal>zones.winzones</literal> property name syntax for nested properties.  
			</para>

			<para>
				In this example for a correlated subquery against a named window, assume that the <literal>OrderNamedWindow</literal> has been created and contains order events. The query returns for each <literal>MarketData</literal> event
				the list of order ids for orders with the same symbol:
			</para>
			<programlisting><![CDATA[select price,
 (select window(orderId) as winorders 
  from OrderNamedWindow onw 
  where onw.symbol = md.symbol) as orderIds
 from MarketData md]]></programlisting>

			<para>
				Output events for the above query contain two properties: the <literal>price</literal> property and the <literal>orderIds</literal> property. The latter property is a nested property that contains the <literal>winorders</literal> property of type array. 
			</para>

			<para>
				Another option to reduce selected rows to a single value is through the use of enumeration methods.
			</para>
			
			<programlisting><![CDATA[select price,
 (select *  from OrderNamedWindow onw
  where onw.symbol = md.symbol).selectFrom(v => v) as ordersSymbol
 from MarketData md]]></programlisting>

			<para>
				Output events for the above query also contain a Collection of underlying events in the <literal>ordersSymbol</literal> property.
			</para>
		</sect2>

		<sect2 xml:id="epl-subqueries-hints">
			<title>Hints Related to Subqueries</title>

			<para>
				The following hints are available to tune performance and memory use of subqueries.
			</para>

			<para>
				Use the <literal>@Hint('set_noindex')</literal> hint for a statement that utilizes one or more subqueries. It instructs the engine to always perform a full scan. The engine does not build an implicit index or use an explicitly-created index when this hint is provided. Use of the hint may result in reduced memory use but poor statement performance.
			</para>

			<para>
				The following hints are available to tune performance and memory use of subqueries that select from named windows (does not apply to tables).
			</para>
			
			<para>
				Named windows are globally-visible data windows. As such an application may create explicit indexes as discussed in <xref linkend="named_explicit_index"/>. The engine may also elect to create implicit indexes (no create-index EPL required) for index-based lookup of rows when executing <literal>on-select</literal>, <literal>on-merge</literal>, <literal>on-update</literal> and <literal>on-delete</literal> statements and for statements that subquery a named window.
			</para>
			
			<para>
				By default and without specifying a hint, each statement that subqueries a named window also maintains its own index for looking up events held by the named window. The engine maintains the index by consuming the named window insert and remove stream. When the statement is destroyed it releases that index.
			</para>

			<para>
				Specify the <literal>@Hint('enable_window_subquery_indexshare')</literal> hint to enable subquery index sharing for named windows. When using this hint, indexes for subqueries are maintained by the named window itself (and not each statement context partition), are shared between one or more statements and may also utilize explicit indexes. Specify the hint once as part of the <literal>create window</literal> statement.
			</para>

			<para>
				This sample EPL statement creates a named window with subquery index sharing enabled:
			</para>
			<programlisting><![CDATA[@Hint('enable_window_subquery_indexshare')
create window OrdersNamedWindow#keepall as OrderMapEventType]]></programlisting>

			<para>
				When subquery index sharing is enabled, performance may increase as named window stream consumption is no longer needed for correlated subqueries. You may also expect reduced memory use especially if a large number of EPL statements perform similar subqueries against a named window. Subquery index sharing may require additional short-lived object creation and may slightly increase lock held time for named windows. 
			</para>

			<para>
				The following statement performs a correlated subquery against the named window above. When a settlement event arrives it select the order detail for the same order id as provided by the settlement event:
			</para>
			<programlisting><![CDATA[select 
  (select * from OrdersNamedWindow as onw 
    where onw.orderId = se.orderId) as orderDetail
  from SettlementEvent as se]]></programlisting>
  
			  <para>
				  With subquery index sharing enabled the engine maintains an index of order events by order id for the named window, and shares that index between additional statements until the time all utilizing statements are destroyed.
			  </para>
			
			<para>
				You may disable subquery index sharing for a specific statement by specifying the <literal>@Hint('disable_window_subquery_indexshare')</literal> hint, as this example shows, causing the statement to maintain its own index:
			</para>
			<programlisting><![CDATA[@Hint('disable_window_subquery_indexshare')
select 
  (select * from OrdersNamedWindow as onw 
    where onw.orderId = se.orderId) as orderDetail
  from SettlementEvent as se]]></programlisting>
			
		</sect2>
    </sect1>


    <sect1 xml:id="epl-join">
        <title>Joining Event Streams</title>
		<indexterm><primary>join</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>join</secondary></indexterm>
		
		<sect2 xml:id="epl-join-introducing">
			<title>Introducing Joins</title>

			<para>
				Two or more event streams can be part of the <literal>from</literal>-clause and thus both (all) streams determine the resulting events. This section summarizes the important concepts. The sections that follow present more detail on each topic.
			</para>
			
			<para>
				The default join is an inner join which produces output events only when there is at least one match in all streams.
			</para>

			<para>
				Consider the sample statement shown next:
			</para>
			<programlisting><![CDATA[select * from TickEvent#lastevent, NewsEvent#lastevent]]></programlisting>

			<para>
				The above statement outputs the last TickEvent and the last NewsEvent in one output event when either a TickEvent or a NewsEvent arrives. If no TickEvent was received before a NewsEvent arrives, no output occurs. Similarly when no NewsEvent was received before a TickEvent arrives, no output occurs.
			</para>

			<para>
				The <literal>where</literal>-clause lists the join conditions that Esper uses to relate events in the two or more streams. 
			</para>

			<para>
				The next example statement retains the last TickEvent and last NewsEvent per symbol, and joins the two streams based on their symbol value:
			</para>
			<programlisting><![CDATA[select * from TickEvent#unique(symbol) as t, NewsEvent#unique(symbol) as n
where t.symbol = n.symbol]]></programlisting>

			<para>
				As before, when a TickEvent arrives for a symbol that has no matching NewsEvent then there is no output event.
			</para>

			<para>
				An outer join does not require each event in either stream to have a matching event. The full outer join is useful when output is desired when no match is found. The different outer join types (full, left, right) are explained in more detail below.
			</para>

			<para>
				This example statement is an outer-join and also returns the last TickEvent and last NewsEvent per symbol: 
			</para>
			<programlisting><![CDATA[select * from TickEvent#unique(symbol) as t
full outer join NewsEvent#unique(symbol) as n on t.symbol = n.symbol]]></programlisting>

			<para>
				In the sample statement above, when a TickEvent arrives for a symbol that has no matching NewsEvent, or when a NewsEvent arrives for a symbol that has no matching TickEvent, the statement still produces an output event with a null column value for the missing event.
			</para>

			<para>
				Note that each of the sample queries above defines a data window. The sample queries above use the last-event data window (#lastevent) or the unique data window (#unique). A data window serves to indicate the subset of events to join from each stream and may be required depending on the join.
			</para>

			<para>
				In above queries, when either a TickEvent arrives or when a NewsEvent arrives then the query evaluates and there is output. The same holds true if additional streams are added to the <literal>from</literal>-clause: Each of the streams in the <literal>from</literal>-clause trigger the join to evaluate.
			</para>
			
			<para>
				The <literal>unidirectional</literal> keyword instructs the engine to evaluate the join only when an event arrives from the single stream that was marked with the <literal>unidirectional</literal> keyword. In this case no data window should be specified for the stream marked as <literal>unidirectional</literal> since the keyword implies that the current event of that stream triggers the join.
			</para>
			
			<para>
				Here is the sample statement above with <literal>unidirectional</literal> keyword, so that output occurs only when a TickEvent arrives and not when a NewsEvent arrives:
			</para>
			<programlisting><![CDATA[select * from TickEvent as t unidirectional, NewsEvent#unique(symbol) as n 
where t.symbol = n.symbol]]></programlisting>
			
			<para>
				It is oftentimes the case that an aggregation (count, sum, average) only needs to be calculated in the context of an arriving event or timer. Consider using the <literal>unidirectional</literal> keyword when aggregating over joined streams.
			</para>

			<para>
				An EPL pattern is a normal citizen also providing a stream of data consisting of pattern matches. A time pattern, for example, can be useful to evaluate a join and produce output upon each interval.
			</para>
			
			<para>
				This sample statement includes a pattern that fires every 5 seconds and thus triggers the join to evaluate and produce output, computing an aggregated total quantity per symbol every 5 seconds:
			</para>
			<programlisting><![CDATA[select symbol, sum(qty) from pattern[every timer:interval(5 sec)] unidirectional, 
  TickEvent#unique(symbol) t, NewsEvent#unique(symbol) as n 
where t.symbol = n.symbol group by symbol]]></programlisting>

			<para>
				Named windows as well as reference and historical data such as stored in your relational database, and data returned by a method/script/UDF invocation, can also be included in joins as discussed in <xref linkend="histdata_overview"/> and <xref linkend="joining_method"/>.
			</para>
			
			<para>
				Related to joins are subqueries: A subquery is a <literal>select</literal> within another statement, see <xref linkend="epl-subqueries"/>
			</para>
			
			<para>
				The engine performs extensive query analysis and planning, building internal indexes and strategies as required to allow fast evaluation of many types of queries.
			</para>
		</sect2>

		<sect2 xml:id="epl-join-inner">
			<title>Inner (Default) Joins</title>			
	
			<para>
				Each point in time that an event arrives to one of the event streams, the two event streams are joined and output events are produced according to the <literal>where</literal> clause when matching events are found for all joined streams.
			</para>
	
			<para>
				This example joins 2 event streams. The first event stream consists of fraud warning events for which it keep the last 30 minutes. The second stream is withdrawal events for which it considers the last 30 seconds. The streams are joined on account number.
			</para>
<programlisting><![CDATA[select fraud.accountNumber as accntNum, fraud.warning as warn, withdraw.amount as amount,
       max(fraud.timestamp, withdraw.timestamp) as timestamp, 'withdrawlFraud' as desc
  from com.espertech.esper.example.atm.FraudWarningEvent#time(30 min) as fraud,
       com.espertech.esper.example.atm.WithdrawalEvent#time(30 sec) as withdraw
 where fraud.accountNumber = withdraw.accountNumber]]></programlisting>

			<para>
				Joins can also include one or more pattern statements as the next example shows:
			</para>
<programlisting><![CDATA[select * from FraudWarningEvent#time(30 min) as fraud,
    pattern [every w=WithdrawalEvent -> PINChangeEvent(acct=w.acct)]#lastevent as withdraw
 where fraud.accountNumber = withdraw.w.accountNumber]]></programlisting>
	
			<para>
				The statement above joins the last 30 minutes of fraud warnings with a pattern. The pattern consists of every withdrawal event that is followed by a PIN change event for the same account number. It joins the two event streams on account number. The last-event window instucts the join to only consider the last pattern match.
			</para>
	
			<para>
				In a join and outer join, your statement must declare a data window onto each stream. Streams that are marked as unidirectional and named windows and tables as well as database or methods in a join are an exception and do not require a data window. If you are joining an event to itself via contained-event selection, data windows also do not need to be specified. The reason that a data window must be declared is that a data window specifies which events are considered for the join (i.e. last event, last 10 events, all events, last 1 second of events etc.).
			</para>
	
			<para>
				The next example joins all FraudWarningEvent events that arrived since the statement was started, with the last 20 seconds of PINChangeEvent events:
			</para>
<programlisting><![CDATA[select * from FraudWarningEvent#keepall as fraud, PINChangeEvent#time(20 sec) as pin
 where fraud.accountNumber = pin.accountNumber]]></programlisting>

			<para>
				The above example employed the special keep-all window that retains all events.
			</para>			
		</sect2>

		<sect2 xml:id="epl-outerjoin">
			<title>Outer, Left and Right Joins</title>
	
			<indexterm><primary>outer join</primary></indexterm>
			<indexterm><primary>inner join</primary></indexterm>
			<indexterm><primary>EPL</primary><secondary>outer join</secondary></indexterm>
			<indexterm><primary>EPL</primary><secondary>inner join</secondary></indexterm>
	
			<para>
				Esper supports left outer joins, right outer joins, full outer joins and inner joins in any combination between an unlimited number of event streams. Outer and inner joins can also join reference and historical data as explained in <xref linkend="histdata_overview"/>, as well as join data returned by a method, script or UDF invocation as outlined in <xref linkend="joining_method"/>.
			</para>
	
			<para>
				The keywords <literal>left, right, full</literal> and <literal>inner</literal> control the type of the join between two streams. The optional <literal>on</literal> clause specifies one or more properties that join each stream. The synopsis is as follows:
			</para>

<synopsis>...from <emphasis>stream_def</emphasis> [as name] 
  ((left|right|full outer) | inner) join <emphasis>stream_def</emphasis> 
  [on <emphasis>property</emphasis> = <emphasis>property</emphasis> [and <emphasis>property</emphasis> = <emphasis>property</emphasis> ...] ]
  [ ((left|right|full outer) | inner) join <emphasis>stream_def</emphasis> [on ...]]...
</synopsis>

			<para>
				If the outer join is a left outer join, there will be at least one output event for each event of the stream on the left-hand side of the clause. For example, in the left outer join shown below you get output for each event in the stream RfidEvent, even if the event does not match any event in the event stream OrderList.
			</para>

<programlisting><![CDATA[select * from RfidEvent#time(30 sec) as rfid
       left outer join
       OrderList#length(10000) as orderlist
     on rfid.itemId = orderList.itemId]]></programlisting>
		
			<para>
				Similarly, if the join is a Right Outer Join, then there will be at least one output event for each event of the stream on the right-hand side of the clause. For example, in the right outer join shown below you get output for each event in the stream OrderList, even if the event does not match any event in the event stream RfidEvent.
			</para>

<programlisting><![CDATA[select * from RfidEvent#time(30 sec) as rfid
       right outer join
       OrderList#length(10000) as orderlist
       on rfid.itemId = orderList.itemId]]></programlisting>

			<para>
				For all types of outer joins, if the join condition is not met, the select list is computed with the event properties of the arrived event while all other event properties are considered to be null.
			</para>
			
			<para>
				The next type of outer join is a full outer join. In a full outer join, each point in time that an event arrives to one of the event streams, one or more output events are produced. In the example below, when either an RfidEvent or an OrderList event arrive, one or more output event is produced. The next example shows a full outer join that joins on multiple properties:
			</para>

<programlisting><![CDATA[select * from RfidEvent#time(30 sec) as rfid
       full outer join
       OrderList#length(10000) as orderlist
       on rfid.itemId = orderList.itemId and rfid.assetId = orderList.assetId]]></programlisting>

			<para>
				The last type of join is an inner join. In an inner join, the engine produces at least one output event for each event of the stream on the left-hand side that matches at least one event on the right hand side considering the join properties. For example, in the inner join shown below you get output for each event in the RfidEvent stream that matches one or more events in the OrderList data window:
			</para>

<programlisting><![CDATA[select * from RfidEvent#time(30 sec) as rfid
       inner join
       OrderList#length(10000) as orderlist
       on rfid.itemId = orderList.itemId and rfid.assetId = orderList.assetId]]></programlisting>

			<para>
				Patterns as streams in a join follow this rule: If your query does not specify a data window for the pattern then the pattern stream retains the last match. Thus a pattern must have matched at least once for the last match to become available in a join. Multiple rows from a pattern stream may be retained by declaring a data window onto a pattern using the <literal>pattern [...]#</literal><emphasis>window_spec</emphasis> syntax.
			</para>
	
			<para>
				This example outer joins multiple streams. Here the RfidEvent stream is outer joined to both ProductName and LocationDescription via left outer join:
			</para>

<programlisting><![CDATA[select * from RfidEvent#time(30 sec) as rfid
      left outer join ProductName#keepall as refprod
        on rfid.productId = refprod.prodId
      left outer join LocationDescription#keepall as refdesc
        on rfid.location = refdesc.locId]]></programlisting>
        
			<para>
			  If the optional <literal>on</literal> clause is specified, it may only employ the <literal>=</literal> equals operator and property names. Any other operators must be placed in the <literal>where</literal>-clause.
			  The stream names that appear in the <literal>on</literal> clause may refer to any stream in the <literal>from</literal>-clause.
			</para>
			
			<para>
			  Your EPL may also provide no <literal>on</literal> clause. This is useful when the streams that are joined do not provide any properties to join on, for example when joining with a time-based pattern. 
			</para>

			<para>
				The next example employs a unidirectional left outer join such that the engine, every 10 seconds, outputs a count of the number of RfidEvent events in the 60-second time window.
			</para>
<programlisting><![CDATA[select count(*) from
  pattern[every timer:interval(1)] unidirectional 
  left outer join
  RfidEvent#time(60 sec)]]></programlisting>			
			
		</sect2>

		<sect2 xml:id="epl-unidirectional">
			<title>Unidirectional Joins</title>
	
			<indexterm><primary>unidirectional joins</primary></indexterm>
			<indexterm><primary>EPL</primary><secondary>join, unidirectional</secondary></indexterm>
			<indexterm><primary>EPL</primary><secondary>outer join, unidirectional</secondary></indexterm>
	
			<para>
				In a join or outer join your statement lists multiple event streams, data windows and/or patterns in the <literal>from</literal> clause. As events arrive into the engine, each of the streams (data windows, patterns) provides insert and remove stream events. The engine evaluates each insert and remove stream event provided by each stream, and joins or outer joins each event against data window contents of each stream, and thus generates insert and remove stream join results.
			</para>
				
			<para>
				The direction of the join execution depends on which stream or streams are currently providing an insert or remove stream event for executing the join. 
				A join is thus multidirectional, or bidirectional when only two streams are joined.
				A join can be made unidirectional if your application does not want new results when events arrive on a given stream or streams.
			</para>
	
			<para>
				The <literal>unidirectional</literal> keyword can be used in the <literal>from</literal> clause to identify streams that provide the events to execute the join. If the keyword is present for a stream, all other streams in the <literal>from</literal> clause become passive streams. When events arrive or leave a data window of a passive stream then the join does not generate join results. 
			</para>
	
			<para>
				For example, consider a use case that requires us to join stock tick events (TickEvent) and news events (NewsEvent). The <literal>unidirectional</literal> keyword allows to generate results only when TickEvent events arrive, and not when NewsEvent arrive or leave the 10-second time window:
			</para>
			<programlisting><![CDATA[select * from TickEvent unidirectional, NewsEvent#time(10 sec) 
where tick.symbol = news.symbol]]></programlisting>

			<para>
				Aggregation functions in a <literal>unidirectional</literal> join aggregate within the context of each unidirectional event evaluation and are not cumulative. Thereby aggregation functions when used with <literal>unidirectional</literal> may evaluate faster as they do not need to consider a remove stream (data removed from data windows or named windows).
			</para>
			
			<para>
				The count function in the next query returns, for each TickEvent, the number of matching NewEvent events:
			</para>
			<programlisting><![CDATA[select count(*) from TickEvent unidirectional, NewsEvent#time(10 sec) 
where tick.symbol = news.symbol]]></programlisting>

			<para>
				The following restrictions apply to unidirectional joins:
			</para>
	
			<orderedlist spacing="compact">
				<listitem>
					<para>
						The <literal>unidirectional</literal> keyword can only be specified for a single stream in the <literal>from</literal> clause, unless all streams are in a full outer join and all streams declare <literal>unidirectional</literal>.
					</para>
				</listitem>
				<listitem>
					<para>
						Receiving data from a unidirectional join via the pull API (<literal>iterator</literal> method) is not allowed. This is because the engine holds no state for the single stream that provides the events to execute the join.
					</para>
				</listitem>
				<listitem>
					<para>
						The stream that declares the <literal>unidirectional</literal> keyword cannot declare a data window for that stream, since remove stream events are not processed for the single stream.
					</para>
				</listitem>
			</orderedlist>
		</sect2>

		<sect2 xml:id="epl-unidirectional-fullouterjoin">
			<title>Unidirectional Full Outer Joins</title>
	
			<indexterm><primary>unidirectional full outer joins</primary></indexterm>
			<indexterm><primary>EPL</primary><secondary>outer join, unidirectional</secondary></indexterm>
	
			<para>
				In a full outer join all streams can be marked as <literal>unidirectional</literal>. 
				This is useful for declaring multiple triggering events and for performing a union or merge of streams.
			</para>
	
			<para>
				When marking more than one stream as unidirectional, all streams must be unidirectional and inner, left and right joins are not allowed. 
				This is because unidirectional streams have an undefined depth and cannot be looked-up against.
			</para>

			<para>
				For example, consider a use case where output should occur when either a tick event or a news event arrives:
			</para>

			<programlisting><![CDATA[select * from TickEvent as te unidirectional, 
  full outer join
  NewsEvent as ne unidirectional]]></programlisting>

			<para>
				Place filter criteria for a given stream into parenthesis, for example:
			</para>

			<programlisting><![CDATA[select * from TickEvent(symbol='IBM') unidirectional, 
  full outer join
  TradeEvent(symbol='IBM') unidirectional
  full outer join
  SettlementEvent(symbol='IBM') unidirectional
  where coalesce(TickEvent.price,TradeEvent.price) > 100 // place common critera into a where-clause that may use coalesce]]></programlisting>
		</sect2>

		<sect2 xml:id="epl-join-hints">
			<title>Hints Related to Joins</title>			
			<para>
				When joining 3 or more streams (including any relational or non-relational sources as below) it can sometimes help to provide the query planner instructions how to best execute the join. The engine compiles a query plan for the EPL statement at statement creation time. You can output the query plan to logging (see configuration).
			</para>
			
			<para>
				An outer join that specifies only <literal>inner</literal> keywords for all streams is equivalent to an default (inner) join. The following two statements are equivalent:
			</para>
			<programlisting><![CDATA[select * from TickEvent#lastevent, 
    NewsEvent#lastevent where tick.symbol = news.symbol]]></programlisting>

			<para>
				Equivalent to:
			</para>
			<programlisting><![CDATA[select * from TickEvent#lastevent 
	inner join NewsEvent#lastevent on tick.symbol = news.symbol]]></programlisting>
			
			<para>
				For all types of joins, the query planner determines a query graph: The term is used here for all the information regarding what properties or expressions are used to join the streams. The query graph thus includes the where-clause expressions as well as outer-join on-clauses if this statement is an outer join. The query planner also computes a dependency graph which includes information about all historical data streams (relational and non-relational as below) and their input needs. 
			</para>
			
			<para>
				For default (inner) joins the query planner first attempts to find a path of execution as a nested iteration. For each stream the query planner selects the best order of streams available for the nested iteration considering the query graph and dependency graph. If the full depth of the join is achievable via nested iteration for all streams without full table scan then the query planner uses that nested iteration plan. If not, then the query planner re-plans considering a merge join (Cartesian) approach instead.
			</para>
			
			<para>
				Specify the @Hint('prefer_merge_join') to instruct the query planner to prefer a merge join plan instead of a nested iteration plan. Specify the @Hint('force_nested_iter') to instruct the query planner to always use a nested iteration plan. 
			</para>

			<para>
				For example, consider the below statement. Depending on the number of matching rows in OrderBookOne and OrderBookTwo (named windows in this example, and assumed to be defined elsewhere) the performance of the join may be better using the merge join plan.
			</para>
			<programlisting><![CDATA[@Hint('prefer_merge_join') 
select * from TickEvent#lastevent t, 
	OrderBookOne ob1, OrderBookOne ob2
where ob1.symbol = t.symbol and ob2.symbol = t.symbol 
and ob1.price between t.buy and t.sell and ob2.price between t.buy and t.sell]]></programlisting>

			<para>
				For outer joins the query planner considers nested iteration and merge join (Cartesian) equally and above hints don't apply.
			</para>
		</sect2>
			
    </sect1>

	<sect1 xml:id="histdata_overview" revision="1">
		<title>Accessing Relational Data via SQL</title>
		
		<indexterm><primary>SQL</primary></indexterm>
		<indexterm><primary>join</primary><secondary>relational data via SQL</secondary></indexterm>
		<indexterm><primary>relational databases</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>joining relational data via SQL</secondary></indexterm>

		<para>
			For NEsper .NET also see <xref linkend="appendix_dotnet_histdata_overview"/>.
		</para>

		<para>
			This chapter outlines how reference data and historical data that are stored in a relational database can be queried via SQL within EPL statements. 
		</para>
			
		<para>
			Esper can access via join and outer join as well as via iterator (poll) API all types of event streams to stored data. In order for such data sources to become accessible to Esper, some configuration is required. The <xref linkend="config-database-ref"/> explains the required configuration for database access in greater detail, and includes information on configuring a query result cache.
		</para>
		
		<para>
			Esper does not parse or otherwise inspect your SQL query. Therefore your SQL can make use of any database-specific SQL language extensions or features that your database provides.
		</para>

		<para>
			If you have enabled query result caching in your Esper database configuration, Esper retains SQL query results in cache following the configured cache eviction policy. 
		</para>
			
		<para>
			Also if you have enabled query result caching in your Esper database configuration and provide EPL <literal>where</literal> clause and/or <literal>on</literal> clause (outer join) expressions, then 
			Esper builds indexes on the SQL query results to enable fast lookup. This is especially useful if your queries return a large number of rows. For building the proper indexes, Esper inspects the expression found in your EPL query <literal>where</literal> clause, if present. For outer joins, Esper also inspects your EPL query <literal>on</literal> clause. Esper analyzes the EPL <literal>on</literal> clause and <literal>where</literal> clause expressions, if present, looking for property comparison with or without logical AND-relationships between properties. When a SQL query returns rows for caching, Esper builds and caches the appropriate index and lookup strategies for fast row matching against indexes.
		</para>

		<para>
			Joins or outer joins in which only SQL statements or method, script and UDF invocations are listed in the <literal>from</literal> clause and no other event streams are termed <emphasis>passive</emphasis> joins. A passive join does not produce an insert or remove stream and therefore does not invoke statement listeners with results. A passive join can be iterated on (polled) using a statement's <literal>safeIterator</literal> and <literal>iterator</literal> methods.
		</para>

		<para>
			There are no restrictions to the number of SQL statements or types of streams joined. The following restrictions currently apply:
		</para>

		<itemizedlist spacing="compact">
			<listitem>
				<para>
					An SQL query cannot declare data windows; That is, you cannot create a time or length window on an SQL query. Instead, use <literal>insert into</literal> to make join results available for further processing.
				</para>
			</listitem>
			<listitem>
				<para>
					Your database software must support JDBC prepared statements that provide statement meta data at compilation time. Most major databases provide this function. A workaround is available for databases that do not provide this function.
				</para>
			</listitem>
			<listitem>
				<para>
					JDBC drivers must support the getMetadata feature. A workaround is available as below for JDBC drivers that don't support getting metadata.
				</para>
			</listitem>
		</itemizedlist>
		
		<para>
			The next sections assume basic knowledge of SQL (Structured Query Language).
		</para>

		<sect2 xml:id="histdata_join" revision="1">
			<title>Joining SQL Query Results</title>
			
			<para>
				To join an event stream against stored data, specify the <literal>sql</literal> keyword followed by the name of the database and a parameterized SQL query. The syntax to use in the <literal>from</literal> clause of an EPL statement is:
			</para>
	
			<synopsis>sql:<emphasis>database_name</emphasis> [" <emphasis>parameterized_sql_query</emphasis> "]</synopsis>
	
			<para>
				The engine uses the <emphasis>database_name</emphasis> identifier to obtain configuration information in order to establish a database connection, as well as settings that control connection creation and removal. Please see <xref linkend="config-database-ref"/> to configure an engine for database access.
			</para>
				
			<para>
				Following the database name is the SQL query to execute. The SQL query can contain one or more substitution parameters. The SQL query string is placed in single brackets <literal>[</literal> and <literal>]</literal>. The SQL query can be placed in either single quotes (') or double quotes ("). The SQL query grammer is passed to your database software unchanged, allowing you to write any SQL query syntax that your database understands, including stored procedure calls.
			</para>

			<para>
				Substitution parameters in the SQL query string take the form <literal>${</literal><emphasis>expression</emphasis><literal>}</literal>. The engine resolves <emphasis>expression</emphasis> at statement execution time to the actual expression result by evaluating the events in the joined event stream or current variable values, if any event property references or variables occur in the expression. An <emphasis>expression</emphasis> may not contain EPL substitution parameters.
			</para>

			<para>
				The engine determines the type of the SQL query output columns by means of the result set metadata that your database software returns for the statement. The actual 
				query results are obtained via the <literal>getObject</literal> on <literal>java.sql.ResultSet</literal>.
			</para>

			<para>
				The sample EPL statement below joins an event stream consisting of <literal>CustomerCallEvent</literal> events with the results of an SQL query against the database named <literal>MyCustomerDB</literal> and table <literal>Customer</literal>: 
			</para>
			
			<programlisting><![CDATA[select custId, cust_name from CustomerCallEvent,
  sql:MyCustomerDB [' select cust_name from Customer where cust_id = ${custId} ']]]></programlisting>

			<para>
				The example above assumes that <literal>CustomerCallEvent</literal> supplies an event property named <literal>custId</literal>. The SQL query selects the customer name from the Customer table. The <literal>where</literal> clause in the SQL matches the Customer table column <literal>cust_id</literal> with the value of <literal>custId</literal> in each <literal>CustomerCallEvent</literal> event. The engine executes the SQL query for each new <literal>CustomerCallEvent</literal> encountered.
			</para>

			<para>
				If the SQL query returns no rows for a given customer id, the engine generates no output event. Else the engine generates one output event for each row returned by the SQL query. An outer join as described in the next section can be used to control whether the engine should generate output events even when the SQL query returns no rows.
			</para>

			<para>
				The next example adds a time window of 30 seconds to the event stream <literal>CustomerCallEvent</literal>. It also renames the selected properties to customerName and customerId to demonstrate how the naming of columns in an SQL query can be used in the <literal>select</literal> clause in the EPL query. And the example uses explicit stream names via the <literal>as</literal> keyword.
			</para>

			<programlisting><![CDATA[select customerId, customerName from
  CustomerCallEvent#time(30 sec) as cce,
  sql:MyCustomerDB ["select cust_id as customerId, cust_name as customerName from Customer 
                  where cust_id = ${cce.custId}"] as cq]]></programlisting>

			<para>
				Any window, such as the time window, generates insert stream (istream) events as events enter the window, and remove stream (rstream) events as events leave the window. The engine executes the given SQL query for each <literal>CustomerCallEvent</literal> in both the insert stream and the remove stream. As a performance optimization, the <literal>istream</literal> or <literal>rstream</literal> keywords in the <literal>select</literal> clause can be used to instruct the engine to only join insert stream or remove stream events, reducing the number of SQL query executions.
			</para>
			
			<para>
				Since any expression may be placed within the <literal>${...}</literal> syntax, you may use variables or user-defined functions as well.
			</para>

			<para>
				The next example assumes that a variable by name <literal>varLowerLimit</literal> is defined and that a user-defined function <literal>getLimit</literal> exists on the <literal>MyLib</literal> imported class that takes a <literal>LimitEvent</literal> as a parameter:
			</para>
			<programlisting><![CDATA[select * from LimitEvent le, 
  sql:MyCustomerDB [' select cust_name from Customer where 
      amount > ${max(varLowerLimit, MyLib.getLimit(le))} ']]]></programlisting>

			<para>
				The example above takes the higher of the current variable value or the value returned by the user-defined function to return only those customer names where the amount exceeds the computed limit.
			</para>
		</sect2>

		<sect2 xml:id="histdata_join_where" revision="1">
			<title>SQL Query and the EPL <literal>Where</literal> Clause</title>
			
			<para>
				Consider using the EPL <literal>where</literal> clause to join the SQL query result to your event stream. Similar to EPL joins and outer-joins that join event streams or patterns, the EPL <literal>where</literal> clause provides join criteria between the SQL query results and the event stream (as a side note, an SQL <literal>where</literal> clause is a filter of rows executed by your database on your database server before returning SQL query results).
			</para>

			<para>
				Esper analyzes the expression in the EPL <literal>where</literal> clause and outer-join <literal>on</literal> clause, if present, and builds the appropriate indexes from that information at runtime, to ensure fast matching of event stream events to SQL query results, even if your SQL query returns a large number of rows. Your applications must ensure to configure a cache for your database using Esper configuration, as such indexes are held with regular data in a cache. If you application does not enable caching of SQL query results, the engine does not build indexes on cached data.
			</para>

			<para>
				The sample EPL statement below joins an event stream consisting of <literal>OrderEvent</literal> events with the results of an SQL query against the database named <literal>MyRefDB</literal> and table <literal>SymbolReference</literal>: 
			</para>
			
			<programlisting><![CDATA[select symbol, symbolDesc from OrderEvent as orders,
  sql:MyRefDB ['select symbolDesc from SymbolReference'] as reference
  where reference.symbol = orders.symbol]]></programlisting>

			<para>
				Notice how the EPL <literal>where</literal> clause joins the <literal>OrderEvent</literal> stream to the <literal>SymbolReference</literal> table. In this example, the SQL query itself does not have a SQL <literal>where</literal> clause 
				and therefore returns all rows from table <literal>SymbolReference</literal>.
			</para>

			<para>
				If your application enables caching, the SQL query fires only at the arrival of the first <literal>OrderEvent</literal> event. When the second <literal>OrderEvent</literal> arrives, the join execution uses the cached query result. If the caching policy that you specified in the Esper database configuration evicts the SQL query result from cache, then the engine fires the SQL query again to obtain a new result and places the result in cache.
			</para>
			
			<para>
				If SQL result caching is enabled and your EPL <literal>where</literal> clause, as show in the above example, provides the properties to join, then the engine indexes the SQL query results in cache and retains the index together with the query result in cache. Thus your application can benefit from high performance index-based lookups as long as the SQL query results are found in cache. 
			</para>

			<para>
				The SQL result caches operate on the level of all result rows for a given parameter set. For example, if your query returns 10 rows for a certain set of parameter values then the cache treats all 10 rows as a single entry keyed by the parameter values, and the expiry policy applies to all 10 rows and not to each individual row.
			</para>

			<para>
				It is also possible to join multiple autonomous database systems in a single query, for example:
			</para>

			<programlisting><![CDATA[select symbol, symbolDesc from OrderEvent as orders,
  sql:My_Oracle_DB ['select symbolDesc from SymbolReference'] as reference,
  sql:My_MySQL_DB ['select orderList from orderHistory'] as history
  where reference.symbol = orders.symbol
  and history.symbol = orders.symbol ]]></programlisting>
		</sect2>

		<sect2 xml:id="histdata_outerjoin" revision="1">
			<title>Outer Joins With SQL Queries</title>

			<para>
				You can use outer joins to join data obtained from an SQL query and control when an event is produced. Use a left outer join, such as in the next statement, if you need an output event for each event regardless of whether or not the SQL query returns rows. If the SQL query returns no rows, the join result populates null values into the selected properties.
			</para>
						
			<programlisting><![CDATA[select custId, custName from
  CustomerCallEvent as cce
  left outer join 
  sql:MyCustomerDB ["select cust_id, cust_name as custName 
                     from Customer where cust_id = ${cce.custId}"] as cq
  on cce.custId = cq.cust_id]]></programlisting>

			<para>
				The statement above always generates at least one output event for each <literal>CustomerCallEvent</literal>, containing all columns selected by the SQL query, even if the SQL query does not return any rows. Note the <literal>on</literal> expression that is required for outer joins. The <literal>on</literal> acts as an additional filter to rows returned by the SQL query.
			</para>
		</sect2>

		<sect2 xml:id="histdata_polling_pattern" revision="1">
			<title>Using Patterns to Request (Poll) Data</title>
			
			<para>
				Pattern statements and SQL queries can also be applied together in useful ways. One such use is to poll or request data from a database at regular intervals or following the schedule of the crontab-like <literal>timer:at</literal>.
			</para>
						
			<para>
				The next statement is an example that shows a pattern that fires every 5 seconds to query the NewOrder table for new orders:
			</para>

			<programlisting><![CDATA[insert into NewOrders
select orderId, orderAmount from
  pattern [every timer:interval(5 sec)],
  sql:MyCustomerDB ['select orderId, orderAmount from NewOrders']]]></programlisting>
			
		</sect2>

		<sect2 xml:id="histdata_polling" revision="1">
			<title>Polling SQL Queries via Iterator</title>
			
			<para>
				Usually your SQL query will take part in a join and thus be triggered by an event or pattern occurrence. Instead, your application may need to poll a SQL query and thus use Esper query execution and caching facilities and obtain event data and metadata.
			</para>
						
			<para>
				Your EPL statement can specify an SQL statement without a join. Such a stand-alone SQL statement does not post new events, and may only be queried via the <literal>iterator</literal> poll API. Your EPL and SQL statement may still use variables.
			</para>

			<para>
				The next statement assumes that a <literal>price_var</literal> variable has been declared. It selects from the relational database table named <literal>NewOrder</literal> all rows in which the <literal>price</literal> column is greater 
				then the current value of the <literal>price_var</literal> EPL variable:
			</para>
			<programlisting><![CDATA[select * from sql:MyCustomerDB ['select * from NewOrder where ${price_var} > price']]]></programlisting>

			<para>
				Use the <literal>iterator</literal> and <literal>safeIterator</literal> methods on <literal>EPStatement</literal> to obtain results. The statement does not post events to listeners, it is strictly passive in that sense.
			</para>
		</sect2>

		<sect2 xml:id="histdata_implementation" revision="1">
			<title>JDBC Implementation Overview</title>

			<para>
				The engine translates SQL queries into JDBC <literal>java.sql.PreparedStatement</literal> statements by replacing ${name} parameters with '?' placeholders. It obtains name and type of result columns from the compiled <literal>PreparedStatement</literal> meta data when the EPL statement is created.
			</para>

			<para>
				The engine supplies parameters to the compiled statement via the <literal>setObject</literal> method on <literal>PreparedStatement</literal>. The engine uses the <literal>getObject</literal> method on the compiled statement <literal>PreparedStatement</literal> to obtain column values.
			</para>
		</sect2>
		
		<sect2 xml:id="histdata_nometadata" revision="1">
			<title>Oracle Drivers and No-Metadata Workaround</title>

			<para>
			   Certain JDBC database drivers are known to not return metadata for precompiled prepared SQL statements. This can be a problem as metadata is required by Esper. Esper obtains SQL result set metadata to validate an EPL statement and to provide column types for output events. JDBC drivers that do not provide metadata for precompiled SQL statements require a workaround. Such drivers do generally provide metadata for executed SQL statements, however do not provide the metadata for precompiled SQL statements.
			</para>
			
			<para>
				Please consult the <xref linkend="configuration"/> for the configuration options available in relation to metadata retrieval.
			</para>

			<para>
				To obtain metadata for an SQL statement, Esper can alternatively fire a SQL statement which returns the same column names and types as the actual SQL statement but without returning any rows. This kind of SQL statement is referred to as a <emphasis>sample</emphasis> statement in below workaround description. The engine can then use the sample SQL statement to retrieve metadata for the column names and types returned by the actual SQL statement.
			</para>

			<para>
				Applications can provide a sample SQL statement to retrieve metadata via the <literal>metadatasql</literal> keyword:
			</para>
			<synopsis>sql:<emphasis>database_name</emphasis> ["<emphasis>parameterized_sql_query</emphasis>" metadatasql "<emphasis>sql_meta_query</emphasis>"] </synopsis>

			<para>
				The <emphasis>sql_meta_query</emphasis> must be an SQL statement that returns the same number of columns, the same type of columns and the same column names as the 
				<emphasis>parameterized_sql_query</emphasis>, and does not return any rows.
			</para>

			<para>
				Alternatively, applications can choose not to provide an explicit sample SQL statement. If the EPL statement does not use the <literal>metadatasql</literal> syntax, the engine applies lexical analysis to the SQL statement. From the lexical analysis Esper generates a sample SQL statement adding a restrictive clause "where 1=0"  to the SQL statement.
			</para>

			<para>
				Alternatively, you can add the following tag to the SQL statement: <literal>${$ESPER-SAMPLE-WHERE}</literal>. If the tag  exists in the SQL statement, the engine does not perform lexical analysis and simply replaces the 
				tag with the SQL <literal>where</literal> clause "where 1=0". Therefore this workaround is applicable to SQL statements that cannot be correctly lexically analyzed. The SQL text after the placeholder is not part of the sample query. For example:
			</para>
			<programlisting><![CDATA[select mycol from sql:myDB [
  'select mycol from mytesttable ${$ESPER-SAMPLE-WHERE} where ....'], ...]]></programlisting>

			<para>
				If your <emphasis>parameterized_sql_query</emphasis> SQL query contains vendor-specific SQL syntax, generation of the metadata query may fail to produce a valid SQL statement. If you experience an SQL error while fetching metadata, use any of the above workarounds with the Oracle JDBC driver. 
			</para>
		</sect2>

		<sect2 xml:id="histdata_convert_cols" revision="1">
			<title>SQL Input Parameter and Column Output Conversion</title>

			<para>
			   As part of database access configuration you may optionally specify SQL type mappings. These mappings apply to all queries against the same database identified by name.
			</para>
			
			<para>
			  If your application must perform SQL-query-specific or EPL-statement-specific mapping or conversion between types, the facility to register a conversion callback exists as follows.
			</para>

			<para>
			  Use the <literal>@Hook</literal> instruction and <literal>HookType.SQLCOL</literal> as part of your EPL statement text to register a statement SQL parameter or column conversion hook. 
			  Implement the interface <literal>com.espertech.esper.client.hook.SQLColumnTypeConversion</literal> to perform the input parameter or column value conversion.
			</para>
			
			<para>
			  A sample statement with annotation is shown:
			</para>
			<programlisting><![CDATA[@Hook(type=HookType.SQLCOL, hook='MyDBTypeConvertor')
select * from sql:MyDB ['select * from MyEventTable]]]></programlisting>

			<para>
			  The engine expects <literal>MyDBTypeConvertor</literal> to resolve to a class (considering engine imports) and instantiates one instance of MyDBTypeConvertor for each statement.
			</para>
		</sect2>

		<sect2 xml:id="histdata_convert_row" revision="1">
			<title>SQL Row POJO Conversion</title>

			<para>
			   Your application may also directly convert a SQL result row into a Java class which is an opportunity for your application to interrogate and transform the SQL row result data freely before packing the data into a 
			   Java class. Your application can additionally indicate to skip SQL result rows.
			</para>
			
			<para>
			  Use the <literal>@Hook</literal> instruction and <literal>HookType.SQLROW</literal> as part of your EPL statement text to register a statement SQL output row conversion hook. 
			  Implement the interface <literal>com.espertech.esper.client.hook.SQLOutputRowConversion</literal> to perform the output row conversion.
			</para>
			
			<para>
			  A sample statement with annotation is shown:
			</para>
			<programlisting><![CDATA[@Hook(type=HookType.SQLROW, hook='MyDBRowConvertor')
select * from sql:MyDB ['select * from MyEventTable]]]></programlisting>

			<para>
			  The engine expects <literal>MyDBRowConvertor</literal> to resolve to a class (considering engine imports) and instantiates one MyDBRowConvertor instance for each statement.
			</para>
		</sect2>
	</sect1>
	
	<sect1 xml:id="joining_method" revision="1">
		<title>Accessing Non-Relational Data via Method, Script or UDF Invocation</title>
		
		<indexterm><primary>join</primary><secondary>non-relational data via method, script or UDF invocation</secondary></indexterm>
		<indexterm><primary>EPL</primary><secondary>joining non-relational data via method, script or UDF invocation</secondary></indexterm>

		<para>
			Your application may need to join data that originates from a web service, a distributed cache, an object-oriented database or simply data held in memory by your application. 
			One way to join in external data is by means of method, script or user-defined function invocation (or procedure call or function) in the <literal>from</literal> clause of a statement. 
		</para>
			
		<para>
			The results of such a method, script or UDF invocation in the <literal>from</literal> clause plays the same role as a relational database table in an inner and outer join in SQL.
		</para>

		<para>
			Esper can join and outer join an unlimited number and all types of event streams to the data returned by your invocation. In addition, Esper can be configured to cache the data returned by your method, script or UDF invocations.
		</para>
		
		<para>
			Joins or outer joins in which only SQL statements or method, script or UDF invocations are listed in the <literal>from</literal> clause and no other event streams are termed <emphasis>passive</emphasis> joins. A passive join does not produce an insert or remove stream and therefore does not invoke statement listeners with results. A passive join can be iterated on (polled) using a statement's <literal>safeIterator</literal> and <literal>iterator</literal> methods.
		</para>

		<para>
			The following restrictions currently apply:
		</para>

		<itemizedlist spacing="compact">
			<listitem>
				<para>
					A invocation cannot declare data windows; That is, you cannot create a time or length window on an invocation. Instead, use <literal>insert into</literal> to make join results available for further processing.
				</para>
			</listitem>
		</itemizedlist>

		<sect2 xml:id="joining_method_syntax" revision="1">
			<title>Joining Method, Script or UDF Invocation Results</title>
			<para>
				The syntax for a method, script or UDF invocation in the <literal>from</literal> clause of an EPL statement is:
			</para>
			<synopsis>method: [<emphasis>class_or_variable_name</emphasis>.]<emphasis>method_script_udf_name</emphasis>[(<emphasis>parameter_expressions</emphasis>)] [@type(<emphasis>eventtype_name</emphasis>)]</synopsis>
	
			<para>
				The <literal>method</literal> keyword denotes a method, script or UDF invocation. It is followed by an optional class or variable name.
				The <emphasis>method_script_udf_name</emphasis> is the name of the method, script or user-defined function.
				If you have parameters to your method, script or UDF invocation, these are placed in 
				parentheses after the method or script name. Any expression is allowed as a parameter, and individual parameter expressions are separated by a comma. 
				Expressions may also use event properties of the joined stream. 
			</para>
			
			<para>
				In case the return type of the method is <literal>EventBean</literal> instances, you must provide the <literal>@type</literal> annotation to name the event type of events returned. 
				Otherwise <literal>@type</literal> is not allowed.
			</para>

			<para>
				In the sample join statement shown next, the method <literal>lookupAsset</literal> provided by class (or variable) <literal>MyLookupLib</literal> returns one or more rows based on the asset id (a property of the <literal>AssetMoveEvent</literal>) that is passed to the method:
			</para>
			<programlisting><![CDATA[select * from AssetMoveEvent, method:MyLookupLib.lookupAsset(assetId)]]></programlisting>
	
			<para>
				The following statement demonstrates the use of the <literal>where</literal> clause to join events to the rows returned by an invocation, which in this example does not take parameters:
			</para>
			<programlisting><![CDATA[select assetId, assetDesc from AssetMoveEvent as asset, 
       method:MyLookupLib.getAssetDescriptions() as desc 
where asset.assetid = desc.assetid]]></programlisting>
	
			<para>
				Your method, scipt or UDF invocation may return zero, one or many rows for each invocation. If you have caching enabled through configuration, then Esper can avoid the invocation and instead use cached results. Similar to SQL joins, Esper also indexes cached result rows such that join operations based on the <literal>where</literal> clause or outer-join <literal>on</literal> clause can be very efficient, especially if your invocation returns a large number of rows.
			</para>

			<para>
				If the time taken by method, script or UDF invocations is critical to your application, you may configure local caches as <xref linkend="config-method-invocations"/> describes.
			</para>

			<para>
				Esper analyzes the expression in the EPL <literal>where</literal> clause and outer-join <literal>on</literal> clause, if present, and builds the appropriate indexes from that information at runtime, to ensure fast matching of event stream events to invocation results, even if your invocation returns a large number of rows. Your applications must ensure to configure a cache for your invocation using Esper configuration, as such indexes are held with regular data in a cache. If you application does not enable caching of invocation results, the engine does not build indexes on cached data.
			</para>
			
		</sect2>

		<sect2 xml:id="joining_method_polling" revision="1">
			<title>Polling Invocation Results via Iterator</title>
			
			<para>
				Usually your invocation will take part in a join and thus be triggered by an event or pattern occurrence. Instead, your application may need to poll an invocation and thus use Esper query execution and caching facilities and obtain event data and metadata.
			</para>
						
			<para>
				Your EPL statement can specify an invocation in the <literal>from</literal> clause without a join. Such a stand-alone invocation does not post new events, and may only be queried via the <literal>iterator</literal> poll API. Your EPL statement may still use variables.
			</para>

			<para>
				The next statement assumes that a <literal>category_var</literal> variable has been declared. It polls the <literal>getAssetDescriptions</literal> method passing the current value of the <literal>category_var</literal> EPL variable:
			</para>
			<programlisting><![CDATA[select * from method:MyLookupLib.getAssetDescriptions(category_var)]]]></programlisting>

			<para>
				Use the <literal>iterator</literal> and <literal>safeIterator</literal> methods on <literal>EPStatement</literal> to obtain results. The statement does not post events to listeners, it is strictly passive in that sense.
			</para>
		</sect2>

		<sect2 xml:id="joining_method_exposing" revision="1">
			<title>Providing the Method</title>
			
			<para>
				You application can provide a public static method or can provide an instance method of an existing object.
				The method must accept the same number and type of parameters as listed in the parameter expression list. 
			</para>
						
			<para>
				The examples herein mostly use public static methods. For a detail description of instance methods please see <xref linkend="variable_eventtype"/> and below example.
			</para>
			
			<para>
				If your invocation returns either no row or only one row, then the return type of the method can be a Java class, <literal>java.util.Map</literal> or <literal>Object[]</literal> (object-array). If your invocation can return more then one row, then the return type of the method must be an array of Java class, array of <literal>Map</literal>, <literal>Object[][]</literal> (object-array 2-dimensional) or
				<literal>Collection</literal> or <literal>Iterator</literal> (or subtypes thereof).
			</para>
			
			<para>
				If you are using a Java class, an array of Java class or a <literal>Collection&lt;Class&gt;</literal> or an <literal>Iterator&lt;Class&gt;</literal> as the return type, then the class must adhere to JavaBean conventions: it must expose properties through getter methods.
			</para>

			<para>
				If you are using <literal>java.util.Map</literal> or an array of <literal>Map</literal> or a <literal>Collection&lt;Map&gt;</literal> or an <literal>Iterator&lt;Map&gt;</literal> as the return type, please note the following:
			</para>
			<itemizedlist>
				<listitem>
					Your application must provide a second method that returns event property metadata, as the next section outlines.
				</listitem>
				<listitem>
					Each map instance returned by your method should have <literal>String</literal>-type keys and object values (<literal>Map&lt;String, Object&gt;</literal>).
				</listitem>
			</itemizedlist>

			<para>
				If you are using <literal>Object[]</literal> (object-array) or <literal>Object[][]</literal> (object-array 2-dimensional) or <literal>Collection&lt;Object[]&gt;</literal> or <literal>Iterator&lt;Object[]&gt;</literal> as the return type, please note the following:
			</para>
			<itemizedlist>
				<listitem>
					Your application must provide a second method that returns event property metadata, as the next section outlines.
				</listitem>
				<listitem>
					Each object-array instance returned by your method should have the exact same array position for values as the property metadata indicates and the array length must be the same as the 
					number of properties.
				</listitem>
			</itemizedlist>
			
			<para>
				Your application method must return either of the following:
			</para>
			
			<orderedlist>
				<listitem>
					<para>
						A <literal>null</literal> value or an empty array to indicate an empty result (no rows).
					</para>
				</listitem>
				<listitem>
					<para>
						A Java object or <literal>Map</literal> or <literal>Object[]</literal> to indicate a zero (null) or one-row result.
					</para>
				</listitem>
				<listitem>
					<para>
						 Return multiple result rows by returning either:
					</para>
					<itemizedlist>
						<listitem>
							An array of Java objects.
						</listitem>
						<listitem>
							An array of <literal>Map</literal> instances.
						</listitem>
						<listitem>
							An array of <literal>Object[]</literal> instances.
						</listitem>
						<listitem>
							An array of <literal>EventBean[]</literal> instances (requires <literal>@type</literal>).
						</listitem>
						<listitem>
							A <literal>Collection</literal> of Java objects.
						</listitem>
						<listitem>
							A <literal>Collection</literal> of <literal>Map</literal> instances.
						</listitem>
						<listitem>
							A <literal>Collection</literal> of <literal>Object[]</literal> instances.
						</listitem>
						<listitem>
							An <literal>Collection</literal> of <literal>EventBean[]</literal> instances (requires <literal>@type</literal>).
						</listitem>
						<listitem>
							An <literal>Iterator</literal> of Java objects.
						</listitem>
						<listitem>
							An <literal>Iterator</literal> of <literal>Map</literal> instances.
						</listitem>
						<listitem>
							An <literal>Iterator</literal> of <literal>Object[]</literal> instances.
						</listitem>
						<listitem>
							An <literal>Iterator</literal> of <literal>EventBean[]</literal> instances (requires <literal>@type</literal>).
						</listitem>
					</itemizedlist>
				</listitem>
			</orderedlist>
			
			<para>
				As an example, consider the method 'getAssetDescriptions' provided by class 'MyLookupLib' as discussed earlier:
			</para>
			<programlisting><![CDATA[]]>select assetId, assetDesc from AssetMoveEvent as asset,
       method:com.mypackage.MyLookupLib.getAssetDescriptions() as desc 
  where asset.assetid = desc.assetid</programlisting>
																	
			<para>
				The 'getAssetDescriptions' method may return multiple rows and is therefore declared to return an array of the class 'AssetDesc'. The class AssetDesc is a POJO class (not shown here):
			</para>
			<programlisting><![CDATA[public class MyLookupLib {
  ...
  public static AssetDesc[] getAssetDescriptions() {
    ...
    return new AssetDesc[] {...};
  }]]></programlisting>

			<para>
				The example above specifies the full Java class name of the class 'MyLookupLib' class in the EPL statement. The package name does not need to be part of the EPL if your application imports the package using the auto-import configuration through the API or XML, as outlined in <xref linkend="config-class--package-imports"/>.
			</para>
			
			<para>
				Alternatively the example above could return a <literal>Collection</literal> wherein the method declares as <literal>public static Collection&lt;AssetDesc&gt; getAssetDescriptions() {...}</literal> or an <literal>Iterator</literal> wherein the method declares as <literal>public static Iterator&lt;AssetDesc&gt; getAssetDescriptions() {...}</literal>.
			</para>
			
			<para>
				Method overloading is allowed as long as overloaded methods return the same result type.
			</para>

			<sect3 xml:id="joining_method_exposing_instance" revision="1">
				<title>Providing an Instance Method</title>

				<para>
					If you application has an existing object instance such as a service or a dependency injected bean then it must make the instance available as a variable. Please see <xref linkend="variable_eventtype"/>
					for more information.
				</para>
				
				<para>
					For example, assuming you provided a <literal>stateChecker</literal> variable that points to an object instance that provides a public <literal>getMatchingAssets</literal> instance method
					and that returns property <literal>assetIds</literal>, you may use the state checker service in the <literal>from</literal>-clause as follows:
				</para>
				<programlisting><![CDATA[select assetIds from AssetMoveEvent, method:stateChecker.getMatchingAssets(assetDesc)]]></programlisting>
			</sect3>

		</sect2>

		<sect2 xml:id="joining_method_map" revision="1">
			<title>Using a <literal>Map</literal> Return Type</title>
			
			<para>
				Your application may return <literal>java.util.Map</literal> or an array of <literal>Map</literal> from invocations. If doing so, your application must provide metadata about each row: it must declare the property name and property type of each <literal>Map</literal> entry of a row. This information allows the engine to perform type checking of expressions used within the statement.
			</para>
			
			<para>
				You declare the property names and types of each row by providing a method that returns property metadata. The metadata method must follow these conventions:
			</para>
			
			<orderedlist>
				<listitem>
					<para>
						The method name providing the property metadata must have same method name appended by the literal <literal>Metadata</literal>.
					</para>
				</listitem>
				<listitem>
					<para>
						The method must have an empty parameter list and must be declared public and static.
					</para>
				</listitem>
				<listitem>
					<para>
						The method providing the metadata must return a <literal>Map</literal> of <literal>String</literal> property name keys and <literal>java.lang.Class</literal> property name types (<literal>Map&lt;String, Class&gt;</literal>).
					</para>
				</listitem>
			</orderedlist>
			
			<para>
				In the following example, a class 'MyLookupLib' provides a method to return historical data based on asset id and asset code:
			</para>
			<programlisting><![CDATA[]]>select assetId, location, x_coord, y_coord from AssetMoveEvent as asset,
       method:com.mypackage.MyLookupLib.getAssetHistory(assetId, assetCode) as history</programlisting>
																	
			<para>
				A sample implementation of the class 'MyLookupLib' is shown below. 
			</para>
			<programlisting><![CDATA[public class MyLookupLib {
  ...
  // For each column in a row, provide the property name and type
  //
  public static Map<String, Class> getAssetHistoryMetadata() {
    Map<String, Class> propertyNames = new HashMap<String, Class>();
    propertyNames.put("location", String.class);
    propertyNames.put("x_coord", Integer.class);
    propertyNames.put("y_coord", Integer.class);
    return propertyNames;
  }
... 
  // Lookup rows based on assetId and assetCode
  // 
  public static Map<String, Object>[] getAssetHistory(String assetId, String assetCode) {
    Map rows = new Map[2];	// this sample returns 2 rows
    for (int i = 0; i < 2; i++) {
      rows[i] = new HashMap();
      rows[i].put("location", "somevalue");
      rows[i].put("x_coord", 100);
      // ... set more values for each row
    }
    return rows;
  }]]></programlisting>

			<para>
				In the example above, the 'getAssetHistoryMetadata' method provides the property metadata: the names and types of properties in each row.  The engine calls this method once per statement to determine event typing information.
			</para>

			<para>
				The 'getAssetHistory' method returns an array of <literal>Map</literal> objects that are two rows. The implementation shown above is a simple example. The parameters to the method are the assetId and assetCode properties of the AssetMoveEvent joined to the method. The engine calls this method for each insert and remove stream event in AssetMoveEvent.
			</para>
			
			<para>
				To indicate that no rows are found in a join, your application method may return either a <literal>null</literal> value or an array of size zero.
			</para>
			
			<para>
				Alternatively the example above could return a <literal>Collection</literal> wherein the method declares as <literal>public static Collection&lt;Map&gt; getAssetHistory() {...}</literal> or an <literal>Iterator</literal> wherein the method declares as <literal>public static Iterator&lt;Map&gt; getAssetHistory() {...}</literal>.
			</para>
		</sect2>

		<sect2 xml:id="joining_method_oa" revision="1">
			<title>Using a Object Array Return Type</title>
			
			<para>
				Your application may return <literal>Object[]</literal> (object-array) or an array of <literal>Object[]</literal> (object-array 2-dimensional) from invocations. If doing so, your application must provide metadata about each row: it must declare the property name and property type of each array entry of a row in the exact same order as provided by value rows. This information allows the engine to perform type checking of expressions used within the statement.
			</para>
			
			<para>
				You declare the property names and types of each row by providing a method that returns property metadata. The metadata method must follow these conventions:
			</para>
			
			<orderedlist>
				<listitem>
					<para>
						The method name providing the property metadata must have same method name appended by the literal <literal>Metadata</literal>.
					</para>
				</listitem>
				<listitem>
					<para>
						The method must have an empty parameter list and must be declared public and static.
					</para>
				</listitem>
				<listitem>
					<para>
						The method providing the metadata must return a <literal>LinkedHashMap</literal> of <literal>String</literal> property name keys and <literal>java.lang.Class</literal> property name types (<literal>Map&lt;String, Class&gt;</literal>).
					</para>
				</listitem>
			</orderedlist>
			
			<para>
				In the following example, a class 'MyLookupLib' provides a method to return historical data based on asset id and asset code:
			</para>
			<programlisting><![CDATA[]]>select assetId, location, x_coord, y_coord from AssetMoveEvent as asset,
       method:com.mypackage.MyLookupLib.getAssetHistory(assetId, assetCode) as history</programlisting>
																	
			<para>
				A sample implementation of the class 'MyLookupLib' is shown below. 
			</para>
			<programlisting><![CDATA[public class MyLookupLib {
  ...
  // For each column in a row, provide the property name and type
  //
  public static LinkedHashMap<String, Class> getAssetHistoryMetadata() {
    LinkedHashMap<String, Class> propertyNames = new LinkedHashMap<String, Class>();
    propertyNames.put("location", String.class);
    propertyNames.put("x_coord", Integer.class);
    propertyNames.put("y_coord", Integer.class);
    return propertyNames;
  }
... 
  // Lookup rows based on assetId and assetCode
  // 
  public static Object[][] getAssetHistory(String assetId, String assetCode) {
    Object[][] rows = new Object[5][];	// this sample returns 5 rows
    for (int i = 0; i < 5; i++) {
      rows[i] = new Object[2]; // single row has 2 fields
      rows[i][0]  = "somevalue";
      rows[i][1] = 100;
      // ... set more values for each row
    }
    return rows;
  }]]></programlisting>

			<para>
				In the example above, the 'getAssetHistoryMetadata' method provides the property metadata: the names and types of properties in each row.  The engine calls this method once per statement to determine event typing information.
			</para>

			<para>
				The 'getAssetHistory' method returns an <literal>Object[][]</literal> that represents five rows. The implementation shown above is a simple example. The parameters to the method are the assetId and assetCode properties of the AssetMoveEvent joined to the method. The engine calls this method for each insert and remove stream event in AssetMoveEvent.
			</para>
			
			<para>
				To indicate that no rows are found in a join, your application method may return either a <literal>null</literal> value or an array of size zero.
			</para>		

			<para>
				Alternatively the example above could return a <literal>Collection</literal> wherein the method declares as <literal>public static Collection&lt;Object[]&gt; getAssetHistory() {...}</literal> or an <literal>Iterator</literal> wherein the method declares as <literal>public static Iterator&lt;Object[]&gt; getAssetHistory() {...}</literal>.
			</para>
		</sect2>

		<sect2 xml:id="joining_method_eventbeanarray" revision="1">
			<title>Using an <literal>EventBean</literal> Return Type</title>
			<para>
				When the return type is <literal>EventBean[]</literal>, <literal>Collection&lt;EventBean&gt;</literal> or <literal>Iterator&lt;EventBean&gt;</literal>, you must specify the event type name using <literal>@type</literal>.
			</para>

			<para>
				For example assuming the event type <literal>ItemEvent</literal> is declared as <literal>create schema ItemEvent(p0 string)</literal>:
			</para>
			<programlisting><![CDATA[select * from MyEvent, method:MyLib.myFunc() @type(ItemEvent)]]></programlisting>
			<programlisting><![CDATA[public static EventBean[] myFunc(EPLMethodInvocationContext context) {
  EventBean[1] events = new EventBean[1];
  events[0] = context.getEventBeanService().adapterForMap(Collections.singletonMap("p0", "hello"), "ItemEvent");
  return events;
}]]></programlisting>
		</sect2>

		<sect2 xml:id="joining_method_script" revision="1">
			<title>Providing the Script</title>
			
			<para>
				Your script must declare the return type as <literal>EventBean[]</literal>. In the <literal>@type</literal> annotation you must provide an event type name.
			</para>

			<para>
				For example assuming the event type <literal>ItemEvent</literal> is declared as <literal>create schema ItemEvent(id string)</literal>:
			</para>
			<programlisting><![CDATA[select id from MyEvent, method:myItemProducerScript()]]></programlisting>

			<para>
				The example JavaScript script is:
			</para>
			<programlisting><![CDATA[create expression EventBean[] @type(ItemEvent) js:myItemProducerScript() [
myItemProducerScript()
function myItemProducerScript() {
  var EventBeanArray = Java.type(\"com.espertech.esper.client.EventBean[]\");
  var events = new EventBeanArray(1);
  events[0] = epl.getEventBeanService().adapterForMap(java.util.Collections.singletonMap(\"id\", \"id1\"), \"ItemEvent\");
  return events;
}]]]></programlisting>
		</sect2>

		<sect2 xml:id="joining_method_udf" revision="1">
			<title>Providing the UDF</title>
			
			<para>
				Your script must declare the return type of the UDF as <literal>EventBean[]</literal>. In the <literal>@type</literal> annotation you must provide an event type name.
			</para>

			<para>
				For example assuming you have registered a user-defined function <literal>myUserDefinedFunction</literal>:
			</para>
			<programlisting><![CDATA[select id from MyEvent, method:myUserDefinedFunction() @type(ItemEvent)]]></programlisting>
		</sect2>
	</sect1>
	
    <sect1 xml:id="epl_createschema">
        <title>Declaring an Event Type: <emphasis>Create Schema</emphasis></title>

		<indexterm><primary>create schema</primary></indexterm>
		<indexterm><primary>event type</primary><secondary>declaring</secondary></indexterm>
		<para>
			EPL allows declaring an event type via the <literal>create schema</literal> clause and also by means of the static or runtime configuration API <literal>addEventType</literal> functions. The term schema and event type has the same meaning in EPL.
		</para>

		<para>
			Your application can declare an event type by providing the property names and types or by providing a class name. Your application may also declare a variant stream schema.
		</para>

		<para>
			When using the <literal>create schema</literal> syntax to declare an event type, the engine automatically removes the event type when there are no started statements referencing the event type, including the statement that declared the event type. When using
			the configuration API, the event type stays cached even if there are no statements that refer to the event type and until explicitly removed via the runtime configuration API.
		</para>

		<sect2 xml:id="epl_createschema_properties" revision="1">
			<title>Declare an Event Type by Providing Names and Types</title>

			<para>
				The synopsis of the <literal>create schema</literal> syntax providing property names and types is:
			</para>
			<synopsis>create [map | objectarray | avro] schema <emphasis>schema_name</emphasis> [as] 
    (<emphasis>property_name</emphasis> <emphasis>property_type</emphasis> [,<emphasis>property_name</emphasis> <emphasis>property_type</emphasis> [,...])
  [inherits <emphasis>inherited_event_type</emphasis>[, <emphasis>inherited_event_type</emphasis>] [,...]]
  [starttimestamp <emphasis>timestamp_property_name</emphasis>]
  [endtimestamp <emphasis>timestamp_property_name</emphasis>]
  [copyfrom <emphasis>copy_type_name</emphasis> [, <emphasis>copy_type_name</emphasis>] [,...]]</synopsis>

			<para>				
				The <literal>create</literal> keyword can be followed by <literal>map</literal> to instruct the engine to represent events of that type by the Map event representation, or <literal>objectarray</literal> to denote an Object-array event type, or <literal>avro</literal> to denote an Avro event type. If neither the <literal>map</literal> or <literal>objectarray</literal> or <literal>avro</literal> keywords are provided, the engine-wide default event representation applies. 
			</para>

			<para>				
				After <literal>create schema</literal> follows a <emphasis>schema_name</emphasis>. The schema name is the event type name.
			</para>

			<para>				
				The <emphasis>property_name</emphasis> is an identifier providing the event property name. The <emphasis>property_type</emphasis> is also required for each property. Valid property types are listed in <xref linkend="variable_create"/>
				and in addition include:
			</para>
	
			<orderedlist spacing="compact">
				<listitem>
					<para>
						 Any Java class name, fully-qualified or the simple class name if imports are configured.
					</para>
				</listitem>
				<listitem>
					<para>
						 Add left and right square brackets <literal>[]</literal> to any type to denote an array-type event property.
					</para>
				</listitem>
				<listitem>
					<para>
						Use an event type name as a property type.
					</para>
				</listitem>
				<listitem>
					<para>
						The <literal>null</literal> keyword for a null-typed property.
					</para>
				</listitem>
			</orderedlist>

			<para>
				The optional <literal>inherits</literal> keywords is followed by a comma-separated list of event type names that are the supertypes to the declared type.
			</para>
			
			<para>
				The optional <literal>starttimestamp</literal> keyword is followed by a property name. Use this to tell the engine that your event has a timestamp. The engine checks that the property name exists on the declared type and returns a date-time value.
				Declare a timestamp property if you want your events to implicitly carry a timestamp value for convenient use with interval algebra methods as a start timestamp. 
			</para>

			<para>
				The optional <literal>endtimestamp</literal> keyword is followed by a property name. Use this together with starttimestamp to tell the engine that your event has a duration. The engine checks that the property name exists on the declared type and returns a date-time value.
				Declare an endtimestamp property if you want your events to implicitly carry a duration value for convenient use with interval algebra methods.
			</para>

			<para>
				The optional <literal>copyfrom</literal> keyword is followed by a comma-separate list of event type names. For each event type listed, the engine looks up that type and adds all event property definitions to the newly-defined type, in addition to those listed explicitly (if any). The resulting order of properties is that copied-from properties are first (in the order of event types and their property order) and explicitly-listed properties last. 
			</para>

			<para>
				A few example event type declarations follow:
			</para>
			<programlisting><![CDATA[// Declare type SecurityEvent
create schema SecurityEvent as (ipAddress string, userId String, numAttempts int)
			
// Declare type AuthorizationEvent with the roles property being an array of String 
// and the hostinfo property being a POJO object
create schema AuthorizationEvent(group String, roles String[], hostinfo com.mycompany.HostNameInfo)

// Declare type CompositeEvent in which the innerEvents property is an array of SecurityEvent
create schema CompositeEvent(group String, innerEvents SecurityEvent[])

// Declare type WebPageVisitEvent that inherits all properties from PageHitEvent
create schema WebPageVisitEvent(userId String) inherits PageHitEvent

// Declare a type with start and end timestamp (i.e. event with duration).
create schema RoboticArmMovement (robotId string, startts long, endts long) 
  starttimestamp startts endtimestamp endts
  
// Create a type that has all properties of SecurityEvent plus a userName property
create schema ExtendedSecurityEvent (userName string) copyfrom SecurityEvent

// Create a type that has all properties of SecurityEvent 
create schema SimilarSecurityEvent () copyfrom SecurityEvent

// Create a type that has all properties of SecurityEvent and WebPageVisitEvent plus a userName property
create schema WebSecurityEvent (userName string) copyfrom SecurityEvent, WebPageVisitEvent]]></programlisting>	
  
			  <para>
				  To elaborate on the <literal>inherits</literal> keyword, consider the following two schema definitions:
			  </para>
			  <programlisting><![CDATA[create schema Foo as (prop1 string)]]></programlisting>
			  <programlisting><![CDATA[create schema Bar() inherits Foo]]></programlisting>

			  <para>
			      Following above schema,  Foo is a supertype or Bar and therefore any Bar event also fulfills Foo and matches where Foo matches. An EPL statement such as <literal>select * from Foo</literal> 
			      returns any Foo event as well as any event that is a subtype of Foo such as all Bar events. When your EPL queries don't use any Foo events there is no cost, thus <literal>inherits</literal> is 
			      generally an effective way to share properties between types. The start and end timestamp are also inherited from any supertype that has the timestamp property names defined.
			  </para>

			  <para>
				  The optional <literal>copyfrom</literal> keyword is for defining a schema based on another schema. This keyword causes the engine to copy property definitions: There is no inherits, extends, supertype or subtype relationship between the types listed.
			  </para>
			  
			  <para>
				  To define an event type <literal>Bar</literal> that has the same properties as <literal>Foo</literal>:
			  </para>
			  <programlisting><![CDATA[create schema Foo as (prop1 string)]]></programlisting>
			  <programlisting><![CDATA[create schema Bar() copyfrom Foo]]></programlisting>
			  
			  <para>
				  To define an event type <literal>Bar</literal> that has the same properties as <literal>Foo</literal> and that adds its own property <literal>prop2</literal>:
			  </para>
			  <programlisting><![CDATA[create schema Foo as (prop1 string)]]></programlisting>
			  <programlisting><![CDATA[create schema Bar(prop2 string) copyfrom Foo]]></programlisting>

			  <para>
			    If neither the <literal>map</literal> or <literal>objectarray</literal> or <literal>avro</literal> keywords are provided, and if the create-schema statement provides the <literal>@EventRepresentation(objectarray)</literal> annotation the engine expects object array events. If the statement provides the <literal>@EventRepresentation(avro)</literal> annotation the engine expects Avro objects as events. If the statement provides the <literal>@EventRepresentation(map)</literal> annotation the engine expects Map objects as events. If neither annotation is provided, the engine uses the configured default event representation as discussed in <xref linkend="config-engine-eventmeta-representation"/>.
			  </para>

			  <para>
			    The following two EPL statements both instructs the engine to represent Foo events as object arrays. When sending Foo events into the engine use the <literal>sendEvent(Object[] data, String typeName)</literal> footprint.
			  </para>
			  <programlisting><![CDATA[create objectarray schema Foo as (prop1 string)]]></programlisting>
			  <programlisting><![CDATA[@EventRepresentation(objectarray) create schema Foo as (prop1 string)]]></programlisting>

			  <para>
			    The next two EPL statements both instructs the engine to represent Foo events as Maps. When sending Foo events into the engine use the <literal>sendEvent(Map data, String typeName)</literal> footprint.
			  </para>
			  <programlisting><![CDATA[create map schema Foo as (prop1 string)]]></programlisting>
			  <programlisting><![CDATA[@EventRepresentation(map) create schema Foo as (prop1 string)]]></programlisting>

			  <para>
			    The following two EPL statements both instructs the engine to represent Foo events as Avro <literal>GenericData.Record</literal>. When sending Foo events into the engine use the <literal>sendEventAvro(Object genericDataDotRecord, String typeName)</literal> footprint.
			  </para>
			  <programlisting><![CDATA[create avro schema Foo as (prop1 string)]]></programlisting>
			  <programlisting><![CDATA[@EventRepresentation(avro) create schema Foo as (prop1 string)]]></programlisting>
		</sect2>

		<sect2 xml:id="epl_createschema_class" revision="1">
			<title>Declare an Event Type by Providing a Class Name</title>

			<para>
				When using Java classes as the underlying event representation your application may simply provide the class name:
			</para>
			<synopsis>create schema <emphasis>schema_name</emphasis> [as] <emphasis>class_name</emphasis>
  [starttimestamp <emphasis>timestamp_property_name</emphasis>]
  [endtimestamp <emphasis>timestamp_property_name</emphasis>]</synopsis>

			<para>				
				The <emphasis>class_name</emphasis> must be a fully-qualified class name (including the package name) if imports are not configured. If you application configures imports then the simple class name suffices without package name.
			</para>
	
			<para>
				The optional <literal>starttimestamp</literal> and <literal>endtimestamp</literal> keywords have a meaning as defined earlier.
			</para>

			<para>
				The next example statements declare an event type based on a class:
			</para>
			<programlisting><![CDATA[// Shows the use of a fully-qualified class name to declare the LoginEvent event type
create schema LoginEvent as com.mycompany.LoginValue

// When the configuration includes imports, the declaration does not need a package name
create schema LogoutEvent as SignoffValue]]></programlisting>	
		</sect2>

		<sect2 xml:id="epl_createschema_variant" revision="1">
			<title>Declare a Variant Stream</title>

			<para>
				A variant stream is a predefined stream into which events of multiple disparate event types can be inserted.
				Please see <xref linkend="insert-into-merging-types"/> for rules regarding property visibility and additional information.
			</para>
			
			<para>
				The synopsis is:
			</para>
			<synopsis>create variant schema <emphasis>schema_name</emphasis> [as] <emphasis>eventtype_name|*</emphasis> [, <emphasis>eventtype_name|*</emphasis>] [,...]</synopsis>

			<para>
				Provide the <literal>variant</literal> keyword to declare a variant stream.
			</para>

			<para>				
				The '<literal>*</literal>' wildcard character declares a variant stream that accepts any type of event inserted into the variant stream.
			</para>

			<para>				
				Provide <emphasis>eventtype_name</emphasis> if the variant stream should hold events of the given type only. When using <literal>insert into</literal> to 
				insert into the variant stream the engine checks to ensure the inserted event type or its supertypes match the required event type.
			</para>
	
			<para>
				A few examples are shown below:
			</para>
			<programlisting><![CDATA[// Create a variant stream that accepts only LoginEvent and LogoutEvent event types
create variant schema SecurityVariant as LoginEvent, LogoutEvent

// Create a variant stream that accepts any event type
create variant schema AnyEvent as *]]></programlisting>	
		</sect2>
	</sect1>

	<sect1 xml:id="split_overview" revision="1">
		<title>Splitting and Duplicating Streams</title>
		
		<para>
			EPL offers a convenient syntax to splitting, routing or duplicating events into multiple streams, and for receiving unmatched events among a set of filter criteria.
		</para>
		
		<para>
		  For splitting a single event that acts as a container and expose child events as a property of itself consider the contained-event syntax as described in <xref linkend="epl-containedeventselect"/>.
		  For generating marker events for contained-events please see below.
		</para>

		<para>
			You may define a triggering event or pattern in the <literal>on</literal>-part of the statement followed by multiple <literal>insert into</literal>, <literal>select</literal> and <literal>where</literal> clauses.
		</para>

		<para>
			The synopsis is:
		</para>

<synopsis>[context <emphasis>context_name</emphasis>]
on <emphasis>event_type</emphasis>[(<emphasis>filter_criteria</emphasis>)] [as <emphasis>stream_name</emphasis>]
insert into <emphasis>insert_into_def</emphasis> select <emphasis>select_list</emphasis> [where <emphasis>condition</emphasis>]
[insert into <emphasis>insert_into_def</emphasis> select <emphasis>select_list</emphasis> [from <emphasis>contained-event-selection</emphasis>] [where <emphasis>condition</emphasis>]]
[insert into <emphasis>insert_into_def</emphasis> select <emphasis>select_list</emphasis> [from <emphasis>contained-event-selection</emphasis>] [where <emphasis>condition</emphasis>]]
[insert into...]
[output first | all]</synopsis>

		<para>
			The <emphasis>event_type</emphasis> is the name of the type of events that trigger the split stream. It is optionally
followed by <emphasis>filter_criteria</emphasis> which are filter expressions to apply to arriving events. The optional <literal>as</literal> keyword
can be used to assign a stream name. Patterns and named windows can also be specified in the <literal>on</literal> clause.
		</para>

		<para>
			Following the <literal>on</literal>-clause is one or more <emphasis>insert into</emphasis> clauses as described in <xref linkend="epl-insert-into"/> and <emphasis>select</emphasis> clauses as described in <xref linkend="epl-select-list"/>. 
		</para>
		
		<para>
			The second and subsequent <literal>insert into</literal> and <literal>select</literal> clause pair can have a <literal>from</literal> clause for <emphasis>contained-event-selection</emphasis>. 
			This is useful when your trigger events themselves contain events that must be processed individually and that may be delimited by marker events that you can define.
		</para>
		
		<para>
			Each <literal>select</literal> clause may be followed by a <literal>where</literal> clause containing a condition. If the condition is true for the event, the engine transforms the event according to the <literal>select</literal> clause and inserts it into the corresponding stream.
		</para>

		<para>
			At the end of the statement can be an optional <literal>output</literal> clause. By default the engine inserts into the first stream for which the <literal>where</literal> clause condition matches if one was specified, starting from the top. If you specify the <literal>output all</literal> keywords, then the engine inserts into each stream (not only the first stream) for which the <literal>where</literal> clause condition matches or that do not have a <literal>where</literal> clause.
		</para>

		<para>
			If, for a given event, none of the <literal>where</literal> clause conditions match, the statement listener receives the unmatched event. The statement listener only receives unmatched events and does not receive any transformed or inserted events. The <literal>iterator</literal> method to the statement returns no events.
		</para>
		
		<para>
			You may specify an optional context name to the effect that the split-stream operates according to the context dimensional information as declared for the context. See <xref linkend="context"/> for more information.
		</para>

		<para>
			In the below sample statement, the engine inserts each <literal>OrderEvent</literal> into the <literal>LargeOrders</literal> stream if the order quantity is 100 or larger, or into the <literal>SmallOrders</literal> stream if the order quantity is smaller then 100:
		</para>
		<programlisting><![CDATA[on OrderEvent 
  insert into LargeOrders select * where orderQty >= 100
  insert into SmallOrders select *]]></programlisting>

		<para>
			The next example statement adds a new stream for medium-sized orders. The new stream receives orders that have an order quantity between 20 and 100:
		</para>
		<programlisting><![CDATA[on OrderEvent 
  insert into LargeOrders select orderId, customer where orderQty >= 100
  insert into MediumOrders select orderId, customer where orderQty between 20 and 100
  insert into SmallOrders select orderId, customer where orderQty > 0]]></programlisting>

		<para>
			As you may have noticed in the above statement, orders that have an order quantity of zero don't match any of the conditions. The engine does not insert such order events into any stream and the listener to the statement receives these unmatched events.
		</para>

		<para>
			By default the engine inserts into the first <literal>insert into</literal> stream without a <literal>where</literal> clause or for which the <literal>where</literal> clause condition matches. To change the default behavior and insert into all matching streams instead (including those without a <literal>where</literal> clause), add the <literal>output all</literal> keywords.
		</para>

		<para>
			The sample statement below shows the use of the <literal>output all</literal> keywords. The statement populates both the <literal>LargeOrders</literal> stream with large orders as well as the <literal>VIPCustomerOrders</literal> stream with orders for certain customers based on customer id:
		</para>
		<programlisting><![CDATA[on OrderEvent 
  insert into LargeOrders select * where orderQty >= 100
  insert into VIPCustomerOrders select * where customerId in (1001, 1002)
  output all]]></programlisting>
  
	  <para>
		  Since the <literal>output all</literal> keywords are present, the above statement inserts each order event into either both streams or only one stream or none of the streams, depending on order quantity and customer id of the order event. The statement delivers order events not inserted into any of the streams to the listeners and/or subscriber to the statement.
	  </para>

	  <para>
		  The following limitations apply to split-stream statements:
	  </para>	
		<orderedlist spacing="compact">
			<listitem>
				<para>
					Aggregation functions and the <literal>prev</literal> and <literal>prior</literal> operators are not available in conditions and the <literal>select</literal>-clause.
				</para>
			</listitem>
		</orderedlist>
		
		<sect2 xml:id="split_containedeventselect" revision="1">
			<title>Generating Marker Events for Contained Events</title>
			
			<para>
				When a trigger event contains properties that are themselves events, or more generally when your application needs to split the trigger event into multiple events,
				or to generate marker events (begin, end etc.) or process contained events in a defined order, you may specify a <literal>from</literal> clause.  
			</para>
			
			<para>
				The <literal>from</literal> clause is only allowed for the second and subsequent <literal>insert into</literal> and <literal>select</literal> clause pair.
				It specifies how the trigger event should get unpacked into individual events and is based on the 
				<xref linkend="epl-containedeventselect"/>.
			</para>
			
			<para>
				For example, assume there is an order event that contains order items:
			</para>
			
			<programlisting><![CDATA[create schema OrderItem(itemId string)]]></programlisting>
			<programlisting><![CDATA[create schema OrderEvent(orderId string, items OrderItem[])]]></programlisting>

			<para>
				We can tell the engine that, for each order event, it should process in the following order:
			</para>

			<orderedlist>
				<ns:listitem>
					<para>Process a single <literal>OrderBeginEvent</literal> that holds just the order id.</para>
				</ns:listitem>
				<ns:listitem>
					<para>Process all order items contained in an order event.</para>
				</ns:listitem>
				<ns:listitem>
					<para>Process a single <literal>OrderEndEvent</literal> that holds just the order id.</para>
				</ns:listitem>
			</orderedlist>

			<para>
				The EPL is:
			</para>

			<programlisting><![CDATA[on OrderEvent
  insert into OrderBeginEvent select orderId
  insert into OrderItemEvent select * from [select orderId, * from items]
  insert into OrderEndEvent select orderId
  output all]]></programlisting>

			<para>
				When an <literal>OrderEvent</literal> comes in, the engine first processes an <literal>OrderBeginEvent</literal>. The engine unpacks the order event and for each
				order item processes an <literal>OrderItemEvent</literal> containing the respective item. The engine last processes an <literal>OrderEndEvent</literal>.
			</para>
			
			<para>
				Such begin and end marker events are useful to initiate and terminate an analysis using context declaration, for example.
				The next two EPL statements declare a context and perform a simple count of order items per order:
			</para>

			<programlisting><![CDATA[create context OrderContext 
  initiated by OrderBeginEvent as obe
  terminated by OrderEndEvent(orderId = obe.orderId)]]></programlisting>
			<programlisting><![CDATA[context OrderContext select count(*) as orderItemCount from OrderItemEvent output when terminated]]></programlisting>
		</sect2>
	</sect1>

	<sect1 xml:id="variables_overview" revision="1">
		<title>Variables and Constants</title>
		
		<indexterm><primary>variable</primary></indexterm>
		<indexterm><primary>EPL</primary><secondary>variable</secondary></indexterm>

		<para>
			A <emphasis>variable</emphasis> is a scalar, object, event or set of aggregation values that is available for use in all statements including patterns. Variables can be used in an expression anywhere in a statement as well as in the <literal>output</literal> clause for output rate limiting.
		</para>
			
		<para>
			Variables must first be declared or configured before use, by defining each variable's type and name. Variables can be created via the <literal>create variable</literal> syntax or declared by runtime or static configuration. Variables can be assigned new values by using the <literal>on set</literal> syntax or via the <literal>setVariableValue</literal> methods on <literal>EPRuntime</literal>. The <literal>EPRuntime</literal> also provides method to read variable values.
		</para>
		
		<para>
		  A variable can be declared constant. A constant variable always has the initial value and cannot be assigned a new value. A constant variable can be used like any other variable and can be used wherever a constant is required. By declaring a variable constant you enable the Esper engine to 
		  optimize and perform query planning knowing that the variable value cannot change.
		</para>

		<para>
			When declaring a class-typed, event-typed or aggregation-typed variable you may read or set individual properties within the same variable.
		</para>

		<para>
			 The engine guarantees consistency and atomicity of variable reads and writes on the level of context partition (this is a soft guarantee, see below). Variables are optimized for fast read access and are also multithread-safe.
		</para>		
		
		<para>
			 When you associate a context to the variable then each context partition maintains its own variable value. See <xref linkend="context_variables"/> for more information.
		</para>		

		<para>
			 Variables can also be removed, at runtime, by destroying all referencing statements including the statement that created the variable, or by means of the runtime configuration API.
		</para>		

		<sect2 xml:id="variable_create" revision="1">
			<title>Creating Variables: The <literal>Create Variable</literal> Clause</title>

			<para>
				The <literal>create variable</literal> syntax creates a new variable by defining the variable type and name. In alternative to the syntax, variables can also be declared in the runtime and engine configuration options.
			</para>

			<para>
				The synopsis for creating a variable is as follows: 
			</para>
			<synopsis>create [constant] variable <emphasis>variable_type</emphasis> [[]] <emphasis>variable_name</emphasis> [ = <emphasis>assignment_expression</emphasis> ]</synopsis>
			
			<para>
			  Specify the optional <literal>constant</literal> keyword when the variable is a constant whose associated value cannot be altered. Your EPL design should prefer constant variables over non-constant variables.
			</para>
			
			<para>
				The <emphasis>variable_type</emphasis> can be any of the following:
			</para>
			<synopsis>variable_type
	:  string
	|  char 
	|  character
	|  bool 
	|  boolean
	|  byte
	|  short 
	|  int 
	|  integer 
	|  long 
	|  double
	|  float
	|  object
	|  <emphasis>enum_class</emphasis>
	|  <emphasis>class_name</emphasis>
	|  <emphasis>event_type_name</emphasis></synopsis>
								
			<para>
				Variable types can accept null values. The <literal>object</literal> type is for an untyped variable that can be assigned any value. You can provide a class name (use imports) or a fully-qualified class name to declare a variable of that Java class type including an enumeration class. You can also supply the name of an event type to declare a variable that holds an event of that type.
			</para>
						
			<para>
			   Append <literal>[]</literal> to the variable type to declare an array variable. A limitation is that if your variable type is an event type then array is not allowed (applies to variables only and not to named windows or tables). For arrays of primitives, specify <literal>[primitive]</literal>, for example <literal>int[primitive]</literal>.
			</para>

			<para>
				The <emphasis>variable_name</emphasis> is an identifier that names the variable. The variable name should not already be in use by another variable.
			</para>
				
			<para>				
				The <literal>assignment_expression</literal> is optional. Without an assignment expression the initial value for the variable is <literal>null</literal>. If present, it supplies the initial value for the variable.
			</para>

			<note>
				<para>
					When specifying a <emphasis>class_name</emphasis> or <emphasis>enum_class</emphasis> class name the class must have <literal>public</literal> visibility.
				</para>
			</note>

			<para>
				The <literal>EPStatement</literal> object of the <literal>create variable</literal> statement provides access to variable values. 
				The pull API methods <literal>iterator</literal> and <literal>safeIterator</literal> return the current variable value. 
				Listeners to the <literal>create variable</literal> statement subscribe to changes in variable value: the engine posts new and old value of the variable to all listeners when the variable value is updated by an <literal>on set</literal> statement.
			</para>

			<para>
				The example below creates a variable that provides a threshold value. The name of the variable is <literal>var_threshold</literal> and its type is <literal>long</literal>. The variable's initial value is <literal>null</literal> as no other value has been assigned:
			</para>
			<programlisting><![CDATA[create variable long var_threshold]]></programlisting>

			<para>
				This statement creates an integer-type variable named <literal>var_output_rate</literal> and initializes it to the value ten (10):
			</para>
			<programlisting><![CDATA[create variable integer var_output_rate = 10]]></programlisting>

			<para>
				The next statement declares a constant string-type variable:
			</para>
			<programlisting><![CDATA[create constant variable string const_filter_symbol = 'GE']]></programlisting>

			<para>
				In addition to creating a variable via the <literal>create variable</literal> syntax, the runtime and engine configuration API also allows adding variables. The next code snippet illustrates the
				use of the runtime configuration API to create a string-typed variable:
			</para>
			<programlisting><![CDATA[epService.getEPAdministrator().getConfiguration()
  .addVariable("myVar", String.class, "init value");]]></programlisting>
  
			<para>
				The following example declares a constant that is an array of string:
			</para>
			<programlisting><![CDATA[create constant variable string[] const_filters = {'GE', 'MSFT'}]]></programlisting>
			<para>
				The next example declares a constant that is an array of enumeration values. It assumes the <literal>Color</literal> enumeration class was imported:
			</para>
			<programlisting><![CDATA[create constant variable Color[] const_colors = {Color.RED, Color.BLUE}]]></programlisting>

			<para>
				For an array of primitive-type bytes, specify the <literal>primitive</literal> keyword in square brackets, as the next example shows:
			</para>
			<programlisting><![CDATA[create variable byte[primitive] mybytes = SomeClass.getBytes()]]></programlisting>

			<para>
				Use the <literal>new</literal> keyword to initialize object instances (the example assumes the package or class was imported):
			</para>
			<programlisting><![CDATA[create constant variable AtomicInteger cnt = new AtomicInteger(1)]]></programlisting>

			  <para>
				  The engine removes the variable if the statement that created the variable is destroyed and all statements that reference the variable are also destroyed. 
				  The <literal>getVariableNameUsedBy</literal> and the <literal>removeVariable</literal> methods, both part of the runtime <literal>ConfigurationOperations</literal> API, provide use information and can remove a variable. If the variable was added via configuration, it can only be removed via the configuration API.
			  </para>
		</sect2>

		<sect2 xml:id="variable_set" revision="1">
			<title>Setting Variable Values: The <literal>On Set</literal> Clause</title>

			<para>
				The <literal>on set</literal> statement assigns a new value to one or more variables when a triggering event arrives or a triggering pattern occurs. Use the <literal>setVariableValue</literal> methods on <literal>EPRuntime</literal> to assign variable values programmatically.
			</para>

			<para>
				The synopsis for setting variable values is: 
			</para>
			<synopsis>on <emphasis>event_type</emphasis>[<emphasis>(filter_criteria)</emphasis>] [as <emphasis>stream_name</emphasis>]
  set <emphasis>variable_name</emphasis> = <emphasis>expression</emphasis> [, <emphasis>variable_name</emphasis> = <emphasis>expression</emphasis> [,...]]
</synopsis>
								
			<para>
				The <emphasis>event_type</emphasis> is the name of the type of events that trigger the variable assignments. It is optionally followed by <emphasis>filter_criteria</emphasis> which are filter expressions to apply to arriving events. The optional <literal>as</literal> keyword can be used to assign an stream name. Patterns and named windows can also be specified in the <literal>on</literal> clause.
			</para>
			
			<para>
				The comma-separated list of variable names and expressions set the value of one or more variables. Subqueries may by part of expressions however aggregation functions and the <literal>prev</literal> or <literal>prior</literal> function may not be used in expressions.
			</para>

			<para>
				All new variable values are applied atomically: the changes to variable values by the <literal>on set</literal> statement 
				become visible to other statements all at the same time. No changes are visible to other processing threads until the <literal>on set</literal> statement completed processing, and at that time all changes become visible at once.
			</para>

			<para>
				The <literal>EPStatement</literal> object provides access to variable values. The pull API methods <literal>iterator</literal> and <literal>safeIterator</literal> return the current variable values for each of the variables set by the statement. 
				Listeners to the statement subscribe to changes in variable values: the engine posts new variable values of all variables to any listeners.
			</para>

			<para>
				In the following example, a variable by name <literal>var_output_rate</literal> has been declared previously. When a NewOutputRateEvent event arrives, the variable is updated to a new value supplied by the event property 'rate':
			</para>
			<programlisting><![CDATA[on NewOutputRateEvent set var_output_rate = rate]]></programlisting>

			<para>
				The next example shows two variables that are updated when a ThresholdUpdateEvent arrives:
			</para>
			<programlisting><![CDATA[on ThresholdUpdateEvent as t 
  set var_threshold_lower = t.lower,
      var_threshold_higher = t.higher]]></programlisting>

			<para>
				The sample statement shown next counts the number of pattern matches using a variable. The pattern looks for OrderEvent events that are followed by CancelEvent events for the same order id within 10 seconds of the OrderEvent:
			</para>
			<programlisting><![CDATA[on pattern[every a=OrderEvent -> (CancelEvent(orderId=a.orderId) where timer:within(10 sec))]
  set var_counter = var_counter + 1]]></programlisting>
		</sect2>

		<sect2 xml:id="variable_using" revision="1">
			<title>Using Variables</title>

			<para>
				A variable name can be used in any expression and can also occur in an output rate limiting clause. This section presents examples and discusses performance, consistency and atomicity attributes of variables.
			</para>

			<para>
				The next statement assumes that a variable named 'var_threshold' was created to hold a total price threshold value. 
				The statement outputs an event when the total price for a symbol is greater then the current threshold value:
			</para>
			<programlisting><![CDATA[select symbol, sum(price) from TickEvent 
group by symbol 
having sum(price) > var_threshold]]></programlisting>

			<para>
				This example uses a variable to dynamically change the output rate on-the-fly. The variable 'var_output_rate' holds the current rate at which the statement posts a current count to listeners:
			</para>
			<programlisting><![CDATA[select count(*) from TickEvent output every var_output_rate seconds]]></programlisting>
			
			<para>
				Variables are optimized towards high read frequency and lower write frequency. Variable reads do not incur locking overhead (99% of the time) while variable writes do incur locking overhead. 
			</para>
				
			<para>
				The engine softly guarantees consistency and atomicity of variables when your statement executes in response to an event or timer invocation. Variables acquire a stable value (implemented by versioning) when your statement starts executing in response to an event or timer invocation, and variables do not change value during execution. When one or more variable values are updated via <literal>on set</literal> statements, the changes to all updated variables become visible to statements as one unit and only when the <literal>on set</literal> statement completes successfully.
			</para>

			<para>
				The atomicity and consistency guarantee is a soft guarantee. If any of your application statements, in response to an event or timer invocation, execute for a time interval longer then 15 seconds (default interval length), then the engine may use current variable values after 15 seconds passed, rather then then-current variable values at the time the statement started executing in response to an event or timer invocation.				
			</para>

			<para>
				The length of the time interval that variable values are held stable for the duration of execution of a given statement is by default 15 seconds, but can be configured via engine default settings.
			</para>
		</sect2>

		<sect2 xml:id="variable_object" revision="1">
			<title>Object-Type Variables</title>

			<para>
				A variable of type <literal>object</literal> (or <literal>java.lang.Object</literal> via the API) can be assigned any value including null. When using an object-type variable in an expression, your statement may need to cast the
				value to the desired type.
			</para>
			
			<para>
			  The following sample EPL creates a variable by name <literal>varobj</literal> of type object:
			</para>		
			<programlisting><![CDATA[create variable object varobj]]></programlisting>
		</sect2>

		<sect2 xml:id="variable_eventtype" revision="1">
			<title>Class and Event-Type Variables</title>

			<para>
				The <literal>create variable</literal> syntax and the API accept a fully-qualified class name or alternatively the name of an event type. This is useful when you want a single variable to have multiple property values to read or set.
			</para>

			<para>
			  The next statement assumes that the event type <literal>PageHitEvent</literal> is declared:
			</para>		
			<programlisting><![CDATA[create variable PageHitEvent varPageHitZero]]></programlisting>

			<para>
			  These example statements show two ways of assigning to the variable:
			</para>		
			<programlisting><![CDATA[// You may assign the complete event
on PageHitEvent(ip='0.0.0.0') pagehit set varPageHitZero = pagehit]]></programlisting>
			<programlisting><![CDATA[// Or assign individual properties of the event
on PageHitEvent(ip='0.0.0.0') pagehit set varPageHitZero.userId = pagehit.userId]]></programlisting>

			<para>
			  Similarly statements may use properties of class or event-type variables as this example shows:
			</para>		
			<programlisting><![CDATA[select * from FirewallEvent(userId=varPageHitZero.userId)]]></programlisting>

			<para>
			  Instance method can also be invoked:
			</para>		
			<programlisting><![CDATA[create variable com.example.StateCheckerService stateChecker]]></programlisting>
			<programlisting><![CDATA[select * from TestEvent as e where stateChecker.checkState(e)]]></programlisting>
			
			<para>
				A variable that represents a service for calling instance methods could be initialized by calling a factory method. This example assumes the classes were added to imports:
			</para>
			<programlisting><![CDATA[create constant variable StateCheckerService stateChecker = StateCheckerServiceFactory.makeService()]]></programlisting>

			<para>
				You can add a variable via the configuration API; an example code snippet is next:
			</para>
			<programlisting><![CDATA[admin.getConfiguration().addVariable("stateChecker", StateCheckerService.class, StateCheckerServiceFactory.makeService(), true);]]></programlisting>

			<para>
				Application objects can also be passed via transient configuration information as described in <xref linkend="configuration-transientobjects"/>.
			</para>

			<note>
				When using non-constant class or event-type variables and when your EPL intends to set property values on the variable itself (i.e. <literal>set varPageHitZero.userId</literal>), please note the following requirements. In order for the engine to assign property values, the underlying event type must allow writing property values. If using JavaBean event classes the class must have setter methods and a default constructor. The underlying event type must also be copy-able i.e. implement <literal>Serializable</literal> or configure a copy method (only for non-constant variables and when setting property values).
			</note>			
		</sect2>
	</sect1>		

	<sect1 xml:id="create-exprglobal" revision="1">
		<title>Declaring Global Expressions, Aliases and Scripts: <emphasis>Create Expression</emphasis></title>
		
		<indexterm><primary>create expression</primary></indexterm>
		<indexterm><primary>expression</primary><secondary>declaring</secondary></indexterm>
		<indexterm><primary>script</primary><secondary>declaring</secondary></indexterm>
		<para>
			Your application can declare an expression or script using the <literal>create expression</literal> clause. Such expressions or scripts become available 
			globally to any EPL statement.
		</para>
		
		<para>
			The synopsis of the <literal>create expression</literal> syntax is:
		</para>
		<synopsis>create expression <emphasis>expression_or_script</emphasis></synopsis>
		
		<para>
		  Use the <literal>create expression</literal> keywords and append the expression or scripts. 
		</para>
		
		<para>
		  At the time your application creates the <literal>create expression</literal> statement the expression or script becomes globally visible.
		</para>

		<para>
		  At the time your application destroys the <literal>create expression</literal> statement the expression or script are no longer visible.
		  Existing statements that use the global expression or script are unaffected.
		</para>

		<para>
		  Expression aliases are the simplest means of sharing expressions and do not accept parameters. 
		  Expression declarations limit the expression scope to the parameters that are passed.
		</para>

		<para>
			The engine may cache declared expression result values and reuse cache values, see <xref linkend="config-engine-execution-declaredexprvaluecachesize"/>.
		</para>

		<sect2 xml:id="create-exprglobal-alias" revision="1">
			<title>Global Expression Aliases</title>
		
			<para>
				The syntax and additional examples for declaring an expression is outlined in <xref linkend="epl-syntax-expression-alias"/>,
				which discusses expression aliases that are visible within the same EPL statement i.e. visible locally only.
			</para>
						
			<para>
				When using the <literal>create expression</literal> syntax to declare an expression the engine remembers the expression alias and expression
				and allows the alias to be referenced in all other EPL statements.
			</para>
	
			<para>
				The below EPL declares a globally visible expression alias for an expression that computes the total of the mid-price which is the buy and sell price divided by two:
			</para>
			<programlisting><![CDATA[create expression totalMidPrice alias for { sum((buy + sell) / 2) }]]></programlisting>			
	
			<para>
				The next EPL returns mid-price for events for which the mid-price per symbol stays below 10:
			</para>
			<programlisting><![CDATA[select symbol, midPrice from MarketDataEvent group by symbol having midPrice < 10]]></programlisting>				
			
			<para>
			  The expression name must be unique among all other expression aliases and expression declarations.
			</para>
			
			<para>
			  Your application can provide an expression alias of the same name local to a given EPL statement as well as globally using <literal>create expression</literal>. 
			  The locally-provided expression alias overrides the global expression alias.
			</para>
			
			<para>
				The engine validates global expression aliases at the time your application creates a statement that references the alias. 
				When a statement references a global alias, the engine uses the that statement's local expression scope to validate the expression.
				Expression aliases can therefore be dynamically typed and type information does not need to be the same 
				for all statements that reference the expression alias.
			</para>
		</sect2>

		<sect2 xml:id="create-exprglobal-expr" revision="1">
			<title>Global Expression Declarations</title>
		
			<para>
				The syntax and additional examples for declaring an expression is outlined in <xref linkend="epl-syntax-expression-decl"/>,
				which discusses declaring expressions that are visible within the same EPL statement i.e. visible locally only.
			</para>
						
			<para>
				When using the <literal>create expression</literal> syntax to declare an expression the engine remembers the expression
				and allows the expression to be referenced in all other EPL statements.
			</para>
	
			<para>
				The below EPL declares a globally visible expression that computes a mid-price and that requires a single parameter:
			</para>
			<programlisting><![CDATA[create expression midPrice { in => (buy + sell) / 2 }]]></programlisting>			
	
			<para>
				The next EPL returns mid-price for each event:
			</para>
			<programlisting><![CDATA[select midPrice(md) from MarketDataEvent as md]]></programlisting>				
			
			<para>
			  The expression name must be unique for global expressions. It is not possible to declare the same global expression twice with the same name.
			</para>
			
			<para>
			  Your application can declare an expression of the same name local to a given EPL statement as well as globally using <literal>create expression</literal>. 
			  The locally-declared expression overrides the globally declared expression.
			</para>
			
			<para>
				The engine validates globally declared expressions at the time your application creates a statement that references the global expression. 
				When a statement references a global expression, the engine uses that statement's type information to validate the global expressions.
				Global expressions can therefore be dynamically typed and type information does not need to be the same 
				for all statements that reference the global expression.
			</para>

			<para>
				This example shows a sequence of EPL, that can be created in the order shown, and that demonstrates expression validation at time of referral:
			</para>
			<programlisting><![CDATA[create expression minPrice {(select min(price) from OrderWindow)}]]></programlisting>				
			<programlisting><![CDATA[create window OrderWindow#time(30) as OrderEvent]]></programlisting>				
			<programlisting><![CDATA[insert into OrderWindow select * from OrderEvent]]></programlisting>				
			<programlisting><![CDATA[// Validates and incorporates the declared global expression
select minPrice() as minprice from MarketData]]></programlisting>				
		</sect2>

		<sect2 xml:id="create-exprglobal-script" revision="1">
			<title>Global Scripts</title>
		
			<para>
				The syntax and additional examples for declaring scripts is outlined in <xref linkend="script"/>,
				which discusses declaring scripts that are visible within the same EPL statement i.e. visible locally only.
			</para>
	
			<para>
				When using the <literal>create expression</literal> syntax to declare a script the engine remembers the script
				and allows the script to be referenced in all other EPL statements.
			</para>
	
			<para>
				The below EPL declares a globally visible script in the JavaScript dialect that computes a mid-price:
			</para>
			<programlisting><![CDATA[create expression midPrice(buy, sell) [ (buy + sell) / 2 ]]]></programlisting>			
	
			<para>
				The next EPL returns mid-price for each event:
			</para>
			<programlisting><![CDATA[select midPrice(buy, sell) from MarketDataEvent]]></programlisting>				
			
			<para>
				The engine validates globally declared scripts at the time your application creates a statement that references the global script. 
				When a statement references a global script, the engine uses that statement's type information to determine parameter types.
				Global scripts can therefore be dynamically typed and type information does not need to be the same 
				for all statements that reference the global script.
			</para>

			<para>
			  The script name in combination with the number of parameters must be unique for global scripts. 
			  It is not possible to declare the same global script twice with the same name and number of parameters.
			</para>
			
			<para>
			  Your application can declare a script of the same name and number of parameters that is local to a given EPL statement as well as globally using <literal>create expression</literal>. 
			  The locally-declared script overrides the globally declared script. 
			</para>

		</sect2>
	</sect1>

	<sect1 xml:id="epl-containedeventselect">
	   <title>Contained-Event Selection</title>
	   
		<para>
			Contained-event selection is for use when an event contains properties that are themselves events, or more generally when your application needs to split an event into multiple events. One example is when application events are coarse-grained structures and you need to perform bulk operations on the rows of the property graph in an event.
		</para>

		<para>
			Use the contained-event selection syntax in a filter expression such as in a pattern, <literal>from</literal> clause, subselect, on-select and on-delete. This section provides the synopsis and examples.
		</para>

		<para>
			To review, in the <literal>from</literal> clause a <emphasis>contained_selection</emphasis> may appear after the event stream name and filter criteria, and before any data windows.
		</para>

		<para>
			The synopsis for <emphasis>contained_selection</emphasis> is as follows:
		</para>
		<synopsis>[select <emphasis>select_expressions</emphasis> from] 
  <emphasis>contained_expression</emphasis> [@type(<emphasis>eventtype_name</emphasis>)] [as <emphasis>alias_name</emphasis>]
  [where <emphasis>filter_expression</emphasis>]</synopsis>

		<para>
			The <literal>select</literal> clause and <emphasis>select_expressions</emphasis> are optional and may be used to select specific properties of contained events.
		</para>

		<para>
			The <emphasis>contained_expression</emphasis> is required and returns individual events. The expression can, for example, be an event property name that returns an event fragment, i.e. a property that can itself be represented as an event by the underlying event representation. The expression can also be any other expression such as a single-row function or a script that returns either an array or a <literal>java.util.Collection</literal> of events. Simple values such as integer or string are not fragments but can be used as well as described below.
		</para>
		
		<para>
		    Provide the <literal>@type(name)</literal> annotation after the contained expression to name the event type of events returned by the expression. The annotation is optional and not needed when the contained-expression is an event property that returns a class or other event fragment.
		</para>
		
		<para>
			The <emphasis>alias_name</emphasis> can be provided to assign a name to the expression result value rows.
		</para>

		<para>
			The <literal>where</literal> clause and <emphasis>filter_expression</emphasis> is optional and may be used to filter out properties.
		</para>

		<para>
			As an example event, consider a media order. A media order consists of order items as well as product descriptions. A media order event can be represented as an object graph (POJO event representation), or a structure of nested Maps (Map event representation) or a XML document (XML DOM or Axiom event representation) or other custom plug-in event representation.
		</para>

		<para>
			To illustrate, a sample media order event in XML event representation is shown below. Also, a XML event type can optionally be strongly-typed with an explicit XML XSD schema that is not shown here. Note that Map and POJO representation can be considered equivalent for the purpose of this example.
		</para>

		<para>
			Let us now assume the event type <literal>MediaOrder</literal> as being represented by the root node <literal>&lt;mediaorder&gt;</literal> of such XML snip:
		</para>

	<programlisting><![CDATA[<mediaorder>
  <orderId>PO200901</orderId>
  <items>
    <item>
      <itemId>100001</itemId>
      <productId>B001</productId>
      <amount>10</amount>
      <price>11.95</price>
    </item>
  </items>
  <books>
    <book>
      <bookId>B001</bookId>
      <author>Heinlein</author>
      <review>
        <reviewId>1</reviewId>
        <comment>best book ever</comment>
      </review>
    </book>
    <book>
      <bookId>B002</bookId>
      <author>Isaac Asimov</author>
    </book>
  </books>
</mediaorder>]]></programlisting>

		<para>
			The next query utilizes the contained-event selection syntax to return each book:
		</para>
		<programlisting><![CDATA[select * from MediaOrder[books.book]]]></programlisting>

		<para>
			The result of the above query is one event per book. Output events contain only the book properties and not any of the mediaorder-level properties.
		</para>

		<para>
			Note that, when using listeners, the engine delivers multiple results in one invocation of each listener. Therefore listeners to the above statement can expect a single invocation passing all book events within one media order event as an array.
		</para>
		
		<para>
			 To better illustrate the position of the contained-event selection syntax in a statement, consider the next two queries:
		</para>
		<programlisting><![CDATA[select * from MediaOrder(orderId='PO200901')[books.book]]]></programlisting>

		<para>
			The above query the returns each book only for media orders with a given order id. This query illustrates a contained-event selection and a data window: 
		</para>
		<programlisting><![CDATA[select count(*) from MediaOrder[books.book]#unique(bookId)]]></programlisting>
		
		<para>
			The sample above counts each book unique by book id.
		</para>

		<para>
			Contained-event selection can be staggered. When staggering multiple contained-event selections the staggered contained-event selection is relative to its parent.
		</para>

		<para>
			This example demonstrates staggering contained-event selections by selecting each review of each book:
		</para>
		<programlisting><![CDATA[select * from MediaOrder[books.book][review]]]></programlisting>

		<para>
			Listeners to the query above receive a row for each review of each book. Output events contain only the review properties and not the book or media order properties.
		</para>
		
		<para>
			The following is not valid:
		</para>
		<programlisting><![CDATA[// not valid
select * from MediaOrder[books.book.review]]]></programlisting>

		<para>
			The <literal>book</literal> property in an indexed property (an array or collection) and thereby requires an index in order to determine which book to use. The expression <literal>books.book[1].review</literal> is valid and means all reviews of the second (index 1) book.
		</para>
		
		<para>
			The contained-event selection syntax is part of the filter expression and may therefore occur in patterns and anywhere a filter expression is valid. 
		</para>

		<para>
			A pattern example is below. The example assumes that a <literal>Cancel</literal> event type has been defined that also has an <literal>orderId</literal> property:
		</para>
		<programlisting>select * from pattern [c=Cancel -&gt; books=MediaOrder(orderId = c.orderId)[books.book] ]</programlisting>
	
		<para>
			When used in a pattern, a filter with a contained-event selection returns an array of events, similar to the match-until clause in patterns. The above statement returns, in the <literal>books</literal> property, an
			array of book events.
		</para>

		<sect2 xml:id="epl-containedeventselect-select" revision="1">
			<title>Select-Clause in a Contained-Event Selection</title>

			<para>
				 The optional <literal>select</literal> clause provides control over which fields are available in output events. The expressions in the select-clause apply only to the properties available underneath the property in the <literal>from</literal> clause, and the properties of the enclosing event.
			</para>

			<para>
				 When no <literal>select</literal> is specified, only the properties underneath the selected property are available in output events.
			</para>

			<para>
				 In summary, the <literal>select</literal> clause may contain:
			</para>

			<orderedlist spacing="compact">
				<listitem>
					<para>
						Any expressions, wherein properties are resolved relative to the property in the <literal>from</literal> clause.
					</para>
				</listitem>
				<listitem>
					<para>
						Use the wildcard (<literal>*</literal>) to provide all properties that exist under the property in the <literal>from</literal> clause.
					</para>
				</listitem>
				<listitem>
					<para>
						Use the <emphasis>alias_name</emphasis><literal>.*</literal> syntax to provide all properties that exist under a property in the <literal>from</literal> clause.
					</para>
				</listitem>
			</orderedlist>

			<para>
				The next query's <literal>select</literal> clause selects each review for each book, and the order id as well as the book id of each book:
			</para>
			<programlisting><![CDATA[select * from MediaOrder[select orderId, bookId from books.book][select * from review]
// ... equivalent to ...
select * from MediaOrder[select orderId, bookId from books.book][review]]]]></programlisting>
	
			<para>
				Listeners to the statement above receive an event for each review of each book. Each output event has all properties of the review row, and in addition the <literal>bookId</literal> of each book and the <literal>orderId</literal> of the order.
				Thus <literal>bookId</literal> and <literal>orderId</literal> are found in each result event, duplicated when there are multiple reviews per book and order.
			</para>

			<para>
				 The above query uses wildcard <literal>(*)</literal> to select all properties from reviews. As has been discussed as part of the <literal>select</literal> clause, the wildcard (<literal>*</literal>) and <emphasis>property_alias</emphasis><literal>.*</literal> do not copy properties for performance reasons. The wildcard syntax instead specifies the underlying type, and additional properties are added onto that underlying type if required. Only one wildcard (<literal>*</literal>) and <emphasis>property_alias</emphasis><literal>.*</literal> (unless used with a column rename) may therefore occur in the <literal>select</literal> clause list of expressions.
			</para>

			<para>
				All the following queries produce an output event for each review of each book. The next sample queries illustrate the options available to control the fields of output events.
			</para>

			<para>
				The output events produced by the next query have all properties of each review and no other properties available:
			</para>
			<programlisting><![CDATA[select * from MediaOrder[books.book][review]]]></programlisting>

			<para>
				The following query is not a valid query, since the order id and book id are not part of the contained-event selection:
			</para>
			<programlisting><![CDATA[// Invalid select-clause: orderId and bookId not produced.
select orderId, bookId from MediaOrder[books.book][review]]]></programlisting>

			<para>
				This query is valid. Note that output events carry only the <literal>orderId</literal> and <literal>bookId</literal> properties and no other data:
			</para>
			<programlisting><![CDATA[select orderId, bookId from MediaOrder[books.book][select orderId, bookId from review]
//... equivalent to ...
select * from MediaOrder[select orderId, bookId from books.book][review]]]></programlisting>

			<para>
				This variation produces output events that have all properties of each book and only <literal>reviewId</literal> and <literal>comment</literal> for each review:
			</para>
			<programlisting><![CDATA[select * from MediaOrder[select * from books.book][select reviewId, comment from review]
// ... equivalent to ...
select * from MediaOrder[books.book as book][select book.*, reviewId, comment from review]]]></programlisting>
	
			<para>
				The output events of the next EPL have all properties of the order and only <literal>bookId</literal> and <literal>reviewId</literal> for each review:
			</para>
			<programlisting><![CDATA[select * from MediaOrder[books.book as book]
    [select mediaOrder.*, bookId, reviewId from review] as mediaOrder]]></programlisting>

			<para>
				This EPL produces output events with 3 columns: a column named <literal>mediaOrder</literal> that is the order itself, a column named <literal>book</literal> for each book and a column named <literal>review</literal> that holds each review:
			</para>
			<programlisting><![CDATA[insert into ReviewStream select * from MediaOrder[books.book as book]
  [select mo.* as mediaOrder, book.* as book, review.* as review from review as review] as mo]]></programlisting>
			<programlisting><![CDATA[// .. and a sample consumer of ReviewStream...
select mediaOrder.orderId, book.bookId, review.reviewId from ReviewStream]]></programlisting>

			<para>
				Please note these limitations:
			</para>
			<orderedlist spacing="compact">
				<listitem>
					<para>
						Sub-selects, aggregation functions and the <literal>prev</literal> and <literal>prior</literal> operators are not available in contained-event selection.
					</para>
				</listitem>
				<listitem>
					<para>
						Expressions in the <literal>select</literal> and <literal>where</literal> clause of a contained-event selection can only reference properties relative to the current event and property.
					</para>
				</listitem>
			</orderedlist>

		</sect2>
	
		<sect2 xml:id="epl-containedeventselect-where" revision="1">
			<title>Where Clause in a Contained-Event Selection</title>
			<para>
				The optional <literal>where</literal> clause may be used to filter out properties at the same level that the where-clause occurs.
			</para>
			
			<para>
				The properties in the filter expression must be relative to the property in the <literal>from</literal> clause or the enclosing event.
			</para>

			<para>
				This query outputs all books with a given author:
			</para>
			<programlisting><![CDATA[select * from MediaOrder[books.book where author = 'Heinlein']]]></programlisting>
	
			<para>
				This query outputs each review of each book where a review comment contains the word 'good':
			</para>
			<programlisting><![CDATA[select * from MediaOrder[books.book][review where comment like 'good']]]></programlisting>
	
		</sect2>

		<sect2 xml:id="epl-containedeventselect-join" revision="1">
			<title>Contained-Event Selection and Joins</title>
			<para>
				This section discusses contained-event selection in joins.
			</para>

			<para>
				When joining within the same event it is not required to specify a data window. Recall, in a join or outer join there must be a data window specified that defines the subset of events available to be joined. For self-joins, no data window is required and the join
				executes against the data returned by the same event.
			</para>
			
			<para>
				This query inner-joins items to books where book id matches the product id:
			</para>
			<programlisting><![CDATA[select book.bookId, item.itemId 
from MediaOrder[books.book] as book, 
      MediaOrder[items.item] as item 
where productId = bookId]]></programlisting>

			<para>
				Query results for the above query when sending the media order event as shown earlier are:
			</para>

			<informaltable frame="topbot">
				<tgroup cols="2">
					<colspec colwidth="1*"/>
					<colspec colwidth="1*"/>
					<thead>
						<row>
							<entry>book.bookId</entry>
							<entry>item.itemId</entry>
						</row>
					</thead>
					<tbody>
						<row>
							<entry>B001</entry>
							<entry>100001</entry>
						</row>
					</tbody>
				</tgroup>				
			</informaltable>

			<para>
				The next example query is a left outer join. It returns all books and their items, and for books without item it returns the book and a <literal>null</literal> value:
			</para>

			<programlisting><![CDATA[select book.bookId, item.itemId 
from MediaOrder[books.book] as book 
  left outer join 
    MediaOrder[items.item] as item 
  on productId = bookId]]></programlisting>

			<para>
				Query results for the above query when sending the media order event as shown earlier are:
			</para>

			<informaltable frame="topbot">
				<tgroup cols="2">
					<colspec colwidth="1*"/>
					<colspec colwidth="1*"/>
					<thead>
						<row>
							<entry>book.bookId</entry>
							<entry>item.itemId</entry>
						</row>
					</thead>
					<tbody>
						<row>
							<entry>B001</entry>
							<entry>100001</entry>
						</row>
						<row>
							<entry>B002</entry>
							<entry>null</entry>
						</row>
					</tbody>
				</tgroup>				
			</informaltable>

			<para>
				A full outer join combines the results of both left and right outer joins. The joined table will contain all records from both tables, and fill in <literal>null</literal> values for missing matches on either side.
			</para>

			<para>
				This example query is a full outer join, returning all books as well as all items, and filling in <literal>null</literal> values for book id or item id if no match is found:
			</para>
			
			<programlisting><![CDATA[select orderId, book.bookId,item.itemId 
from MediaOrder[books.book] as book 
  full outer join 
     MediaOrder[select orderId, * from items.item] as item 
  on productId = bookId 
order by bookId, item.itemId asc
]]></programlisting>

			<para>
				As in all other continuous queries, aggregation results are cumulative from the time the statement was created.
			</para>
			
			<para>
				The following query counts the cumulative number of items in which the product id matches a book id:
			</para>

			<programlisting><![CDATA[select count(*) 
from MediaOrder[books.book] as book, 
      MediaOrder[items.item] as item 
where productId = bookId]]></programlisting>

			<para>
				The <literal>unidirectional</literal> keyword in a join indicates to the query engine that aggregation state is not cumulative. The next query counts the number of items in which the product id matches a book id for each event:
			</para>
			
			<programlisting><![CDATA[select count(*) 
from MediaOrder[books.book] as book unidirectional, 
      MediaOrder[items.item] as item 
where productId = bookId]]></programlisting>

		</sect2>

		<sect2 xml:id="epl-containedeventselect-pojoexample" revision="1">
			<title>Sentence and Word Example</title>

			<para>
				The next example splits an event representing a sentence into multiple events in which each event represents a word. It represents all events and the logic to split events into contained events as Java code. 
				The next chapter has additional examples that use Map-type events and put contained-event logic into a separate expression or script.
			</para>

			<para>
				The sentence event in this example is represented by a class declared as follows:
			</para>
			
			<programlisting><![CDATA[public class SentenceEvent {
  private final String sentence;

  public SentenceEvent(String sentence) {
    this.sentence = sentence;
  }

  public WordEvent[] getWords() {
    String[] split = sentence.split(" ");
    WordEvent[] words = new WordEvent[split.length];
    for (int i = 0; i < split.length; i++) {
      words[i] = new WordEvent(split[i]);
    }
    return words;
  }
}]]></programlisting>

			<para>
				The sentence event as above provides an event property <literal>words</literal> that returns each word event.
			</para>

			<para>
				The declaration of word event is also a class:
			</para>

			<programlisting><![CDATA[public class WordEvent {
  private final String word;

  public WordEvent(String word) {
    this.word = word;
  }

  public String getWord() {
    return word;
  }
}]]></programlisting>

			<para>
				The EPL statement to populate a stream of words from a sentence event is:
			</para>
			<programlisting><![CDATA[insert into WordStream select * from SentenceEvent[words]]]></programlisting>

			<para>
				Finally, the API call to send a sentence event to the engine is shown here:
			</para>
			<programlisting><![CDATA[epService.getEPRuntime().sendEvent(new SentenceEvent("Hello Word Contained Events"));]]></programlisting>
		</sect2>		
		
		<sect2 xml:id="epl-containedeventselect-examples" revision="1">
			<title>More Examples</title>
			
			<para>
			  The examples herein are not based on the POJO events of the prior example. They are meant to demonstrate different types of contained-event expressions and the use of <literal>@type(</literal><emphasis>type_name</emphasis><literal>)</literal> to identify the event type of the return values of the contained-event expression.
			</para>
			
			<para>
			   The example first defines a few sample event types:
			</para>
			<programlisting><![CDATA[create schema SentenceEvent(sentence String)]]></programlisting>
			<programlisting><![CDATA[create schema WordEvent(word String)]]></programlisting>
			<programlisting><![CDATA[create schema CharacterEvent(char String)]]></programlisting>

			<para>
			   The following EPL assumes that your application defined a plug-in single-row function by name <literal>splitSentence</literal> that returns an array of Map, producting output events that are <literal>WordEvent</literal> events:
			</para>
			<programlisting><![CDATA[insert into WordStream select * from SentenceEvent[splitSentence(sentence)@type(WordEvent)]]]></programlisting>
			
			<para>
			   The example EPL shown next invokes a JavaScript function which returns some events of type <literal>WordEvent</literal>:
			</para>
			<programlisting><![CDATA[expression Collection js:splitSentenceJS(sentence) [
  var CollectionsClazz = Java.type('java.util.Collections');
  var words = new java.util.ArrayList();
  words.add(CollectionsClazz.singletonMap('word', 'wordOne'));
  words.add(CollectionsClazz.singletonMap('word', 'wordTwo'));
  words;
]
select * from SentenceEvent[splitSentenceJS(sentence)@type(WordEvent)]]]></programlisting>

			<para>
			   In the next example the sentence event first gets split into words and then each word event gets split into character events via an additional <literal>splitWord</literal> single-row function,
			   producing events of type <literal>CharacterEvent</literal>:
			</para>
			<programlisting><![CDATA[select * from SentenceEvent
  [splitSentence(sentence)@type(WordEvent)]
  [splitWord(word)@type(CharacterEvent)]]]></programlisting>
		</sect2>		
		
		<sect2 xml:id="epl-containedeventselect-arrayofprimitive" revision="1">
			<title>Contained Expression Returning an Array of Property Values</title>

			<para>
				Your <emphasis>contained_expression</emphasis> may return an array of property values such as an array of integer or string values.
				In this case you must specify a <literal>@type(name)</literal> annotation and provide an event type name that declares a single column with a type that matches the array component type.
			</para>

			<programlisting><![CDATA[create schema IdContainer(id int)]]></programlisting>
			<programlisting><![CDATA[create schema MyEvent(ids int[])]]></programlisting>
			<programlisting><![CDATA[select * from MyEvent[ids@type(IdContainer)]]]></programlisting>

			<para>
				This example declares a named window and uses on-delete:
			</para>
			<programlisting><![CDATA[create window MyWindow#keepall (id int)]]></programlisting>
			<programlisting><![CDATA[on MyEvent[ids@type(IdContainer)] as my_ids 
delete from MyWindow my_window 
where my_ids.id = my_window.id]]></programlisting>
		</sect2>		

		<sect2 xml:id="epl-containedeventselect-arrayofevent" revision="1">
			<title>Contained Expression Returning an Array of EventBean</title>

			<para>
				Your <emphasis>contained_expression</emphasis> may return an array of <literal>EventBean</literal> instances.
				This is handy when the expression itself must decide the type of each event to return.
			</para>

			<para>
				For example:
			</para>

			<programlisting><![CDATA[create schema BaseEvent();]]></programlisting>
			<programlisting><![CDATA[create schema AEvent(pa string) inherits BaseEvent;]]></programlisting>
			<programlisting><![CDATA[create schema BEvent(pb string) inherits BaseEvent;]]></programlisting>
			<programlisting><![CDATA[create schema ValueEvent(value string);]]></programlisting>
			<programlisting><![CDATA[select * from ValueEvent[mySplitFunction(value) @type(BaseEvent)]]]></programlisting>

			<para>
				Then declare <literal>mySplitFunction</literal> returning an array of events, such as:
			</para>
			<programlisting><![CDATA[public static EventBean[] mySplitFunction(String value, EPLMethodInvocationContext context) {
  EventBean[] events = new EventBean[1];
  if (value.startsWith("A")) {
    events[0] =  context.getEventBeanService().adapterForMap(Collections.singletonMap("pa", value), "AEvent");
  }
  else {
    events[0] =  context.getEventBeanService().adapterForMap(Collections.singletonMap("pb", value), "BEvent");
  }
  return events;
}]]></programlisting>
		</sect2>		

		<sect2 xml:id="epl-containedeventselect-beginend" revision="1">
			<title>Generating Marker Events Such as a Begin and End Event</title>

			<para>
				The syntax for splitting and duplicating streams can be used to generate marker events.
				Please see <xref linkend="split_containedeventselect"/> for more information.
			</para>
		</sect2>		

		<sect2 xml:id="epl-containedeventselect-restrictions" >
			<title>Contained-Event Limitations</title>
			<para>
				The following restrictions apply to contained-event selection:
			</para>        
			<itemizedlist spacing="compact">
				<listitem>
					<para>
						When selecting contained events from a named window in a join, the stream must be marked as <literal>unidirectional</literal>.
					</para>
				</listitem>
				<listitem>
					<para>
						Selecting contained events from a named window in a correlated subquery is not allowed. 
					</para>
				</listitem>
			</itemizedlist>
		</sect2>
	</sect1>
	
	<sect1 xml:id="epl-update">
	   <title>Updating an Insert Stream: The <literal>Update IStream</literal> Clause</title>
	   
		<para>
			The <literal>update istream</literal> statement allows declarative modification of event properties of events entering a stream. Update is a pre-processing step to each new event, modifying an event before the event applies to any statements.
		</para>

		<para>
			The synopsis of <literal>update istream</literal> is as follows:
		</para>

		<synopsis>update istream <emphasis>event_type</emphasis> [as <emphasis>stream_name</emphasis>]
  set <emphasis>property_name</emphasis> = <emphasis>set_expression</emphasis> [, <emphasis>property_name</emphasis> = <emphasis>set_expression</emphasis>] [,...]
  [where <emphasis>where_expression</emphasis>]
</synopsis>

		<para>
			The <emphasis>event_type</emphasis> is the name of the type of events that the <literal>update</literal> applies to.
			The optional <literal>as</literal> keyword can be used to assign a name to the event type for use with subqueries, for example.
			Following the <literal>set</literal> keyword is a comma-separated list of property names and expressions that provide the event properties to change and values to set.
		</para>

		<para>
			The optional <literal>where</literal> clause and expression can be used to filter out events to which to apply updates.
		</para>
		
		<para>
			Listeners to an <literal>update</literal> statement receive the updated event in the insert stream (new data) and the event prior to the update in the remove stream (old data). Note that if there are multiple update statements that all apply to the same event then the engine will ensure that the output events delivered to listeners or subscribers are consistent with the then-current updated properties of the event (if necessary making event copies, as described below, in the case that listeners are attached to update statements). Iterating over an update statement returns no events.
		</para>

		<para>
			As an example, the below statement assumes an <literal>AlertEvent</literal> event type that has properties named <literal>severity</literal> and <literal>reason</literal>:
		</para>
		
		<programlisting><![CDATA[update istream AlertEvent 
  set severity = 'High'
  where severity = 'Medium' and reason like '%withdrawal limit%']]></programlisting>

		<para>
			The statement above changes the value of the <literal>severity</literal> property to "High" for <literal>AlertEvent</literal> events that have a medium severity and contain a specific reason text.
		</para>

		<para>
			Update statements apply the changes to event properties before other statements receive the event(s) for processing, e.g. "<literal>select * from AlertEvent</literal>" receives the updated <literal>AlertEvent</literal>. This is true regardless of the order in which your application creates statements.
		</para>

		<para>
			When multiple update statements apply to the same event, the engine executes updates in the order in which update statements are created. We recommend the <literal>@Priority</literal> EPL annotation to define a deterministic order of processing updates, especially in the case where update statements get created and destroyed dynamically or multiple update statements update the same fields. The update statement with the highest <literal>@Priority</literal> value applies last.
		</para>

		<para>
			The <literal>update</literal> clause can be used on streams populated via <literal>insert into</literal>, as this example utilizing a pattern demonstrates:
		</para>		
		<programlisting><![CDATA[insert into DoubleWithdrawalStream 
select a.id, b.id, a.account as account, 0 as minimum 
from pattern [a=Withdrawal -> b=Withdrawal(id = a.id)]]]></programlisting>
		<programlisting><![CDATA[update istream DoubleWithdrawalStream set minimum = 1000 where account in (10002, 10003)]]></programlisting>

		<para>
			When using <literal>update istream</literal> with named windows, any changes to event properties apply before an event enters the named window.
			The <literal>update istream</literal> is not available for tables.
		</para>

		<para>
			Consider the next example (shown here with statement names in @Name EPL annotation, multiple EPL statements):
		</para>		
		<programlisting><![CDATA[@Name("CreateWindow") create window MyWindow#time(30 sec) as AlertEvent

@Name("UpdateStream") update istream MyWindow set severity = 'Low' where reason = '%out of paper%'

@Name("InsertWindow") insert into MyWindow select * from AlertEvent

@Name("SelectWindow") select * from MyWindow]]></programlisting>

		<para>
			The <literal>UpdateStream</literal> statement specifies an <literal>update</literal> clause that applies to all events entering the named window. Note that <literal>update</literal> does not apply to events already in the named window at the time an application creates the <literal>UpdateStream</literal> statement, it only applies to new events entering the named window (after an application created the <literal>update</literal> statement).
		</para>

		<para>
			Therefore, in the above example listeners to the <literal>SelectWindow</literal> statement as well as the <literal>CreateWindow</literal> statement receive the updated event, 
			while listeners to the <literal>InsertWindow</literal> statement receive the original <literal>AlertEvent</literal> event (and not the updated event).
		</para>

		<para>
			Subqueries can also be used in all expressions including the optional <literal>where</literal> clause.
		</para>		

		<para>
			This example demonstrates a correlated subquery in an assignment expression and also demonstrates the optional <literal>as</literal> keyword. It assigns the <literal>phone</literal> property of an <literal>AlertEvent</literal> event a new value based on the lookup within all unique <literal>PhoneEvent</literal> events (according to an <literal>empid</literal> property) correlating the <literal>AlertEvent</literal> property <literal>reporter</literal> with the <literal>empid</literal> property of <literal>PhoneEvent</literal>:
		</para>		
		<programlisting><![CDATA[update istream AlertEvent as ae
  set phone = 
    (select phone from PhoneEvent#unique(empid) where empid = ae.reporter)]]></programlisting>
		
		<para>
			When updating indexed properties use the syntax <emphasis>propertyName</emphasis><literal>[</literal><emphasis>index</emphasis><literal>] = </literal><emphasis>value</emphasis> with the index value being an integer number. 
			When updating mapped properties use the syntax <emphasis>propertyName</emphasis><literal>(</literal><emphasis>key</emphasis><literal>) = </literal><emphasis>value</emphasis> with the key being a string value.
		</para>

		<para>
			When using <literal>update</literal>, please note these limitations:
		</para>
		
		<orderedlist spacing="compact">
			<listitem>
				<para>
					Expressions may not use aggregation functions.
				</para>
			</listitem>
			<listitem>
				<para>
					The <literal>prev</literal> and <literal>prior</literal> functions may not be used.
				</para>
			</listitem>
			<listitem>
				<para>
					For underlying event representations that are Java objects, a event object class must implement the <literal>java.io.Serializable</literal> interface as discussed below.
				</para>
			</listitem>
			<listitem>
				<para>
					When using an XML underlying event type, event properties in the XML document representation are not available for update.
				</para>
			</listitem>
			<listitem>
				<para>
					Nested properties are not supported for update. Revision event types and variant streams may also not be updated.
				</para>
			</listitem>
		</orderedlist>
		
		<sect2 xml:id="epl-update-object" revision="1">
			<title>Immutability and Updates</title>
	
			<para>
				When updating event objects the engine maintains consistency across statements. The engine ensures that an update to an event does not impact the results of statements that look for or retain the original un-updated event. 
				As a result the engine may need to copy an event object to maintain consistency.
			</para>

			<para>
				In the case your application utilizes Java objects as the underlying event representation and an <literal>update</literal> statement updates properties on an object, then in order to maintain consistency across statements 
				it is necessary for the engine to copy the object before changing properties (and thus not change the original object). 
			</para>

			<para>
				For Java application objects, the copy operation is implemented by serialization. Your event object must therefore implement the <literal>java.io.Serializable</literal> interface to become eligible for update. As an alternative to serialization, you may instead configure a copy method as part of the event type configuration via <literal>ConfigurationEventTypeLegacy</literal>.
			</para>			
		</sect2>
	</sect1>
	
	<sect1 xml:id="epl-grouped-delivery" revision="1">
		<title>Controlling Event Delivery : The <literal>For</literal> Clause</title>
		
		<para>
			The engine delivers all result events of a given statement to the statement's listeners and subscriber (if any) in a single invocation of each listener and subscriber's <literal>update</literal> method passing an array of result events. For example, a statement using a time-batch window may provide many result events after a time period passes, a pattern may provide multiple matching events or in a join the join cardinality could be multiple rows.
		</para>
		
		<para>
			For statements that typically post multiple result events to listeners the <literal>for</literal> keyword controls the number of invocations of the engine to listeners and subscribers and the subset of all result events delivered by each invocation. 
			This can be useful when your application listener or subscriber code expects multiple invocations or expects that invocations only receive events that belong together by some additional criteria.
		</para>

		<para>
			The <literal>for</literal> keyword is a reserved keyword. It is followed by either the <literal>grouped_delivery</literal> keyword for grouped delivery or the <literal>discrete_delivery</literal> keyword for discrete delivery. The <literal>for</literal> clause is valid after any EPL select statement.
		</para>

		<para>
			The synopsis for grouped delivery is as follows:
		</para>
		<synopsis>... for grouped_delivery (<emphasis>group_expression</emphasis> [, <emphasis>group_expression</emphasis>] [,...])</synopsis>
		
		<para>
			The <emphasis>group_expression</emphasis> expression list provides one or more expressions to apply to result events. The engine invokes listeners and subscribers once for each distinct set of values returned by <emphasis>group_expression</emphasis> expressions passing only the events for that group.
		</para>

		<para>
			The synopsis for discrete delivery is as follows:
		</para>
		<synopsis>... for discrete_delivery</synopsis>

		<para>
			With discrete delivery the engine invokes listeners and subscribers once for each result event passing a single result event in each invocation.
		</para>

		<para>
			Consider the following example without <literal>for</literal>-clause. The time batch data window collects RFIDEvent events for 10 seconds and posts an array of result events:
		</para>
		<programlisting><![CDATA[select * from RFIDEvent#time_batch(10 sec)]]></programlisting>
		
		<para>
			Let's consider an example event sequence as follows:
		</para>
		<table frame="topbot">
			<title>Sample Sequence of Events for <literal>For</literal> Keyword</title>
			<tgroup cols="1">
				<colspec colwidth="1*"/>
				<thead>
					<row>
						<entry>Event</entry>
					</row>
				</thead>
				<tbody>
					<row>
						<entry>RFIDEvent(id:1, zone:'A')</entry>
					</row>
					<row>
						<entry>RFIDEvent(id:2, zone:'B')</entry>
					</row>
					<row>
						<entry>RFIDEvent(id:3, zone:'A')</entry>
					</row>
				</tbody>
			</tgroup>
		</table> 
		
		<para>
			Without <literal>for</literal>-clause and after the 10-second time period passes, the engine delivers an array of 3 events in a single invocation to listeners and the subscriber.
		</para>

		<para>
			The next example specifies the <literal>for</literal>-clause and grouped delivery by zone:
		</para>
		<programlisting><![CDATA[select * from RFIDEvent#time_batch(10 sec) for grouped_delivery (zone)]]></programlisting>

		<para>
			With grouped delivery and after the 10-second time period passes, the above statement delivers result events in two invocations to listeners and the subscriber: The first invocation delivers an array of two events that contains zone A events with id 1 and 3. The second invocation delivers an array of 1 event that contains a zone B event with id 2.
		</para>
		
		<para>
			The next example specifies the <literal>for</literal>-clause and discrete delivery:
		</para>
		<programlisting><![CDATA[select * from RFIDEvent#time_batch(10 sec) for discrete_delivery]]></programlisting>

		<para>
			With discrete delivery and after the 10-second time period passes, the above statement delivers result events in three invocations to listeners and the subscriber: The first invocation delivers an array of 1 event that contains the event with id 1, the second invocation delivers an array of 1 event that contains the event with id 2 and the third invocation delivers an array of 1 event that contains the event with id 3.
		</para>

		<para>
		  Remove stream events are also delivered in multiple invocations, one for each group, if your statement selects remove stream events explicitly via <literal>irstream</literal> or <literal>rstream</literal> keywords.
		</para>

		<para>
		  The <literal>insert into</literal> for inserting events into a stream is not affected by the <literal>for</literal>-clause.
		</para>

		<para>
		  The delivery order respects the natural sort order or the explicit sort order as provided by the <literal>order by</literal> clause, if present.
		</para>

		<para>
			The following are known limitations:
		</para>
		<orderedlist spacing="compact">
			<listitem>
				<para>
					The engine validates <emphasis>group_expression</emphasis> expressions against the output event type, therefore all properties specified in <emphasis>group_expression</emphasis> expressions must occur in the <literal>select</literal> clause.
				</para>
			</listitem>
		</orderedlist>			
	</sect1>
</chapter>
